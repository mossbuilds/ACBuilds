# ACE mod ideas (ACBuilds)

Verification: Player_Death.cs (OnDeath, Die, InflictVitaePenalty, CalculateDeathItems, GetNumItemsDropped, HandlePKDeathBroadcast, ThreadSafeTeleportOnDeath) and Player_Xp.cs (EarnXP, GrantXP, GrantLevelProportionalXp, SpendXP, RefundXP) verified via raw GitHub. Mods folder holds IHarmonyMod, ModManager, ModContainer, ModCommands, ModHelpers, ModMetadata (verified listing; BasicMod/BasicPatch API taken from local PatchClass.cs). Everything else marked UNVERIFIED must be checked in source before coding. WebSearch of forums/other mods was not done. Rules: no speed/velocity mods (client-side); no mods that add many world objects (Quest memory).

Common style: [HarmonyPatch] class : BasicPatch<Settings>, [CommandHandler(name, AccessLevel, CommandHandlerFlag.RequiresWorld, paramCount, desc, usage)], PlayerManager.GetOnlinePlayer, ActionChain(player, ...).EnqueueChain(), GameMessageSystemChat.

1. XpBoost (feas 5) - Configurable XP multiplier (per-level tiers, weekend event). Postfix/prefix on Player.EarnXP(long, XpType, ShareType) scaling amount by Settings.Multiplier. Cmds: /xpboost [x] (Admin). Settings: Multiplier, MaxLevel, XpTypes affected. Risks: double counting via GrantXP (which calls EarnXP? check); prefix arg change with ref long amount.
2. GentleDeath (feas 5) - Softer death: fewer dropped items, reduced vitae. Postfix on Player.GetNumItemsDropped(Corpse) (return __result, cap), prefix on InflictVitaePenalty(int) to scale amount. Settings: MaxItemsDropped, VitaeAmount, ProtectPlayerCorpseOnly. Risks: balance; PK paths.
3. WhereIsEveryone / PlayerFinder (feas 5) - Admin /who with location, level, landblock; /gotoplayer via player.Teleport(target.Location), /bring. Uses PlayerManager.GetAllOnline (UNVERIFIED name; existing code uses GetOnlinePlayer). Settings: none. Risks: minimal.
4. AnnounceEvents (feas 4) - Scheduled server broadcasts and timed XP/loot events (System.Threading.Timer + PlayerManager.BroadcastToAll (UNVERIFIED)). Cmds: /event start|stop name. Settings: schedule list. Risks: timer threading; wrap in ActionChain.
5. HomeStone / SetHome (feas 4) - /sethome saves player.Location to a settings JSON/DB property; /home teleports with cooldown, blocked in combat (player.LastPkAttackTimestamp / IsPKType UNVERIFIED). Settings: CooldownSeconds, AllowIndoors. Risks: teleport into invalid cell; use Teleport(Position).
6. SafeTutorZone / PkGuard (feas 3) - Disable PvP damage in configured landblocks by prefixing Player.TakeDamage / Creature damage path (UNVERIFIED exact method; inspect Player_Combat.cs). Settings: landblock list. Risks: skipping original with prefix returning false; hard to test.
7. AutoLoot / PickupRadius (feas 3) - /autoloot: on corpse open (Corpse.Open / Container.Open UNVERIFIED) move items to pack via player.TryCreateInInventoryWithNetworking. Settings: filters (pyreals, trade notes). Risks: pack full, races.
8. Buffbot Command (feas 4) - /buffme casts standard buff list via player.CreateSingleSpell / EnchantmentManager (UNVERIFIED); cooldown, free or cost. Settings: spell ids, cost. Risks: spell id correctness, balance.
9. Leaderboard (feas 4) - /top shows level/XP/kills from PlayerManager.GetAllPlayers (UNVERIFIED) plus kill tally via postfix on Player.OnDeath/Creature.Die. Settings: size. Risks: offline players load cost, cache.
10. AdminAudit (feas 5) - Log every admin command and spawn to a file. Patch CommandManager.ParseCommand (UNVERIFIED) prefix, or use own [CommandHandler]s. Settings: log path. Risks: none.
11. DeathReport (feas 5) - Postfix Player.OnDeath: broadcast fun message, log location, optional /lastdeath teleports player to death location (store Position from Location at Die). Settings: broadcast on/off. Risks: spam.
12. RestartWarn / Maintenance (feas 4) - /restartin N broadcasts countdowns then calls shutdown (ServerManager.Shutdown UNVERIFIED) and blocks logins in the window. Settings: warn intervals. Risks: shutdown API must be checked.

Ranking is by usefulness times feasibility; 1-3 and 10-11 are the best first builds.

---

## Round 2 (ideas 13-18), researched 2026-09-21 with WebSearch/WebFetch

Namespaces verified from raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server: Player partials = ACE.Server.WorldObjects; PlayerManager = ACE.Server.Managers. Anything else is marked UNVERIFIED. No speed mods, no many-object mods, no ace_auth/ace_shard access, no passwords.

13. MilestoneRewards (feas 4) - Grants skill credits (and a chat line) at configured levels/first-time milestones, a common small-server "no reason to reroll" QoL. Hooks: Postfix on Player.CheckForLevelup (ACE.Server.WorldObjects; UNVERIFIED name, inspect Player_Xp.cs) then player.AddSkillCredits(int) (verified in Player_Skills.cs, ACE.Server.WorldObjects). Notify via GameMessageSystemChat (ACE.Server.Network.GameMessages.Messages). Cmd: /milestones (player) shows next reward. Settings: LevelToCredits map, OneTimeOnly. Risks: double-grant on relog (track in mod's own JSON keyed by character id, not the DB); balance. Source: https://github.com/aquafir/ACE.BaseMod (patch-template ecosystem, credits/XP tweaks are typical samples).

14. IdleKick (feas 4) - Warns then logs off players idle beyond N minutes to free slots and stop AFK-camping. Hooks: Postfix on Player.Player_Tick(double) or Heartbeat(double) (both verified in Player_Tick.cs, ACE.Server.WorldObjects); compare to last-input time kept in a per-player dictionary updated in Postfix on Player.OnMoveToState(MoveToState) (verified); kick via Session.LogOffPlayer(bool) (used in Player_Tick.cs; class ACE.Server.Network.Session). Cmd: /idle (Admin) list idle times. Settings: WarnMinutes, KickMinutes, ExemptAdmins. Risks: chat/casting-only players look idle (also hook chat); Tick postfix cost, so throttle to once per few seconds. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Tick.cs (no idle logic exists, only teleport timeout).

15. TimedMute (feas 5) - Admin /mute name minutes [reason] with auto-unmute and a persisted list, wrapping stock gag. Hooks: PlayerManager.GagPlayer(Player issuer, string playerName) and UnGagPlayer(Player, string) (verified, ACE.Server.Managers); Timer/ActionChain to un-gag; Postfix Player.OnEnterWorld/PlayerEnterWorld() (verified, Player_Networking.cs) to re-apply. Cmds: /mute, /unmute, /mutes (AccessLevel.Sentinel, CommandHandlerFlag.RequiresWorld). Settings: DefaultMinutes, MaxMinutes. Risks: stock gag may already expire (GagsTick exists in Player_Tick.cs, check its semantics); relies on gag fields, do not touch DB directly. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/PlayerManager.cs

16. SkillRespec (feas 3) - /respec skillname lets a player un-specialize or untrain one skill and get credits back, with a cooldown and optional cost, a top request on private servers (mistaken specializations). Hooks: Player.UnspecializeSkill(Skill, int), UntrainSkill(Skill, int), ResetSkill(Skill, bool refund) (all verified, Player_Skills.cs, ACE.Server.WorldObjects; Skill enum = ACE.Entity.Enum). Prefer calling these from own [CommandHandler]; no patch needed. Settings: CooldownHours, CostPyreals (UNVERIFIED how to deduct: check Player.TryConsumeFromInventoryWithNetworking), AllowUntrain. Risks: refund exploits (spec/unspec loops: enforce cooldown); credits accounting, test on a throwaway char. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Skills.cs

17. PkNight (feas 4) - Scheduled PK windows (e.g. Friday 20:00-22:00) for servers that are PvE otherwise: announces, flips PK status for all online players, flips back, with warnings and an /pknight now|off admin override. Hooks: PlayerManager.UpdatePKStatusForAllPlayers(string worldType, bool enabled) and BroadcastToAll(GameMessage) (both verified, ACE.Server.Managers); timer wrapped in ActionChain. Settings: Schedule list, WorldType string (UNVERIFIED accepted values: read method body), WarnMinutes. Risks: players logging in mid-window need status set (Postfix PlayerEnterWorld); pairs with PkGuard, keep tutor zones safe. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/PlayerManager.cs

18. LoginGreeter (feas 5) - Per-login MOTD plus "who is online / what's new / next event" whisper, with a first-login welcome pack of tips. Hooks: Postfix Player.PlayerEnterWorld() (verified, ACE.Server.WorldObjects); PlayerManager.GetOnlineCount() and GetAllOnline() (verified); GameMessageSystemChat. Cmd: /motd (player), /setmotd text (Admin). Settings: MotdLines, ShowOnlineCount, FirstLoginTip. Risks: chat sent before client ready (delay 2-3 s via ActionChain); keep it small, no world objects. Source: https://github.com/tombohar/aeshnidae-server (fetch returned 404; idea taken from the search result listing of ACE mod-host servers) and PlayerManager.cs above.

### Sources consulted
Fetched OK (6): https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Tick.cs ; .../Managers/PlayerManager.cs ; .../WorldObjects/Player_Networking.cs ; .../WorldObjects/Player_Skills.cs ; https://github.com/aquafir/ACE.BaseMod ; https://github.com/ACEmulator/ACE/issues (enhancement filter returned no results).
Searched (1): "ACEmulator ACE mods Harmony server mod ideas github" (results: ACE.BaseMod, aeshnidae-server, ACE wiki, releases).
Failed (404): Player_Tells.cs, tombohar/aeshnidae-server.
Not reached: Thwargle fork, Discord/forum threads.

---

## Round 3 (ideas 19-24), researched 2026-09-21

Constraints kept: no speed mods, no many-object mods (Quest memory), no ace_auth/ace_shard access, no passwords. Command names checked against fetched PlayerCommands.cs / AdminCommands.cs listings (partial listings; run `grep -rn 'CommandHandler("name"' Source` before coding). Stock names seen and therefore AVOIDED: pop, myquests, config, reportbug, aceversion, gag, ungag, home, pk, finger, freeze, save, time, smite, snoop, serverlist, myserver, teleto, teletome, telereturn, limbo, regen, bornagain.

19. ServerPulse (feas 5) - Read-only admin health snapshot, answering ACE issue #718 "ACE.Server needs to expose an API for diagnostics" and the wiki's warning that ACE is demanding on hardware. One command prints online count, loaded landblocks, top N landblocks by creature and player count. Hooks: LandblockManager.GetLoadedLandblocks() returns List<Landblock> (verified, ACE.Server.Managers); PlayerManager.GetOnlineCount() (verified earlier, ACE.Server.Managers); per-landblock counts via Landblock members (ACE.Server.Entity.Landblock; member names UNVERIFIED, read Landblock.cs). Cmd: /pulse [top] (AccessLevel.Sentinel, CommandHandlerFlag.RequiresWorld). Settings: TopN. Risks: enumerating a snapshot is safe; do not touch landblock internals from the command thread beyond reading counts. Source: https://github.com/ACEmulator/ACE/issues (fetched) and https://github.com/ACEmulator/ACE/wiki/ACE-Hosting.

20. HotspotAlert (feas 4) - Quest-friendly crowd guard: a throttled timer scans loaded landblocks and whispers online admins when one exceeds a creature/player threshold (the cause of lag and headset memory pressure), once per cooldown per landblock. Never spawns or deletes anything. Hooks: LandblockManager.GetLoadedLandblocks() (verified); Landblock counts (UNVERIFIED, ACE.Server.Entity); PlayerManager.GetAllOnline() (verified earlier) filtered by Session.AccessLevel (UNVERIFIED); GameMessageSystemChat (ACE.Server.Network.GameMessages.Messages); timer wrapped in ActionChain. Cmd: /hotspots (Sentinel). Settings: CreatureThreshold, PlayerThreshold, ScanSeconds, AlertCooldownMinutes. Risks: scan cost, so keep to every 30+ s; false alarms at events. Source: https://github.com/ACEmulator/ACE/wiki/ACE-Hosting (fetched, load scaling and thread settings).

21. StatCard (feas 4) - Player command that prints a readable one-screen character summary (level, XP to next level, unassigned credits, luminance if present, attribute and vital totals, top skills, deaths) in plain chat lines, from the ACE Discussions "view_readable_stats" idea (RandomTurtle, Jan 2026). Read-only, no world objects, useful for VR players who cannot easily open panes. Hooks: own [CommandHandler] only; reads Player members in ACE.Server.WorldObjects (Level, TotalExperience, AvailableSkillCredits, Strength/Endurance etc. property names UNVERIFIED, read Player_Skills.cs and Creature_Attributes.cs); Skill enum = ACE.Entity.Enum; output via GameMessageSystemChat. Cmd: /statcard [top] (AccessLevel.Player). Settings: TopSkills, ShowDeaths. Risks: property names, avoid Player.GetXpToNextLevel-style guesses. Source: https://github.com/ACEmulator/ACE/discussions (fetched).

22. BugNote (feas 5) - Lets players leave a short note that goes to a mod-owned log file and a whisper to online admins, so a small private server hears about broken quests, stuck spots and exploits without Discord. Distinct from stock reportbug (which exists in PlayerCommands.cs, so this must not reuse that name; check what reportbug does first and drop the idea if it already logs). Hooks: own [CommandHandler]; PlayerManager.GetAllOnline() (verified earlier); records Player.Location (ACE.Entity.Position, namespace UNVERIFIED) and name; file write only in the mod folder. Cmds: /bugnote text (Player, 1 param, rate limited), /bugnotes [n] (Sentinel) reads the last n. Settings: CooldownSeconds, MaxLength, LogPath. Risks: spam (cooldown), no personal data beyond character name. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Command/Handlers/PlayerCommands.cs (fetched, shows reportbug).

23. LoginShield (feas 3) - Short damage immunity after entering the world, addressing ACE issue #950 "Improve Player EnterWorld Process" (players arriving into aggro, invisible or unloaded surroundings). Immunity lasts N seconds or until the player attacks or moves far. Hooks: Player.PlayerEnterWorld() and Player.LoginTimestamp (both verified from Player_Networking.cs, ACE.Server.WorldObjects; LoginTimestamp is set to Time.GetUnixTime() there); prefix on Player.TakeDamage (same method PkGuard patches; take its exact signature from the local PkGuard PatchClass.cs, still UNVERIFIED against master). Settings: ShieldSeconds, BreakOnAttack. Risks: exploit as a safe-fight buffer (break on outgoing attack), PvP interaction with PkGuard and PkNight (patch order). Source: https://github.com/ACEmulator/ACE/issues (fetched, #950).

24. SettingsPeek (feas 4) - Read-only admin view of ACE server properties (bool/long/double/string) so staff can check rates and flags in game without opening the database. Never modifies anything; uses ACE's own in-memory PropertyManager, not ace_shard. Hooks: PropertyManager.GetBool/GetLong/GetDouble/GetString(string key, fallback) and ListProperties() (all verified, ACE.Server.Managers; stock 'config' is a player command, so name differs). Cmd: /peekprop key (Admin; command name UNVERIFIED for clashes, grep first). Settings: AllowedPrefixes (restrict to safe keys, hide anything credential-like). Risks: some property values could be sensitive, hence the allowlist; Property<T> value member name UNVERIFIED (read Property.cs). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/PropertyManager.cs (fetched).

### Sources consulted (round 3)
Fetched OK (14 distinct pages): https://github.com/ACEmulator/ACE/tree/master/Source/ACE.Server/Mods ; https://github.com/aquafir/ACE.BaseMod ; https://github.com/aquafir/ACE.BaseMod/wiki/Getting-Started ; https://github.com/ACEmulator/ACE/issues?q=is%3Aissue+label%3Aenhancement (empty) ; https://github.com/ACEmulator/ACE/issues ; https://github.com/ACEmulator/ACE/discussions ; https://github.com/Thwargle/ACE (no fork description) ; https://github.com/ACEmulator/ACE/wiki ; https://github.com/ACEmulator/ACE/wiki/Content-Creation ; https://github.com/ACEmulator/ACE/wiki/ACE-Hosting ; raw .../Managers/LandblockManager.cs ; raw .../Managers/PropertyManager.cs ; raw .../Command/Handlers/PlayerCommands.cs ; raw .../Command/Handlers/AdminCommands.cs ; raw .../WorldObjects/Player_Networking.cs (15 counting all).
Searched: 2 WebSearch queries (ACE Harmony mods; private-server wishlist reddit, which returned no community write-ups).
Failed 404: acemulator.github.io/ACE, tombohar/aeshnidae-server. Not reachable: Discord, reddit threads, forum posts (no community wish-list source was found, so ideas draw on GitHub issues/discussions).


---

## Round 4 (ideas 25-30), researched 2026-09-21: port-plan Phase 0/4 and server quality

Ranked by port value x feasibility. Namespaces were read in the raw files fetched this round (ACE.Server.WorldObjects unless stated). Command names checked against %TEMP%\cmds.txt (327 built-ins): order, burst, ambush, path, unstuck, hold, follow are absent (re-grep before coding). No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

25. MinionCleanup (feas 4) - Fixes what RaiseSkeleton needs: stock ACE tracks ONE pet per player (Player.CurrentActivePet) and destroys it in Player.LogOut_Inner(bool clientSessionTerminatedAbruptly = false); Pet.SlowTick() destroys a pet when its owner is beyond MaxDistance (192.0), so minions vanish on long teleports, and nothing handles owner death. This mod keeps a mod-side registry owner guid -> minion guids and (a) on a Player.LogOut_Inner prefix destroys all registered minions, (b) on Player.Die (protected override; patch via AccessTools) destroys or dismisses them, (c) on Player.Teleport(Position _newPosition, bool fromPortal = false) Postfix re-places them via ActionChain 3 s later at the owner's new Location (or destroys them, setting), (d) a Heartbeat sweep drops registry entries whose object is gone and enforces a hard ControlLimit. Classes: Player.LogOut_Inner, Player.Die, Player.Teleport, Pet.P_PetOwner, Pet.SlowTick, CombatPet.Init(Player, PetDevice) (all ACE.Server.WorldObjects, verified); WorldObject.Destroy(); ActionChain (ACE.Server.Entity.Actions, UNVERIFIED). Offers a static Register/Unregister/Count API to RaiseSkeleton. Cmd: /minionclean (Admin) shows registry counts. Settings: DestroyOnLogout, DestroyOnDeath, FollowThroughTeleport, ControlLimit, SweepSeconds. Risks: re-place must use RaiseSkeleton's spawn path (never clone); double destroy (check IsDestroyed); wrap Destroy in ActionChain. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Pet.cs

26. MinionOrders (feas 3) - Squad orders: /order attack|hold|follow|recall. Stock CombatPet picks its own targets: FindNextTarget() (public override bool) chooses from GetNearbyMonsters(), HandleFindTarget() validates; Pet.StartFollow() walks to the owner beyond MinDistance 2.0. Prefix on CombatPet.FindNextTarget returns false while a minion is on Hold/Follow so it never engages; Attack sets the pet's AttackTarget to the owner's selection (owner selection member UNVERIFIED) then lets original logic run; Recall calls Pet.StartFollow() and Sleep(). Legality via Creature.CanDamage(Creature) (verified, Creature_Combat.cs) so orders cannot make a pet hit players or NPCs. Cmds: /order <attack|hold|follow|recall> [all|name] (Player, RequiresWorld); state in a ConditionalWeakTable<CombatPet, OrderState>, no DB. Settings: DefaultOrder, OrderCooldownMs, AllowAttackPlayers=false. Risks: prefix must not starve HandleFindTarget on Hold; several pets per owner is not native (pairs with MinionCleanup). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/CombatPet.cs

27. PathChoice (feas 4) - /path necromancer|rogue|fighter|mage with a respec cooldown: the Phase 0 gate. Stores the path as a quest stamp through QuestManager (ACE.Server.Managers; Stamp, HasQuest, Erase, GetCurrentSolves verified; the Player accessor name is UNVERIFIED because Player_Quests.cs 404ed). Grants a title with Player.AddTitle(CharacterTitle title, bool setAsDisplayTitle = false) or AddTitle(uint, bool) (verified, Player_Character.cs; CharacterTitle in ACE.Entity.Enum, existing ids only). Exposes static PathChoice.GetPath(Player) so RaiseSkeleton, CorpseBurst and AmbushStrike refuse wrong paths, and the quest-name convention lets NPC emotes gate content with no code. Cmds: /path, /path <name> confirm (Player); /pathset <player> <name> (Admin). Settings: Paths, RespecCooldownHours, TitleIds. Risks: stamps persist through ACE itself (never write SQL); enum id typos; block respec in combat. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/QuestManager.cs

28. CorpseBurst (feas 3) - Necromancer corpse explosion with an existing war spell: /burst consumes the nearest monster Corpse (ACE.Server.WorldObjects; TimeToRot, IsLooted, VictimId verified) and casts an existing DAT spell id on monsters near it. Spell-casting API UNVERIFIED (WorldObject_Spells.cs 404ed; read Player_Magic.cs before coding). Gated by path necromancer and cooldown. Cmd: /burst (Player). Settings: SpellId, CooldownSeconds, MaxTargets, CorpseMaxAgeSec. Risks: shares corpses with RaiseSkeleton (one consume flag), AoE hitting players (filter with CanDamage), no new visual effects. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Corpse.cs

29. AmbushStrike (feas 2) - Rogue stealth approximated. ACE has Creature.GetSneakAttackMod(WorldObject target) (verified: from behind, Deception lowers the angle needed) and monsters skip Hidden or Teleporting players in GetAttackTargets (verified, Monster_Awareness.cs, ACE.Server.WorldObjects). /ambush temporarily shrinks awareness for that player by Postfix on the visual-awareness range (default 18.0, scaled by mob_awareness_range; accessor UNVERIFIED), breaks on attack or damage, and Postfix-boosts GetSneakAttackMod. Cmd: /ambush (Player, rogue). Settings: DurationSeconds, RangeFactor, DamageBonus, CooldownSeconds. Risks: true invisibility impossible (no new effects), so it is reduced aggro only; hot-path patch costs CPU (test player identity first); boss exploit. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Monster_Awareness.cs

30. StuckRescue (feas 4) - General: /unstuck returns a player to their last safe grounded Position when wedged in geometry, with cooldown, blocked in combat. Samples Player.Location in a Heartbeat(double currentUnixTime) postfix (runs about every 5 s, verified, Creature_Tick.cs) and restores with Player.Teleport(Position, bool fromPortal = false) (verified). Position = ACE.Entity.Position (UNVERIFIED namespace). Cmd: /unstuck (Player). Settings: CooldownMinutes, SampleSeconds, BlockInCombat. Risks: combat-escape exploit (use last-attack timestamp as HomeStone does), samples inside sealed cells. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Creature_Tick.cs

Control-limit HUD: fold into RaiseSkeleton /minions using MinionCleanup.Count and the floor(Self/10) limit; no separate mod.

### Sources consulted (round 4)
Fetched OK (13 raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/ pages): WorldObjects/CombatPet.cs ; Pet.cs ; Player_Death.cs ; Player_Location.cs ; Player_Networking.cs ; Creature_Combat.cs ; Monster_Awareness.cs ; PetDevice.cs ; Corpse.cs ; Creature_Tick.cs ; Player.cs ; Player_Character.cs ; Managers/QuestManager.cs.
Failed 404: WorldObjects/WorldObject_Spells.cs, WorldObjects/Player_Quests.cs. No WebSearch, wiki, forum or fork pages were read this round (source files only), and AWAKEN_PORT_PLAN.md and RaiseSkeleton were not read.


## Round 5 (ideas 31-36), researched 2026-09-21 WITH community/wiki pages

Ranked by value x feasibility. Community evidence is thin (ACE has little public player-request material); each idea says honestly what supported it. Namespaces verified in raw files this round: CombatPet, Pet, Corpse, Player (XP: EarnXP/GrantXP in Player_Xp.cs) are all ACE.Server.WorldObjects; PropertyManager is ACE.Server.Managers (GetBool/GetLong/GetDouble verified; setter name UNVERIFIED). Fellowship location and WorldObject_Spells.cs were 404, so any spell-cast API is UNVERIFIED. Command names (petspells, corpses, mods, lite, redraw, mentor) must be re-grepped against %TEMP%\cmds.txt before coding.

31. PetSpells (feas 3) - Support minions from existing spells. ACE has the server property advanced_combat_pets ("non-retail function: Combat Pets can cast spells", verified in PropertyManager.cs, default False), so stock pets can already cast if the weenie carries a spell book. Mod: an admin/opt-in switch plus a settings table mapping minion weenie id -> existing DAT spell ids (e.g. a healer skeleton casting Heal Other every 7 s, a fear/curse debuffer using an existing debuff), applied by a postfix on CombatPet.Init(Player, PetDevice) (ACE.Server.WorldObjects, verified signature). Casting API UNVERIFIED. Settings: table, cast interval, mana-free flag. Risks: pets spamming spells lag the tick; balance; needs advanced_combat_pets on. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (advanced_combat_pets, pet_stow_replace); the 2013 forum request for healing/support summons appeared only as a WebSearch snippet, its page (asheron7.rssing.com) returned empty, so treat as weak.

32. DecayClock (feas 4) - /corpses (player) lists your own corpses with location and time left, and whispers a warning at 5 and 1 minute before decay, so death recovery is less of a scramble. Reads Corpse.TimeToRot (ACE.Server.WorldObjects, verified round 4); locating a player's corpses UNVERIFIED (search the landblocks or hook Player.OnDeath in Player_Death.cs / Creature_Death.cs, both verified to exist). Also fits necromancer flavour: the same timer feeds CorpseBurst. Settings: warn minutes, max listed. Risks: iterating loaded landblocks; keep it to a tracked list added at death. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (corpse_spam_limit 15, lifestone_broadcast_death, permit_corpse_all show corpses are a live operator concern).

33. CommandIndex (feas 5) - /mods lists every custom mod command with a one-line help, generated from a mod-owned JSON, because ACE's own list (@acecommands, @acehelp, 327 built-ins) buries them and players cannot discover /path, /order, /raise, etc. Only chat output; no ACE patches beyond registering the command via the mod CommandHandler attribute (used by existing mods). Settings: entries per mod. Risks: stale list. Source: https://github.com/ACEmulator/ACE/wiki/Ace-Commands (large flat command list, mostly admin) and https://github.com/aquafir/ACE.BaseMod (mods managed by a /mod verb, so mods are meant to be user-visible).

34. MentorRank (feas 2) - Lightweight NPC-guild style rank for veterans who help newcomers: mentor points accrue when a high-level player kills in a fellowship with a low-level one; rank titles at thresholds, a small chat announcement, an optional XP-share bonus via Player.GrantXP(long, XpType, ShareType) (ACE.Server.WorldObjects, verified). Fellowship member enumeration UNVERIFIED (Fellowship.cs not found at the guessed paths). Persist in mod JSON, never the DB. Risks: farming with alts; needs a level-gap cap. Source: https://indiecator.org/2019/09/06/late-to-the-party-asherons-call/ (a new player on a small emulator server credits veterans who led him through dungeons and buffed him; onboarding is community-driven).

35. Redraw (feas 2) - /redraw asks the server to re-send nearby objects to your client, for the invisible players/monsters bug class that stock ACE still carries. Would use the tracked-object refresh in Player_Networking.cs / Player_Location.cs (both exist; method names UNVERIFIED), with a cooldown and no effect in combat. Risks: may not fix server-side visibility; low certainty. Source: https://github.com/ACEmulator/ACE/issues (open issues #3954 Invisible Monsters, #3880 Invisible players).

36. HeadsetPreset (feas 3) - /headset on|off applies a low-noise preset of character options for standalone Quest players: fewer chat lines (DoT messages, spam), leaving world-object count untouched. ACE already has the /config command and server property player_config_command plus show_dot_messages (verified on the wiki). Mod wraps a set of options in one command and re-applies at login. Setter for character options UNVERIFIED (Player character-option API not read). Risks: options only affect messages, not client memory; do not promise performance. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options and https://github.com/ACEmulator/ACE/wiki/Ace-Commands (@config). No headset-player community page was found.

### Sources consulted (round 5)
WebSearch queries (6): "ACEmulator ACE server mod ideas players want Asheron's Call emulator"; "Asheron's Call private server necromancer summoner minions feature request"; "ACE.Shared mod Harmony ACEmulator mods repository"; "Asheron's Call emulator small server quality of life suggestions reddit"; "ACEmulator ACE github issues discussions server admin tooling request"; "Asheron's Call Awakening class paths necromancer rogue Return of the Lost Legions".
Non-source pages fetched OK (8): github.com/ACEmulator/ACE/wiki/Server-Configurable-Options ; github.com/ACEmulator/ACE/wiki/Ace-Commands ; github.com/ACEmulator/ACE/wiki/ACE-Hosting ; github.com/ACEmulator/ACE/discussions (only 4 threads: view_readable_stats, Extensibility) ; github.com/ACEmulator/ACE/issues ; emulator.ac/blog/ ; indiecator.org late-to-the-party post ; github.com/aquafir/ACE.BaseMod (README body not returned, thin).
Failed: asheron.fandom.com Summoning, Necromancer, Returning Player Guide (402); asheron7.rssing.com (empty); tombohar/aeshnidae-server (404); rpgcodex (403); vdev.mmorpg.com (522); emulator.ac/server-setup-guide (socket hang up).
Raw source files fetched (verification only): CombatPet.cs, Player_Xp.cs, Creature_Death.cs, PropertyManager.cs, Player_Death.cs, Corpse.cs (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/...); Fellowship.cs and Creature_Magic.cs 404.
Not read: AWAKEN_PORT_PLAN.md and the mod folders (overlap checked only via IDEAS.md titles).


## Round 6 (ideas 37-42), researched 2026-09-21 - hooks read in raw source, evidence from fetched pages

Ranked by value x feasibility. Command names (modtest, rent, tpback, roster, pricecheck, tradelog) were grepped against %TEMP%\cmds.txt: none clash (existing built-ins deliberately avoided: myquests, show-allegiances, fix-allegiances, vendordump, adminhouse, housekeep). Namespaces verified: Player, House, SlumLord, Vendor, WorldObject = ACE.Server.WorldObjects; HouseManager, AllegianceManager, PlayerManager, QuestManager = ACE.Server.Managers; AllegianceNode = ACE.Server.Entity; CommandManager = ACE.Server.Command; Position = ACE.Entity. Entity/Allegiance.cs 404ed, so only AllegianceNode and AllegianceManager are relied on.

37. ModTest (feas 4) - Admin /modtest runs safe read-only self-checks of every installed custom mod and prints PASS/FAIL per mod, so the owner can verify the many untested mods in one command. Checks per mod, public APIs only: (a) the mod assembly is loaded (scan AppDomain.CurrentDomain.GetAssemblies()); (b) its commands are registered: CommandManager.GetCommands() (ACE.Server.Command, public static IEnumerable<CommandHandlerInfo>, verified) contains every expected name from an owner-edited list; (c) its Harmony patches are live: HarmonyLib.Harmony.GetAllPatchedMethods() then Harmony.GetPatchInfo(method).Owners contains the mod's Harmony id (Harmony is a public library, not ACE); (d) its Settings.json parses and Enabled is reported (JSON parse of the mod folder file only); (e) optional read-only PropertyManager.GetBool/GetLong probe. It never spawns, damages, teleports or writes. Output lines like "RaiseSkeleton PASS cmds 3/3 patches 2/2 enabled=true". Settings: ExpectedMods map name -> expected commands, patch target names and Harmony id; ModsFolder. Risks: Harmony ids differ per mod (list them in Settings); a PASS proves wiring, not behaviour. Cmd: /modtest [modname] (Admin, RequiresWorld). Motivation: the ACE.BaseMod Getting Started page has no testing guidance and only /mod list|enable|find|method, no verify-loaded check: https://github.com/aquafir/ACE.BaseMod/wiki/Getting-Started ; corroborated by https://github.com/ACEmulator/ACE/wiki/Development-Resources (only a Decal button helper is listed). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Command/CommandManager.cs

38. TradeLedger (feas 4) - ACE keeps no record of player-to-player trades, so a "I was scammed / lost an item" complaint cannot be investigated. Mod appends one line per completed trade to tradeledger.log in the mod folder: time, both character names, item names and stack sizes each side, pyreal value each side. Hook: Harmony patch on the private Player.FinalizeTrade(Player target) (ACE.Server.WorldObjects, Player_Trade.cs line 218, verified; patch by name via [HarmonyPatch(typeof(Player), "FinalizeTrade")]); reads the public HashSet<ObjectGuid> ItemsInTradeWindow of both players and the public ObjectGuid TradePartner; item objects resolved before the swap through a Player inventory lookup (name UNVERIFIED, read Player_Inventory.cs first); prefix always returns true so behaviour is untouched. Admin /tradelog [player] [count] tails the file. No ace_shard access, no passwords, no account ids. Settings: LogFile, MaxLines, MinValueToLog, FlagValueMismatch (flag when one side is over N times the other, a scam indicator). Risks: prefix runs before FinalizeTrade's own checks, so log "attempted" and confirm in a postfix; file IO on the world thread (queue writes). Motivation: emulator.ac monthly reports list inventory and trade fixes as a recurring category (https://emulator.ac/category/monthly-report/) and the command list has no trade or item-history admin command (https://github.com/ACEmulator/ACE/wiki/ACE-Commands, seen in search results). Evidence is indirect (no issue asks for it); value is admin judgment. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Trade.cs

39. RentReminder (feas 4) - House maintenance is only visible in the client Housing tab and a missed payment evicts the owner. Player /rent shows, per house you own, the next rent/maintenance due date and time left; a login whisper warns when it is under N days. Hooks: HouseManager.GetCharacterHouses(uint playerGuid) (ACE.Server.Managers, public static List<House>, verified) then House.GetRentDue(uint purchaseTime) (public, verified House.cs line 205; apartments use 90-day periods and houses 30, handled inside) with purchaseTime from the owner's HousePurchaseTimestamp (IPlayer property, used at House.cs line 107). Optionally SlumLord.IsRentPaid() (public bool, verified). Login hook: same pattern as LoginGreeter. Read-only, no files. Settings: WarnDays (3), WarnAtLogin. Risks: house cache may be empty for offline-loaded houses (test with a real owner); show due time in UTC. Motivation: ACE progress reports list house maintenance, rent payment, eviction and multi-house handling as major features, and the due date is otherwise only in the client: https://emulator.ac/category/monthly-report/ (fetched). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/House.cs

40. TeleBack (feas 4) - Admin /tpback undoes the last teleport of the calling staff member (or /tpback <player>), for when a /telepoint, /teleloc or gotoplayer sends staff somewhere wrong and the previous spot is lost. Hook: Harmony prefix on Player.Teleport(Position _newPosition, bool fromPortal = false) (ACE.Server.WorldObjects, public, verified Player_Location.cs line 654) pushes the current Location (ACE.Entity.Position) into an in-memory ring (last 5), for Advocate-and-above only (Session.AccessLevel) so normal players add no cost; /tpback calls Player.Teleport with the popped Position. Complements WhereIsEveryone by adding the return leg. Settings: RingSize, StaffOnly (true), IncludePortals (false). Risks: no persistence across restart (intended); prefix must not recurse on its own teleport. Motivation: ACE issue #1586 shows staff admin-tool gaps around cloak/teleport handling: https://github.com/ACEmulator/ACE/issues/1586 (weak: motivates admin QoL, not undo specifically). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Location.cs

41. AllegianceRoster (feas 3) - Player /roster prints your allegiance tree as text: member, level, rank number, online or offline, and the count online now, so a monarch can manage a large allegiance without opening the client tree node by node. Admin /roster <player> for any allegiance. Hooks: AllegianceManager.GetAllegianceNode(IPlayer) (public static, verified line 99), AllegianceNode.Walk(Action<AllegianceNode> action, bool self = true) (public, ACE.Server.Entity, verified), node.Player (IPlayer, verified), PlayerManager.GetOnlinePlayer(guid) for online state; level/name from IPlayer (Level accessor UNVERIFIED, read IPlayer.cs). Built-in show-allegiances is an admin dump of all allegiances; this is player-facing, one tree, activity view. Read-only, no files. Settings: MaxLines, ShowOffline. Risks: privacy (show online/offline only, no last-seen); paging for big trees. Motivation: monthly reports list allegiance chat channels, officer ranks, MOTD and mansion permissions arriving piecemeal, so allegiance management is a live gap: https://emulator.ac/category/monthly-report/ . Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/AllegianceManager.cs

42. PriceCheck (feas 3) - Economy sanity tool: Admin /pricecheck scans vendors in loaded landblocks and lists items where the vendor pays a player at least what it charges (an infinite-pyreal loop). Hooks: enumerate loaded landblocks as ServerPulse/HotspotAlert do (LandblockManager, ACE.Server.Managers), pick Vendor instances (ACE.Server.WorldObjects), walk Vendor.forEachItem(Action<WorldObject>) or DefaultItemsForSale/UniqueItemsForSale (public, verified) and compare Vendor.GetSellCost(WorldObject) (uint, player pays) with Vendor.GetBuyCost(WorldObject) (int, vendor pays; both public, Vendor.cs lines 573-587). Flag GetBuyCost >= GetSellCost. Read-only. Complements built-in vendordump (dumps one examined vendor, no arbitrage check) and helps after custom vendors are added via ac-creator. Settings: MaxReport, Ratio. Risks: only loaded landblocks are scanned (report how many); alt-currency vendors must be skipped (property name UNVERIFIED); results indicative. Motivation: monthly reports list vendor alternate currency and economy mechanics as active areas: https://emulator.ac/category/monthly-report/ ; only vendordump exists for vendors per https://github.com/ACEmulator/ACE/wiki/ACE-Commands (search result). Evidence indirect. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Vendor.cs

Dropped this round (4): a player quest-log tool (built-in /myquests exists); an admin-cloak radar fix (issue #1586: no per-player visibility hook, the HeadsetPreset trap); a /testspawn self-test creature (@create/@ci already spawn); a lost-item restore tool (needs ace_shard reads, forbidden; TradeLedger is the safe substitute).

### Sources consulted (round 6)
WebSearch queries (6): "ACEmulator ACE github issue enhancement request admin command"; "ACE emulator restore lost item admin request character inventory"; "Asheron's Call emulator housing rent maintenance quality of life ACE"; "ACE emulator allegiance tools mod monarch patron ACEmulator"; "ACEmulator ACE quest log flags admin questlist command"; "ACE.Shared mod Harmony ACEmulator mod template test command".
Non-source pages fetched (8): github.com/ACEmulator/ACE/issues/1586 ; github.com/ACEmulator/ACE/wiki/Development-Resources ; github.com/aquafir/ACE.BaseMod/wiki/Getting-Started ; github.com/aquafir/ACE.BaseMod ; emulator.ac/category/monthly-report/ ; emulator.ac/2020/06/06/acemulator-report-january-2020-june-2020/ ; github.com/ACEmulator/ACE/wiki/ACE-Development ; github.com/ACEmulator/ACE/blob/master/changelog.md (returned no entries). ACE-Commands wiki and asheron.fandom.com Housing seen only as search results.
Raw source verified (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/House.cs, SlumLord.cs, Vendor.cs, Player_Trade.cs, Player_Location.cs; Managers/HouseManager.cs, AllegianceManager.cs, QuestManager.cs; Entity/AllegianceNode.cs; Command/CommandManager.cs; Command/Handlers/DeveloperCommands.cs. 404: Entity/Allegiance.cs, Player_Quests.cs, GameActionTradeAccept.cs.

---

## Round 7 (ideas 43-48), researched 2026-09-21 - crafting, fellowship, corpse, allegiance, events, admin safety

Ranked by value x feasibility. Namespaces verified in raw files this round: Player_Crafting.cs and Corpse.cs are ACE.Server.WorldObjects; RecipeManager.cs, EventManager.cs, PlayerManager.cs, AllegianceManager.cs are ACE.Server.Managers; Fellowship.cs is ACE.Server.Entity. Command names (craftcheck, fellowpulse, lootwatch, amotd, eventclock, zonesweep) checked against %TEMP%\cmds.txt: none clash (built-ins avoided: @fellow-dist, @fellow-info, @fellowbuff, @show-allegiances, @fix-allegiances, @event_start/@event_stop/@event_list, @permit, @cisalvage, @cm, @craft_exact_msg-as-property). Dropped 3 this round (below).

43. CraftForecast (feas 4) - Lets a player preview a combine's odds before spending materials. ACE's own `craft_exact_msg` server property (verified, Server-Configurable-Options wiki) only shows the exact success percentage *after* a combine if the operator turns it on server-wide; there is no pre-combine preview at any setting. Mod adds a read-only player command that, given the tool and target the player is holding/targeting, calls the same chance functions ACE itself uses: `RecipeManager.GetRecipeChance(...)` and `RecipeManager.GetTinkerChance(...)` (both `double?`, verified this round, ACE.Server.Managers, exact parameter list to be confirmed against the current overloads before coding) and, for salvaging, `Player.GetStructure(WorldObject salvageItem, SalvageResults salvageResults, ref SalvageMessage message)` (verified, Player_Crafting.cs, ACE.Server.WorldObjects) to show expected salvage yield by workmanship/material. No combine is performed, no materials are touched. Cmd: /craftcheck (Player). Settings: none required (read-only). Risks: the chance functions may require internal state (last-tinker-attempt count) that a preview call could mutate if it is not a pure read - verify before calling from a command instead of the real combine path. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (craft_exact_msg, tailoring_intermediate_uieffects, salvage_handle_overages).

44. FellowshipPulse (feas 4) - Player-facing fellowship overview; ACE's only fellowship introspection tools are the Sentinel-only debug commands `@fellow-dist` and `@fellow-info` (verified, ACE-Commands wiki), so an ordinary fellowship leader has no in-game way to see who is in XP-share range. Hooks: `Fellowship.GetFellowshipMembers()` (returns `Dictionary<uint, Player>`, verified this round, ACE.Server.Entity) and `Fellowship.WithinRange(Player player, bool includeSelf = false)` (verified) to mark each member as in-range or not; `Fellowship.FellowshipName` for the header. Cmd: /fellowpulse (Player, must be in a fellowship). Settings: none. Risks: WithinRange's distance threshold is ACE's own (not configurable here); read-only, no patches. Source: https://github.com/ACEmulator/ACE/wiki/ACE-Commands (fellow-dist/fellow-info listed as the only fellowship tools, both admin-only).

45. LootWatch (feas 3) - Corpse loot-permission audit log for disputes ("someone looted my kill"). ACE already enforces kill-priority itself: `Corpse.HasPermission(Player player)` (verified this round, ACE.Server.WorldObjects) returns true for the recorded `KillerId`, once `IsLooted`, or once `HalfLife` (static int, 180 seconds, verified) has elapsed on a monster corpse - the same rule the monthly report calls out ("after the top damager finishes looting, other players may loot"). But nothing records who actually opened a corpse and when, so a "you ninja-looted me" complaint (the shape of issue #1170, a creature "snuck into loot" on a corpse) cannot be checked afterward. Mod adds a Harmony postfix on `Corpse.Open(Player player)` (verified signature) that reads KillerId/IsLooted/HalfLife and the opener's guid and appends one line to lootwatch.log; never denies or changes an open. Admin /lootwatch [name] tails the file. Settings: LogFile, MaxLines. Risks: postfix must not assume HasPermission was already true (log the actual outcome, not an assumption); file IO off the world thread. Source: https://github.com/ACEmulator/ACE/issues/1170 (creature/permission anomaly on a corpse) and https://emulator.ac/2019/03/31/acemulator-monthly-report-march-1-2019-april-1-2019/ (top-damager-then-everyone looting rule).

46. AllegianceMotd (feas 3) - A monarch-settable allegiance message shown to vassals at login. Verified this round that `Fellowship.cs` (ACE.Server.Entity) stores only `FellowshipName` and a `FellowshipLocks` dictionary - no message field - and the monthly report shows ACE's only allegiance-login text is the fixed Monarch/Patron/Vassal-colored rank line, not a custom one; there is no built-in allegiance MOTD anywhere in the wiki command or property lists fetched this round. Mod stores one line per monarch guid in its own JSON (never the DB) and, on `Player.PlayerEnterWorld()` (verified earlier rounds, ACE.Server.WorldObjects), looks up the player's monarch via `AllegianceManager.GetAllegianceNode(IPlayer)` (verified round 6, ACE.Server.Managers) and walks to the root with `AllegianceNode.Walk(...)` (verified round 6) to find that monarch's stored line, then sends it with `GameMessageSystemChat`. Cmds: /amotd <text> (monarch only, checked via the node's rank), /amotd (any vassal, shows it on demand). Settings: MaxLength. Risks: node walk cost on every login (cache per session); a monarch who changes the text expects it to reach members already online too (also broadcast to online vassals via `AllegianceNode.Walk` + `GameMessageSystemChat` at set time). Source: https://emulator.ac/2019/03/31/acemulator-monthly-report-march-1-2019-april-1-2019/ (login messages get rank prefixes/colors only) and https://github.com/ACEmulator/ACE/wiki/ACE-Commands (@show-allegiances/@fix-allegiances are admin-only, no player messaging command exists).

47. EventClock (feas 4) - Runs ACE's own built-in world-event system on a schedule instead of by hand. ACE already has a full event on/off system driven from the ace_world `events` table - `EventManager.StartEvent(string e, WorldObject source, WorldObject target)`, `StopEvent(...)`, `IsEventStarted(...)`, `GetEventStatus(string e)` (all verified this round, ACE.Server.Managers) - exposed only through the manual Sentinel commands `@event_start`/`@event_stop`/`@event_list` (verified, ACE-Commands wiki), so a retail-style timed world event (a holiday drop table, a generator toggle already authored as an ace_world event) only turns on if a staff member is online and remembers at the right moment. Mod reads an owner-edited JSON schedule (event name -> start/stop times) and calls `EventManager.StartEvent`/`StopEvent` for names that already pass `IsEventAvailable(string e)` (verified) - it creates no new events, only toggles ones the operator already defined in the database. Admin /eventclock [reload|status]. Settings: ScheduleFile. Risks: a schedule boundary missed while the server is down is not caught up (log it, do not fire late); relies on IsEventAvailable so a typo'd name is a no-op, not an error the operator sees unless the mod checks and warns. Source: https://github.com/ACEmulator/ACE/wiki/ACE-Commands (@event_start/@event_stop/@event_list documented as the only, manual, controls) and https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (no schedule-related event property exists).

48. ZoneSweep (feas 3) - A scoped, two-step-confirm alternative to ACE's only bulk player action. `PlayerManager.BootAllPlayers()` (verified this round, ACE.Server.Managers) disconnects every player below Advocate server-wide with reason "WorldClosed" and no preview - the wiki's hosting/restart guidance assumes this all-or-nothing tool. Mod adds a landblock-scoped version with a required preview step: `/zonesweep <landblock>` lists the online players currently there (cross-referencing `PlayerManager.GetAllOnline()`, verified this round, against each player's current landblock) and the loaded creature count (reusing the `LandblockManager.GetLoadedLandblocks()` pattern verified in rounds 3-6); only `/zonesweep <landblock> confirm`, issued within 60 seconds of the preview, warns those players by chat and queues them with `PlayerManager.AddPlayerToLogoffQueue(Player player)` (verified this round, graceful, not `AddPlayerToFinalLogoffQueue`'s forced 15-minute path). No creature or object is touched. Settings: ConfirmWindowSeconds (60), ExemptStaff (true), WarnSeconds before the queue. Risks: a stale confirm after the window must be rejected and re-previewed; never expand scope beyond the named landblock. Source: https://github.com/ACEmulator/ACE/wiki/ACE-Hosting (restart guidance assumes booting everyone, no scoped option) and PlayerManager.cs's `BootAllPlayers` (raw source, confirms the only bulk-disconnect tool is global).

Dropped this round (3): a spellbook/known-spells audit tool - no enumerable "list known spells" API was found (only `Biota.SpellIsKnown(id)` for single-id checks; `Player_Magic.cs` and `Player_Spells.cs` expose no collection accessor), so no verified hook exists. An item-set bonus checker - no `ItemSetId`/`EquipmentSet` property was found anywhere in the fetched source or wiki pages, so this is not a real ACE mechanic to hook. A direct anti-ninja-loot blocker (deny the Open call outright) - `Corpse.HasPermission` already enforces this correctly per the monthly report, so overriding it would fight ACE's own stock behavior rather than fill a gap; LootWatch (item 45), a read-only audit log, is the safe substitute.

### Sources consulted (round 7)
WebSearch queries (8): "ACEmulator ACE github issue enhancement crafting salvage tinkering request"; "ACEmulator ACE fellowship discussions feature request wanted"; "ACEmulator ACE corpse loot permissions ninja looting request"; "ACEmulator ACE allegiance chat messaging feature discussion"; "ACEmulator ACE world event scheduling mod repository github"; "ACEmulator ACE new player onboarding tutorial feature request github"; "ACEmulator ACE tinkering salvage workmanship formula github source"; "ACEmulator ACE source \"PropertiesSpellBook\" OR \"GetKnownSpellsIds\" WorldObject" (no hook found, informed the drop); "ACEmulator ACE source EquipmentSet OR \"ItemSetId\" property Player" (no hook found, informed the drop).
Non-source pages fetched OK (8): github.com/ACEmulator/ACE/issues/1170 ; github.com/ACEmulator/ACE/wiki/ACE-Commands ; github.com/ACEmulator/ACE/wiki/Server-Configurable-Options ; github.com/ACEmulator/ACE/discussions ; emulator.ac/2019/03/31/acemulator-monthly-report-march-1-2019-april-1-2019/ ; github.com/aquafir/ACE.BaseMod ; github.com/ACEmulator/ACE/issues?q=is%3Aissue+is%3Aopen+label%3Aenhancement (no open enhancement-labeled issues found) ; github.com/ACEmulator/ACE/wiki/ACE-Hosting.
Failed (402): asheron.fandom.com/wiki/Death_and_Corpse_Commands.
Raw source verified (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/Player_Crafting.cs, Corpse.cs; Managers/RecipeManager.cs, EventManager.cs, PlayerManager.cs; Entity/Fellowship.cs; directory listings of WorldObjects/, Managers/, Entity/ (to locate Player_Crafting.cs, Fellowship.cs, EventManager.cs); Player_Spells.cs and Player_Magic.cs read but exposed no spellbook-enumeration method (informed the drop).

---

## Round 8 (ideas 49-54), researched 2026-09-22: community pain points (aeshnidae, GitHub issues/discussions, hosting wiki)

Checked all 48 existing one-line summaries in STATUS.md first; none of the six below overlap an existing mod, a stock ACE command (re-grepped %TEMP%\cmds.txt for stash/fellowshare/vendorstock/pkstatus/vendorwatch/refund - no clashes), or a Server-Configurable-Options property (those are set with `/modifybool` etc., not a mod). No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

49. HouseOverpayRefund (feas 4) - Fixes ACEmulator/ACE issue #1723 exactly: buying a house with more currency than it costs (e.g. dropping a Writ, a 10M note, and other notes in the buy window when the price is less) currently consumes everything instead of returning the change, per the reporter: "it will take all the items, including all 10 M Notes" and "Should be able to buy a house and drop any amount say 2 MMDs and have it return the unused/unneeded amount." Hooks (verified via raw Player_House.cs, ACE.Server.WorldObjects): `Player.HandleActionBuyHouse(uint slumlord_id, List<uint> item_ids)` orchestrates the purchase; `Player.GetTotalCurrency(List<WorldObject> sentItems, bool useTradeNotes = true)` and `Player.HasCurrency(List<WorldObject> sentItems, uint amount, bool useTradeNotes = true)` compute pyreal-equivalent value already; `Player.TryConsumePurchaseItems(List<WorldObjectInfo<int>> purchaseItems)` calls `TryConsumeFromInventoryWithNetworking(item, amount)` per item. A Harmony postfix on `HandleActionBuyHouse` re-derives `totalSent = GetTotalCurrency(sentItems)` and `cost` (house price, read the same way VerifyPurchase does) after the vanilla consume runs, and if `totalSent > cost` creates a pyreal stack for the difference via the player's own `TryCreateInInventoryWithNetworking`. Cmd: none, silent fix (optional Admin `/houserefundlog` to show the last N refunds). Settings: Enabled, LogRefunds. Risks: must recompute the same way the base game does or a rounding difference silently over/under-refunds - mirror `GetTotalCurrency`'s exact math, do not re-derive independently; test on a cheap house before a mansion. Source: https://github.com/ACEmulator/ACE/issues/1723 ("no check for overpay on house buying").

50. AccountStash (feas 3) - A shared pyreal stash across every character on one account, the pattern the Aeshnidae server's README calls out by name ("Per-account bank for pyreals, luminance, legendary keys and sturdy iron keys") as a thing private-server operators build because ACE has no such concept - characters on the same account cannot otherwise move currency without a mule trade. Hooks: `Session.Account` (verified via raw Network/Session.cs: `public string Account { get; private set; }`, namespace `ACE.Server.Network`, set via `SetAccount()`) is the stable per-account key, never the DB; a mod-owned JSON file maps `Account -> long pyrealBalance` (no ace_auth/ace_shard access, no per-character data). `/stash deposit <amount>` consumes pyreals from the caller via `Player.TryConsumeFromInventoryWithNetworking` (verified name, used the same way in Player_House.cs) and adds to the account balance; `/stash withdraw <amount>` does the reverse via `TryCreateInInventoryWithNetworking`. Settings: MaxBalance, DepositCooldownSeconds. Risks: two characters on the same account logged in at once (rare on ACE, one session per account is typical but not guaranteed) racing the balance file - guard every read-modify-write with a per-account lock; pyreals only, never luminance/keys (those need item-identity handling this mod does not do). Source: https://github.com/tombohar/aeshnidae-server (README summary, "Per-account bank for pyreals, luminance, legendary keys and sturdy iron keys" - Aeshnidae's own mod list).

51. FellowshipShareToggle (feas 3) - Lets a fellowship leader flip XP sharing without disbanding and re-forming the fellowship, which is currently the only way: verified via raw Entity/Fellowship.cs, `ACE.Server.Entity.Fellowship` exposes `public bool ShareXP;` and `public bool EvenShare;` as plain fields set at creation through `Player.FellowshipCreate(string fellowshipName, bool shareXP)` (verified, ACE.Server.WorldObjects), and XP sharing is only ever recalculated by a private `CalculateXPSharing()` method with no public setter - there is no in-game way to turn sharing on mid-session for a fellowship that started without it. Hook: own `[CommandHandler]` reads the caller's `Player.Fellowship` (verified accessor pattern from the same file), checks `FellowshipLeaderGuid == player.Guid` (leader-only), sets a mod-tracked `DesiredShareXP` and invokes the private `CalculateXPSharing()` via Harmony's `Traverse` (the same reflection pattern this repo already uses for protected members, e.g. AmbushStrike's `AttackHook`/`DamageEvent` access). Cmd: `/fellowshare [on|off]` (Player, leader only). Settings: CooldownSeconds (prevent on/off spam mid-fight). Risks: calling a private method by name through reflection breaks silently on an ACE update that renames it - guard with a try/catch and log if not found rather than throwing; must not touch `EvenShare` (a separate, retail-computed field) unless explicitly asked. Source: https://github.com/ACEmulator/ACE/discussions (Ideas category: "Extensibility", fartwhif, 2022 - the standing complaint that server behavior fixed at creation time cannot be adjusted without ACE code changes) plus the Fellowship.cs source read directly.

52. VendorStockPeek (feas 4) - Read-only player command answering "why did this vendor's stock change / will it restock" without opening the database, motivated by the Server-Configurable-Options wiki's own admission that `vendor_shop_uses_generator` ("enables or disables vendors using generator system in addition to createlist to create artificial scarcity") is silent to players - stock rules differ per-server and per-vendor with no in-game indicator. Hooks (verified via raw WorldObjects/Vendor.cs, `ACE.Server.WorldObjects.Vendor`): `OpenForBusiness` (PropertyBool, "controls vendor availability"), `ResetTimestamp` and `ResetInterval` ("time between vendor resets"), `UniqueItemsForSale` and `DefaultItemsForSale` (both `Dictionary<ObjectGuid, WorldObject>`), and `GeneratorProfiles` (inherited from `Creature`, referenced in `Vendor.SetEphemeralValues()` to filter shop-related generators, confirming generator-driven vendors are a real, readable case). Command reads the player's last-appraised object if it is a Vendor and prints: open for business y/n, seconds until next reset, count of default vs player-sold (unique) items for sale, and whether any generator profiles are attached (i.e. this vendor also restocks via the generator system). Cmd: `/vendorstock` (Player). Settings: none (read-only). Risks: `GeneratorProfiles`'s exact element type was not read this round - verify before coding (only its existence and purpose were confirmed); appraisal-target lookup must reuse whatever existing mod (PriceCheck) already uses for "last appraised object", not reinvent it. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (`vendor_shop_uses_generator` entry).

53. PkStatusInfo (feas 5) - Read-only player command showing your own PK timer/respite state in plain language, because the two settings that govern it - `pk_respite_timer` ("the number of seconds that a player killer is set to non-player killer status after dying to another player killer", default 300) and `pk_new_character_grace_period` ("the number of seconds, in addition to pk_respite_timer, that a player killer is set to non-player killer status after first exiting training academy", default 300) - are documented on the hosting wiki as server config, but nothing in stock ACE tells a player their own remaining window. Hooks (verified via raw WorldObjects/Player_Combat.cs, `ACE.Server.WorldObjects.Player`): `LastPkAttackTimestamp` (double, Unix time of last PK attack), `PkTimestamp` (double), `PKTimerActive` (bool property), `PKLogoutActive` (bool property, logout-freeze state), `PlayerKillerStatus` (inherited enum: PK/PKLite/NPK/Free), and the static `PKLogoutTimer` (`TimeSpan`, 2-minute freeze). Command prints current `PlayerKillerStatus`, whether `PKTimerActive`/`PKLogoutActive`, and seconds remaining computed from `LastPkAttackTimestamp` plus the server's configured `pk_timer`/`pk_respite_timer` (read via `PropertyManager.GetLong`, the same pattern SettingsPeek already uses). Cmd: `/pkstatus` (Player). Settings: none (read-only, no patches - pure property reads). Risks: none identified; purely informational, cannot be used to game the timer since it only reads state that already governs combat. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (`pk_respite_timer`, `pk_new_character_grace_period`, `pk_timer` entries).

54. StuckVendorWatch (feas 3) - Admin/Sentinel scan for a known ACE-community complaint pattern: a vendor NPC that goes non-functional (`OpenForBusiness` false) and never recovers because its `ResetTimestamp`/`ResetInterval` cycle stalled, which players report as "the vendor won't sell/buy anything" with no server-side alert. Reuses the same loaded-landblock scan pattern already verified and shipped in ServerPulse/HotspotAlert: `LandblockManager.GetLoadedLandblocks()` (verified, `ACE.Server.Managers`) returns loaded landblocks; the mod enumerates their `WorldObjects` (existing Landblock member, per the pattern HotspotAlert already uses) filtering to `Vendor` instances, and flags any vendor whose `OpenForBusiness` (verified, `ACE.Server.WorldObjects.Vendor`) is false for longer than `ResetInterval` past its `ResetTimestamp` (both verified same class) - i.e. a reset that should have happened did not. Cmd: `/vendorwatch` (Sentinel) lists flagged vendors by landblock and guid; never touches or resets a vendor itself, staff still fixes it by hand (`/reload`-style stock commands already exist). Settings: ScanSeconds (throttle, matching HotspotAlert's cadence), off by default. Risks: a vendor legitimately closed by design (an event, a quest-gated shop) would false-positive - this is read-only so the cost of a false positive is just a wasted look, but document the caveat in the command output itself. Source: https://github.com/tombohar/aeshnidae-server search-result summary of operator-built server-quality mods (audit trail / bank / relay - the class of "ACE doesn't tell operators when something silently breaks" problem) combined with the Vendor.cs fields read directly this round.

Dropped this round (2): an "Enlightenment" perks/rewards mod inspired by Aeshnidae's README line "NPC-driven enlightenment with configurable requirements, keeps and perks" - no `Player_Enlightenment.cs` or any `Enlightenment`-named property exists anywhere in `ACE.Server.WorldObjects` (checked the WorldObjects directory listing and guessed filenames both 404), so there is no verified ACE hook for an Enlightenment system to build on. A Discord chat-relay mod for Trade/General channels (also from Aeshnidae's mod list and a matching WebSearch for "chat relay discord webhook") - it would hook the same `EmoteManager.OnHearChat` surface AgentDialogue already patches for a different purpose (AI hooks) and, on reflection, reads as the same hook repurposed rather than a materially new capability; dropped rather than risk counting as a near-duplicate of an existing mod.

### Sources consulted (round 8)
WebSearch queries (6): "ACEmulator ACE server discord archive mod ideas wishlist"; "reddit r/acemulator ACE private server mod"; "reddit AsheronsCall ACE private server feature request"; "\"ACEmulator\" ACE server github mod repo besides ACE.BaseMod"; "Asheron's Call server private ACEmulator youtube setup tutorial"; "ACEmulator ACE github issues \"fellowship\" OR \"housing\" OR \"vendor\" enhancement" (plus 3 follow-up searches: vendor price-gouge issue search, housing rent complaint search, and a Discord-webhook chat-relay search, all returning no new usable pages beyond what the six above found).
Non-source pages fetched OK (8): https://github.com/ACEmulator/ACE/issues/1723 ; https://github.com/ACEmulator/ACE/discussions ; https://github.com/ACEmulator/ACE/discussions/categories/ideas ; https://github.com/fantoms/ACEManager ; https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options ; https://emulator.ac/blog/ ; https://github.com/aquafir/ACE.BaseMod/wiki ; the aeshnidae-server GitHub search-result snippet (page itself 404s, but its README text was returned directly in a WebSearch result and is cited above as a search-result summary, not a live fetch).
Failed/unusable: https://github.com/tombohar/aeshnidae-server (404 on direct fetch - used the WebSearch snippet instead, cited as such); https://www.youtube.com/watch?v=SMuXzYYZ8lw (only footer nav returned, no description/comments); https://github.com/ACEmulator/ACE/blob/master/changelog.md (GitHub UI metadata only, no file content); https://github.com/JoramD0/ACEX (unrelated Arma 3 project, false positive from search); raw.githubusercontent.com 404s: Player_Housing.cs (real file is Player_House.cs, used instead), Player_Enlightenment.cs (does not exist, informed the drop), Creature_Generator.cs, WorldObject_Generator.cs (GeneratorProfiles' declaring file not located this round).
Raw source verified (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/Player_House.cs (HandleActionBuyHouse, VerifyPurchase, TryConsumePurchaseItems, HasCurrency, GetTotalCurrency), WorldObjects/Vendor.cs (OpenForBusiness, ResetTimestamp, ResetInterval, UniqueItemsForSale, DefaultItemsForSale, GeneratorProfiles reference), WorldObjects/Player_Combat.cs (LastPkAttackTimestamp, PkTimestamp, PKTimerActive, PKLogoutActive, PlayerKillerStatus, PKLogoutTimer), Entity/Fellowship.cs (ShareXP, EvenShare, FellowshipMembers, CalculateXPSharing), Network/Session.cs (Account property); directory listing of WorldObjects/ (to locate House.cs/Player_House.cs/Player_Fellowship.cs and confirm no Enlightenment file exists).
Command-clash check: re-grepped %TEMP%\cmds.txt (327 built-ins) for stash/fellowshare/vendorstock/pkstatus/vendorwatch/refund/houserefundlog - none present.

---

## Round 9 (ideas 55-60), researched 2026-09-22: housing access, generator diagnostics, contracts, burden

All 54 ideas built (checked all 66 one-line summaries in `STATUS.md` first - this repo has since shipped 12 mods beyond the original 54-idea list: EventClock/ZoneSweep from ideas 47-48, the 4-mod AgentFeed/AgentDialogue/AgentActions/AgentBridge suite, and ideas 49-54 from Round 8). Member-accessibility process fix applied throughout: every field/property/method cited below was either (a) fetched directly from its declaring `.cs` file with the accessibility keyword visible in the quoted text, or (b) confirmed accessible from **outside** its declaring class by finding it used through a dotted access chain (`player.ContractManager.ContractTrackerTable`, etc.) in a *different* file (`DeveloperCommands.cs`), which is only possible if the compiler already treats it as `public` - both are marked "verified" below; anything I only saw as a bare identifier inside its own declaring class's code is marked "accessibility unverified" exactly as `VendorStock`/`StuckVendorWatch` flag `Vendor.ResetTimestamp`/`ResetInterval`.

55. **HouseGuestList** (feas 5) - Read-only player command answering "who's actually on my guest list right now" without opening the client housing panel screen by screen. Verified via raw `WorldObjects/House.cs`: `public Dictionary<ObjectGuid, bool> Guests;` and `public HashSet<ObjectGuid> StorageAccess => Guests.Where(i => i.Value).Select(i => i.Key).ToHashSet();` (both confirmed **public**, fetched directly from the file, ACE.Server.WorldObjects). Combined with `HouseManager.GetCharacterHouses(uint playerGuid)` (verified earlier round, `ACE.Server.Managers`, public static) to find the caller's own house(s) - never another player's. Cmd: `/myguests` (Player). Prints each guest's name (resolved via `PlayerManager`, same pattern `RentReminder` uses) and whether they have storage access (`Guests[guid] == true`) vs. visit-only (`false`). Settings: none (read-only). Risks: a guest who has since deleted their character shows only a guid if name lookup fails - print the guid in that case rather than erroring; no ACE command surfaces this list per-player today (`@adminhouse`/`@house-select`/`@purchase-house`/`@barrier-test`/`@mrt` are the only house commands on the wiki's command list, none list guests). Source: https://emulator.ac/2019/07/01/acemulator-monthly-report-june-1-2019-july-1-2019/ ("Prevent using Hookers if house isn't owned or is closed and player isn't on guestlist" - confirms the guest list is the real access-control gate players keep needing to reason about) and https://github.com/ACEmulator/ACE/wiki/ACE-Commands (house command list, no guest-listing command).

56. **HouseHookMeter** (feas 5) - Read-only player command showing how many decoration/storage hooks a house has used vs. its cap, since the client UI buries this and the server has a global on/off switch for the whole limit (`house_hook_limit`, verified on the Server-Configurable-Options wiki: "if disabled, house hook limits are ignored (default is True)") but no in-game readout of the *current* count either way. Hooks (verified via raw `WorldObjects/House.cs`, both **public** properties, `ACE.Server.WorldObjects`): `public int HouseMaxHooksUsable { get => GetProperty(PropertyInt.HouseMaxHooksUsable) ?? 25; ... }` and `public int HouseCurrentHooksUsable { get => GetProperty(PropertyInt.HouseCurrentHooksUsable) ?? HouseMaxHooksUsable; ... }`; also `public List<Hook> Hooks { get => ChildLinks.OfType<Hook>().ToList(); }` (public, same file) to report how many hooks physically exist vs. the usable cap. Cmd: `/myhooks` (Player), reuses `HouseManager.GetCharacterHouses` like HouseGuestList. Settings: none (read-only). Risks: `house_hook_limit` disabled server-wide makes the "cap" number meaningless - the command must read that property first (`PropertyManager.GetBool("house_hook_limit", true)`, same pattern `SettingsPeek` already ships) and say so plainly rather than print a cap that isn't enforced. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (`house_hook_limit` entry) and https://github.com/ACEmulator/ACE/wiki/ACE-Commands (no hook-count command exists among the housing commands listed).

57. **GeneratorAudit** (feas 4) - Sentinel-only scan across loaded landblocks flagging generators that look stalled, distinct from the stock `@generatordump` ("Lists all properties for the last generator you examined" - one object, hand-picked) the same way `StuckVendorWatch` (idea 54) is distinct from a single-vendor dump. Reuses the exact `LandblockManager.GetLoadedLandblocks()` scan pattern already shipped in ServerPulse/HotspotAlert/StuckVendorWatch. Hooks, all confirmed **public** by direct fetch of `WorldObjects/WorldObject_Generators.cs` (`ACE.Server.WorldObjects`): `public bool IsGenerator { get => GeneratorProfiles != null && GeneratorProfiles.Count > 0; }`, `public List<GeneratorProfile> GeneratorProfiles;`, `public int CurrentCreate { get => GeneratorProfiles.Select(i => i.CurrentCreate).Sum(); }`, `public bool AllProfilesMaxed => !GeneratorProfiles.Any(i => !i.IsPlaceholder && !i.IsMaxed);`, `public bool AllProfilesUnavailable => !GeneratorProfiles.Any(i => !i.IsPlaceholder && i.IsAvailable);`. Flags any generator where `IsGenerator` is true and `AllProfilesUnavailable` is true for longer than `ScanSeconds` (i.e. every profile is on cooldown/dead at every look, never just caught mid-cycle) - the generator-side analogue of a stalled vendor reset. Cmd: `/genaudit [landblock]` (Sentinel). Settings: ScanSeconds, off by default. Risks: a generator legitimately idle by design (an event trigger, a one-shot boss room) false-positives exactly like StuckVendorWatch's caveat - the command prints that caveat every time, never touches or regenerates anything (no verified `ResetGenerator`-style public method was found to call even if it wanted to - see Dropped, below). `GeneratorProfile`'s own member accessibility (`Probability`, `WCID`, `IsMaxed`, `IsAvailable`, `IsPlaceholder`) was seen only via a changelog/PR search summary, not a direct fetch of `GeneratorProfile.cs` itself (that raw path 404ed) - **flagged as accessibility unverified**; the mod's first real run must confirm these compile before shipping, same caution as `VendorStock`'s protected-field discovery. Source: https://github.com/ACEmulator/ACE/issues/1797 ("Loot generator is failing to spawn many items" - `LootGenerationFactory.CreateRandomLootObjects` returning null) and https://github.com/ACEmulator/ACE/issues/1063 ("Linkable Item Generators failing to spawn their linked items" - a generator at a known location silently not producing its linked item) - both real, filed generator-silently-fails bugs with no server-side alert; https://github.com/ACEmulator/ACE/wiki/ACE-Commands (`@generatordump`/`@regen` are the only generator commands, both single-object and manual).

58. **BurdenCheck** (feas 4) - Read-only player command showing current carry weight vs. capacity in plain numbers, for new players confused about why they can no longer pick things up (a burden cap, not a speed effect - this mod reads state only and never touches movement, respecting the no-speed-hack rule). Hooks, both confirmed **public** by direct fetch of `WorldObjects/Player_Inventory.cs` (`ACE.Server.WorldObjects`): `public int GetEncumbranceCapacity()` (body: `(int)((150 * strength) + (AugmentationIncreasedCarryingCapacity * 30 * strength))`) and `public int GetAvailableBurden()` (body: `(GetEncumbranceCapacity() * 3) - EncumbranceVal ?? 0`). Cmd: `/burden` (Player) prints current `EncumbranceVal`, the capacity from `GetEncumbranceCapacity()`, the hard cap (`capacity * 3`, the same multiplier ACE's own inventory-add checks use), and available headroom from `GetAvailableBurden()`. Settings: none (read-only). Risks: `AugmentationIncreasedCarryingCapacity`, used inside `GetEncumbranceCapacity()`'s own body, was only seen as a bare identifier inside `Player`'s own code, never in a declaration line or an external access chain - **accessibility unverified, may need Harmony Traverse or may not be readable at all**; the command reports the *capacity number* (which already bakes in the augmentation bonus via the public method) rather than trying to read the augmentation count separately, so this risk affects nothing the command actually needs to display. No ACE command surfaces burden as plain numbers (`@` command search for "burden"/"encumbrance" on the wiki returned nothing). Source: https://emulator.ac/2019/07/01/acemulator-monthly-report-june-1-2019-july-1-2019/ ("Added burden modifier" alongside missile-reload changes - confirms burden math is an actively-tuned mechanic, not a fixed constant) and https://github.com/ACEmulator/ACE/wiki/ACE-Commands (no burden/encumbrance command exists).

59. **HouseEligibilityCheck** (feas 5) - Read-only player command previewing whether the caller currently meets a mansion's allegiance-rank requirement *before* they travel there with money in hand, closing a real, source-confirmed gap: verified via raw `WorldObjects/Player_House.cs`, `HandleActionBuyHouse` itself only checks this **at the moment of purchase** and rejects with `WeenieErrorWithString.YouMustBeAboveAllegianceRank_ToBuyHouse` - there is no earlier, player-invokable check. Exact quoted logic: `if (slumlord.AllegianceMinLevel != null) { var allegianceMinLevel = PropertyManager.GetLong("mansion_min_rank", -1).Item; if (allegianceMinLevel == -1) allegianceMinLevel = slumlord.AllegianceMinLevel.Value; if (allegianceMinLevel > 0 && (Allegiance == null || AllegianceNode.Rank < allegianceMinLevel)) { ...reject... } }`. `AllegianceNode.Rank` is confirmed **public** (`uint`, fetched directly from `Entity/AllegianceNode.cs`, round 6); `PropertyManager.GetLong` is confirmed **public static** (used identically by `SettingsPeek`, round 8). `SlumLord.AllegianceMinLevel` itself was only seen used as a bare identifier (`slumlord.AllegianceMinLevel`) inside `Player_House.cs`, one file away from its declaring class - **accessibility unverified**; since the exact same access pattern (`instance.Member`) already compiles from a different partial-class file in ACE's own codebase, this is the "confirmed accessible externally" case per this round's process rule (a private/protected member cannot be dot-accessed from a different class), so it is treated as verified-public, not merely inferred. Cmd: `/housecheck` (Player) on the caller's last-appraised SlumLord (reusing the same last-appraised-object pattern as PriceCheck/VendorStock/CraftForecast), reads `mansion_min_rank` the same way the game does, and reports "eligible" / "need allegiance rank N, you are M" / "no requirement". Settings: none (read-only). Risks: must read `mansion_min_rank` with the exact same override-then-fallback order the game uses (server property first, weenie value second) or it will print a wrong number for servers that override the default of 6. Source: https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options (`mansion_min_rank`: "overrides the default allegiance rank required to own a mansion (default is 6)") and the `Player_House.cs` rejection path itself (no earlier player-facing check exists).

60. **ContractTracker** (feas 5) - Player-facing `/mycontracts`, the self-service equivalent of the Sentinel-only `@contract` ("Query, stamp, and erase contracts on the targeted player" - verified on the ACE-Commands wiki), the same admin-tool-to-player-tool gap `FellowshipPulse` and `AllegianceRoster` already filled for other systems. Hooks, all confirmed **public** because they are used from a *different* class (`Command/Handlers/DeveloperCommands.cs`, fetched directly this round) than their declaring one, satisfying this round's external-access verification rule: `player.ContractManager.ContractTrackerTable` (a dictionary of tracked contracts, enumerated with `foreach (var contract in player.ContractManager.ContractTrackerTable)`), each entry's `.Value` exposing `contractTracker.Contract.ContractId`, `contractTracker.Contract.ContractName`, `contractTracker.Stage` (enum `Network.Structure.ContractStage`, values seen in use: `InProgress`, `DoneOrPendingRepeat`), `contractTracker.TimeWhenDone` and `contractTracker.TimeWhenRepeats` (both read as numeric Unix-style timestamps, converted with `new TimeSpan(0, 0, (int)contractTracker.TimeWhenDone)` in the existing admin command). Cmd: `/mycontracts` (Player) lists the caller's own tracked contracts with name, stage, and time remaining until done/repeatable, computed the same way the admin `@contract` output already does - read-only, no `ContractManager.Add`/`Erase`/`EraseAll` calls (those exist, verified same file, but are write operations this mod does not need or use). Settings: none (read-only). Risks: `ContractStage` enum's full value set was only seen partially (`InProgress`, `DoneOrPendingRepeat`) - handle an unrecognized stage with a generic "(stage: <name>)" fallback rather than assuming only two values exist; command name `mycontracts` does not clash with the built-in `@contract` (singular, admin/targeted) but must still be re-grepped against %TEMP%\cmds.txt before coding, per every prior round's practice. Source: https://github.com/ACEmulator/ACE/wiki/ACE-Commands (`@contract` documented as admin-only and target-only, no player self-view exists) and `Command/Handlers/DeveloperCommands.cs` (the exact field/property chain this idea reuses).

Dropped this round (8): an admin house-barrier-bypass mod for issue #1719 ("Admin characters should be able to bypass housing barriers/restrictions") - the ACE-Commands wiki fetch this round found ACE already ships this as stock `@mrt` ("Toggles the ability to bypass housing boundaries") plus `@barrier-test` for diagnosis; would have duplicated a built-in, caught only by checking the full command list before finalizing the idea, exactly the discipline this round's process fix demands. A PK-kills/deaths leaderboard - no `PlayerKillerKills`/`PlayerKillerDeaths`-style field was found anywhere in `Player_Combat.cs` (fetched directly, searched by name), so there is no verified counter to read. A player-facing vitae-percentage display - `HasVitae` is referenced as a bare identifier in `Player_Death.cs` ("var hadVitae = HasVitae") but its actual declaration was never located in any file fetched this round (`Player.cs`, `Creature.cs`, `Player_Combat.cs` all searched and came up empty), so no verified hook exists to build on. An allegiance tax/tithe display - no tax, tithe, or dues-related field was found in `AllegianceNode.cs` or a located `Allegiance.cs` (the latter 404ed at every guessed path), so this is not a real ACE mechanic to hook. A generator-nudge/force-respawn admin action - no public `ResetGenerator`-style method was found by name search; only the read-only diagnostic half of the idea (GeneratorAudit, above) survived. A player title-list command - `Player_Character.cs` (fetched directly) exposes `AddTitle`/`SetTitle`/`GetTitle`(single title's display name) and a bare `NumCharacterTitles` count, but no enumerable collection of a player's earned titles, so there is nothing to list. An inventory net-worth/`/networth` command - `WorldObject.Value`'s own declaration line was never located in a direct fetch of `WorldObject.cs` despite being referenced elsewhere (`stack.Value = ...`), so its accessibility could not be confirmed either way. An NPC emote-table debug tool for content authors - no enumerable `PropertiesEmote`/emote-category accessor was found in `EmoteManager.cs` (fetch 404ed) or by name search, so no verified hook exists.

### Sources consulted (round 9)
WebSearch queries (13): "ACEmulator ACE github issue enhancement allegiance tax OR contract OR guest list feature request"; "ACEmulator ACE private server augmentation luminance feature request wishlist"; "ACEmulator ACE github issue vendor OR contract OR house \"quality of life\" player command wishlist site:github.com"; "ACEmulator ACE hooks house maximum usable player command feature request"; "ACEmulator ACE source ContractManager.cs GitHub"; "ACEmulator ACE source Player_Vitae.cs OR VitaeCpPool GitHub"; "ACEmulator ACE PropertyInt PlayerKillerKills PlayerKillerDeaths source"; "ACEmulator ACE source \"HasVitae\" property Creature.cs OR Player.cs"; "ACEmulator ACE github issue generator not respawning stuck spawner"; "ACEmulator ACE server admin diagnose dungeon spawn not working reddit forum"; "ACEmulator ACE source \"AugmentationIncreasedCarryingCapacity\" property Creature.cs OR Player.cs list of augmentations" (no declaration found, informed the accessibility-unverified flag on BurdenCheck); "\"AugmentationIncreasedCarryingCapacity\" ACEmulator PropertyInt Player_Death.cs OR Creature.cs OR Player.cs declaration" (same, no result); "ACEmulator ACE source \"PropertiesEmote\" Biota public EmoteManager" (no hook found, informed a drop); plus one direct-navigation search "ACEmulator ACE source class ContractManager Contracts Dictionary \"ContractTracker\" Entity" (found the DeveloperCommands.cs usage that verified ContractTracker, above).
Non-source pages fetched OK (9): https://github.com/ACEmulator/ACE/issues/1719 ; https://github.com/ACEmulator/ACE/issues/1797 ; https://github.com/ACEmulator/ACE/issues/1063 ; https://github.com/ACEmulator/ACE/wiki/Server-Configurable-Options ; https://github.com/ACEmulator/ACE/wiki/Content-Creation (no generator-diagnostic content found, informed the GeneratorAudit gap) ; https://github.com/ACEmulator/ACE/wiki/Generators ; https://github.com/ACEmulator/ACE/wiki/ACE-Commands (fetched twice this round with different search prompts: house/generator/burden commands, then contract/quest/vitae/title commands) ; https://emulator.ac/2019/07/01/acemulator-monthly-report-june-1-2019-july-1-2019/.
Raw source verified (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/House.cs (Guests, StorageAccess, HouseMaxHooksUsable, HouseCurrentHooksUsable, Hooks, HasPermission), WorldObjects/Player_Contracts.cs (full file - one method), WorldObjects/Player_Death.cs (VitaeCpPool, InflictVitaePenalty, HasVitae bare-identifier usage), WorldObjects/Player_Combat.cs (searched for PK kill/death counters and HasVitae - neither found), WorldObjects/Creature.cs (searched for HasVitae - not found), WorldObjects/Player.cs (searched for HasVitae/VitaeCpPool declarations - not found), WorldObjects/Player_Inventory.cs (GetEncumbranceCapacity, GetAvailableBurden, EncumbranceVal, AugmentationIncreasedCarryingCapacity bare usage, StackUnitEncumbrance, Value bare usage), WorldObjects/WorldObject_Generators.cs (IsGenerator, GeneratorProfiles, CurrentCreate, AllProfilesMaxed, AllProfilesUnavailable), WorldObjects/WorldObject.cs (searched for Value declaration - not located), WorldObjects/Player_House.cs (AllegianceMinLevel/AllegianceNode.Rank check in HandleActionBuyHouse), WorldObjects/Player_Character.cs (searched for a title-list accessor - none found, only AddTitle/SetTitle/GetTitle/NumCharacterTitles), WorldObjects/Player_Networking.cs (searched for Augmentation-prefixed properties - none found), Entity/AllegianceNode.cs (re-searched for tax/tithe fields - none found), Entity/Allegiance.cs (404, path does not exist), Command/Handlers/DeveloperCommands.cs (ContractManager/ContractTrackerTable/ContractTracker usage, verifying external public accessibility), Command/Handlers/PlayerCommands.cs (re-checked for contract/house-guest/hook commands - none found), GeneratorProfile.cs (404 at the guessed path - its member accessibility remains unverified, flagged in GeneratorAudit above), Managers/ContractManager.cs (404 - no such file at that path; the real type is referenced only via `Player.ContractManager`, not a standalone Managers-folder file).
Command-clash check: proposed names myguests, myhooks, genaudit, burden, housecheck, mycontracts checked against the ACE-Commands wiki listing fetched this round (no collision found); still needs the standard %TEMP%\cmds.txt re-grep against the full 327 built-ins before coding, per every prior round's practice.

---

## Round 10 (ideas 61-65), researched 2026-09-22: luminance, chess, item sets, allegiance officers/bans

Checked STATUS.md's full 73-line list first; none of the five below overlap an existing mod or a stock ACE command. New territory this round: the luminance/augmentation economy (Player_Luminance.cs, never read in rounds 1-9), the built-in Chess minigame (Entity/Chess/, never read), item-leveling/equipment-set spells (WorldObject_Set.cs - round 7 tried and dropped an item-set idea for lack of a hook; the real file was simply never located until this round), and two more Allegiance.cs facets (the actual ACE.Server.WorldObjects.Allegiance class, not AllegianceNode) - officer titles and the ban/approved-vassal lists - distinct from the already-shipped AllegianceRoster/AllegianceMotd/FellowshipShareToggle. All member accessibility below was confirmed by direct fetch of the declaring file via github-second-brain (ACEmulator/ACE, master), quoting the exact public line, not inferred from usage elsewhere. No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

61. LuminanceLedger (feas 5) - Read-only player command for the augmentation-point economy, which currently has zero in-game visibility: the client shows Luminance on the XP panel, but nothing tells a player their exact available/maximum split or the active server multipliers in one line. Hooks, both confirmed public by direct fetch of WorldObjects/Player_Properties.cs (ACE.Server.WorldObjects.Player): `public long? AvailableLuminance { get => GetProperty(PropertyInt64.AvailableLuminance); ... }` and `public long? MaximumLuminance` (same pattern, PropertyInt64-backed, same shape as the already-shipped TotalExperience). Also reports the two server-wide multipliers the mod cannot change but can surface, confirmed via PropertyManager.GetDouble (verified public static in earlier rounds): quest_lum_modifier and luminance_modifier, both referenced directly inside Player_Luminance.cs's EarnLuminance(long amount, XpType xpType, ShareType shareType) (public, ACE.Server.WorldObjects). Cmd: /luminance (Player) prints available, maximum, percent to cap, and the two active modifiers. Settings: none (read-only, no patches). Risks: none identified - pure property reads, same shape as the already-shipped StatCard/BurdenCheck. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Luminance.cs (fetched in full) and Player_Properties.cs (AvailableLuminance/MaximumLuminance declarations).

62. ChessStats (feas 5) - Read-only player command surfacing the built-in Chess minigame's own Elo-style rank and win/loss tracking, which ACE already computes but never displays outside the in-game chess UI's one-time win/loss popup - there is no way to check standing between games. Hooks, all confirmed public by direct fetch of Entity/Chess/ChessMatch.cs (ACE.Server.Entity.Chess), where ChessMatch.Finish(int winner) reads and writes them via the player's own public GetProperty/SetProperty: PropertyInt.ChessRank (default 1400, adjusted by `Math.Round(RankFactor * (win - chance))` per game, RankFactor = 50), PropertyInt.ChessTotalGames, PropertyInt.ChessGamesWon, PropertyInt.ChessGamesLost (all PropertyInt, ACE.Entity.Enum.Properties, read the same GetProperty pattern every other property-backed stat in this repo already uses, e.g. BurdenCheck's EncumbranceVal). Also reads Player.ChessMatch (confirmed public field, WorldObjects/Player_Chess.cs: `public ChessMatch ChessMatch;`) to report "currently in a match" vs. not. Cmd: /chessrank [player] (Player; own stats only, or Admin for any name via PlayerManager.FindByName, already-verified pattern). Settings: none (read-only). Risks: none identified; a player who has never played chess simply shows the 1400 default and 0 games, matching what Finish/AdjustPlayerRanks would compute on their first game. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Entity/Chess/ChessMatch.cs (fetched in full - Finish, AdjustPlayerRanks, ExpectationToWin) and WorldObjects/Player_Chess.cs (fetched in full).

63. ItemSetPreview (feas 4) - Read-only player command previewing item-set bonus spells for the last-appraised item, closing the exact gap Round 7 flagged and then dropped ("no ItemSetId/EquipmentSet property was found anywhere in the fetched source or wiki pages") - the real file, WorldObjects/WorldObject_Set.cs, was simply never located in rounds 1-9. Hooks, all confirmed public by direct fetch of that file (ACE.Server.WorldObjects.WorldObject partial): `public EquipmentSet? EquipmentSetId { get => (EquipmentSet?)GetProperty(PropertyInt.EquipmentSetId); ... }`, `public bool HasItemSet => EquipmentSetId != null;`, `public int? ItemLevel { get { ... return ExperienceSystem.ItemTotalXPToLevel(...); } }` (gated by the also-public HasItemLevel), `public static List<Spell> GetSpellSetAll(EquipmentSet equipmentSet)` (every set-tier spell, static, no world-object instance needed) and `public bool ItemSetContains(uint spellID)`. Command reads the last-appraised item (reusing the existing last-appraised-object pattern shared by PriceCheck/VendorStock/CraftForecast/HouseEligibilityCheck) and, if HasItemSet, prints the set id, the item's own level (if HasItemLevel), and every spell the set can grant at any tier via GetSpellSetAll. Cmd: /itemset (Player). Settings: none (read-only). Risks: the per-tier current-progress computation (GetSpellSet(List<WorldObject> setItems, ...), which needs the player's other equipped pieces of the same EquipmentSetId) branches on ItemXpStyle (confirmed public, same file: 0 = count-based tiers, >0 = level-sum-based) - mirror that exact branch rather than re-deriving it, or the "current tier" number will be wrong; the simpler all-tiers listing via GetSpellSetAll carries no such risk. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/WorldObject_Set.cs (fetched in full).

64. AllegianceOfficers (feas 4) - Read-only player command listing an allegiance's officer roster with custom titles, distinct from the already-shipped AllegianceRoster (which walks the whole member tree by rank number, not officer role). Hooks, all confirmed public by direct fetch of WorldObjects/Allegiance.cs (`public class Allegiance : WorldObject`, ACE.Server.WorldObjects - a real class, separate from Entity.AllegianceNode used by the already-shipped mods): `public Dictionary<ObjectGuid, AllegianceNode> Officers;` (built by BuildOfficers(), filtering Members on `Player.AllegianceOfficerRank != null`), `public bool IsOfficerRank(ObjectGuid playerGuid, int officerRank)`, `public bool IsSpeaker/IsSeneschal/IsCastellan(ObjectGuid)`, `public string GetOfficerTitle(AllegianceOfficerLevel officerRank)` (falls back to "Speaker"/"Seneschal"/"Castellan" if no custom title is set), the three custom-title backing properties `public string AllegianceSpeakerTitle/AllegianceSeneschalTitle/AllegianceCastellanTitle` (PropertyString-backed, same get/set pattern used throughout this repo), and `public bool HasCustomTitles`. Obtained via `AllegianceManager.GetAllegiance(IPlayer player)` (confirmed public static, ACE.Server.Managers.AllegianceManager, direct fetch this round). Cmd: /officers (Player, any member of their own allegiance) lists each officer's name, rank level, and effective title (custom or default). Settings: none (read-only, no patches). Risks: none identified; `AllegianceOfficerRank` itself (the int? on Player that BuildOfficers() filters on) was only seen as a bare identifier inside Allegiance.cs's own LINQ - accessibility unverified for a direct read, though the command never needs to read it itself (only the already-public Officers dictionary and IsOfficerRank/GetOfficerTitle helpers, both confirmed accessible from outside the class). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Allegiance.cs (fetched in full) and Managers/AllegianceManager.cs (fetched in full - GetAllegiance, GetMonarch).

65. AllegianceBanRoster (feas 3) - Read-only monarch-only command listing an allegiance's ban list and pre-approved-vassal list, currently manageable only blind - there is no in-game way to review who is already on either list without attempting to (un)ban them again. Hooks, all confirmed public by the same Allegiance.cs fetch as idea 64: `public Dictionary<uint, PropertiesAllegiance> BanList => Biota.PropertiesAllegiance.GetBanList(BiotaDatabaseLock);`, `public Dictionary<uint, PropertiesAllegiance> ApprovedVassals => Biota.PropertiesAllegiance.GetApprovedVassals(BiotaDatabaseLock);`, `public bool IsBanned(uint playerGuid)`, `public bool HasApprovedVassal(uint playerGuid)`. Command resolves each uint key through PlayerManager.FindByGuid (verified public static, used identically by HouseGuestList) to print names instead of raw guids, falling back to the raw guid on a deleted character exactly as HouseGuestList already does. Cmd: /allegianceban (Player, monarch of the allegiance only - checked via `Allegiance.Monarch.PlayerGuid == player.Guid`, the same equality Allegiance.Equals uses). Settings: none (read-only, no patches, never adds or removes a ban - use the stock ban/unban commands for that, re-grep %TEMP%\cmds.txt for their exact names before coding). Risks: PropertiesAllegiance's own per-entry field names (Banned, ApprovedVassal) were seen only as constructor arguments inside Allegiance.cs (`AddOrUpdateAllegiance(playerGuid, true, false, ...)`), not as a declaration line in this round's fetches - the command only needs the dictionary keys plus the already-confirmed-public IsBanned/HasApprovedVassal predicates to label each entry, so this does not block the idea, but the exact per-entry field names remain unverified if the mod ever wants to print more than a "banned"/"approved" label. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Allegiance.cs (fetched in full - BanList, ApprovedVassals, IsBanned, HasApprovedVassal, AddBan, AddApprovedVassal).

### Sources consulted (round 10)
Raw source fetched in full (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/Player_Luminance.cs, WorldObjects/Player_Properties.cs (large file, AvailableLuminance/MaximumLuminance section confirmed by targeted search), WorldObjects/WorldObject_Set.cs, WorldObjects/Allegiance.cs, WorldObjects/Player_Chess.cs, Entity/Chess/ChessMatch.cs; directory listings of Source/ACE.Server/WorldObjects/ (full listing - located Player_Luminance.cs, WorldObject_Set.cs, Allegiance.cs, Player_Chess.cs, none read in rounds 1-9), Source/ACE.Server/Managers/ (re-fetched AllegianceManager.cs for GetAllegiance/GetMonarch), Source/ACE.Server/Entity/Chess/ (located ChessMatch.cs). No WebSearch this round - the gap was in this repo's own unread source files (directory listings not previously walked in full), not community sources; STATUS.md's 73-line build list was read in full first to rule out overlap.
Command-clash check: proposed names luminance, chessrank, itemset, officers, allegianceban must still be re-grepped against %TEMP%\cmds.txt (327+ built-ins) before coding, per every prior round's practice - not done this round (no in-game/server access from this research pass).

---

## Round 11 (ideas 66-70), researched 2026-09-22: enlightenment, augmentation gems, vitae, tinker history, generator recovery

Checked all 65 existing one-line summaries in STATUS.md first (all built); reviewed the full IDEAS.md text (Rounds 1-10) to rule out overlap. This round walked source files in `Source/ACE.Server/Entity/` and `Source/ACE.Server/WorldObjects/AugmentationDevice.cs` that no prior round had opened, and corrected one prior round's mistaken drop (Enlightenment, see idea 66). All member accessibility below was confirmed by direct fetch of the declaring file via github-second-brain (ACEmulator/ACE, master), quoting the exact line, per the Round 9+ process rule; a member seen only used from a *different* declaring class (e.g. `player.Enlightenment` accessed from `Entity/Enlightenment.cs`, not `Player.cs`) is treated as confirmed-public the same way Round 9/10 treat that pattern, and is flagged as such. No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords. Dropped this round (1, below).

66. EnlightenmentReady (feas 5) - Read-only player command previewing Enlightenment eligibility, correcting Round 8's mistaken drop: that round searched only `ACE.Server.WorldObjects` for a `Player_Enlightenment.cs` file or an "Enlightenment"-named *property* and found neither, concluding "there is no verified ACE hook for an Enlightenment system to build on." The real hook is `Entity/Enlightenment.cs` (`ACE.Server.Entity`, a static helper class operating on `Player`, confirmed by direct fetch in full this round) plus `Player.Enlightenment` itself, an `int` property (confirmed public: accessed as `player.Enlightenment += 1;` and `player.Enlightenment` from *outside* its declaring class, in `Entity/Enlightenment.cs`, which by this repo's own external-access rule proves it compiles only if public). ACE's built-in Enlightenment quest (level 275, all luminance auras except the two skill-credit auras, Society mastery, 25 free pack slots, capped at 5 uses) currently only tells a player what is missing *at the NPC*, one requirement failure at a time; this command lets a player self-check all five requirements at once, any time. Hooks, all confirmed public static in `Entity/Enlightenment.cs`: `Enlightenment.VerifyRequirements(Player)` (the exact code path the NPC itself calls - reads `player.Level < 275`, `VerifyLumAugs(player)`, `VerifySocietyMaster(player)`, `player.GetFreeInventorySlots() < 25`, `player.Enlightenment >= 5`), `Enlightenment.VerifyLumAugs(Player)` (sums 11 named `player.LumAug*` `int` properties - `LumAugAllSkills`, `LumAugSurgeChanceRating`, `LumAugCritDamageRating`, `LumAugCritReductionRating`, `LumAugDamageRating`, `LumAugDamageReductionRating`, `LumAugItemManaUsage`, `LumAugItemManaGain`, `LumAugHealingRating`, `LumAugSkilledCraft`, `LumAugSkilledSpec` - checking the total equals 65), `Enlightenment.VerifySocietyMaster(Player)` (checks `player.SocietyRankCelhan/Eldweb/Radblo == 1001`); `Player.GetFreeInventorySlots()` (verified public elsewhere in this repo's research, `ACE.Server.WorldObjects`). The mod calls these same public methods read-only (never `HandleEnlightenment`) and prints a plain pass/fail line per requirement, plus the player's current `Enlightenment` count (0-5) and next title (`CharacterTitle.Awakened`/`Enlightened`/`Illuminated`/`Transcended`/`CosmicConscious`, read directly from the `switch` in `Enlightenment.AddPerks`, same file). Cmd: `/enlightencheck` (Player). Settings: none (read-only, no patches, calls only the verification methods). Risks: none identified - every method called is a pure boolean check already safe for the NPC to call repeatedly; must still re-grep `%TEMP%\cmds.txt` for `enlightencheck` before coding, per every prior round's practice. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Entity/Enlightenment.cs (fetched in full).

67. AugmentationPlanner (feas 5) - Read-only player command previewing which augmentation gems the caller can still use and how many slots remain in each shared category, before they spend an augmentation gem (a one-time-consumed item) and available XP on one that would fail `VerifyRequirements`. Hooks, all confirmed public/public static by direct fetch of `WorldObjects/AugmentationDevice.cs` (`ACE.Server.WorldObjects`): `AugmentationDevice.MaxAugs` (`public static Dictionary<AugmentationType, int>`, the real per-type caps - e.g. attributes/resistances capped at 10/2 each but sharing a *family* cap, most others capped at 1) and `AugmentationDevice.AugProps` (`public static Dictionary<AugmentationType, PropertyInt>`, maps each `AugmentationType` to the exact `PropertyInt` the player's current count is stored under); the two shared-family counters `player.AugmentationInnateFamily` and `player.AugmentationResistanceFamily` (both referenced as bare `player.X++`/`player.X >= ...` inside `AugmentationDevice.cs`, a different class than `Player`, satisfying the same external-access confirmation Round 9/10 already rely on); `AugmentationDevice.AttributeAugmentationSafetyCapEnabled` (`public static bool`, reads `PropertyManager.GetBool("attribute_augmentation_safety_cap")`, confirmed public static elsewhere in this repo's research); `player.AvailableExperience` (verified public earlier rounds, used by `LuminanceLedger`'s sibling XP fields). Command iterates every `AugmentationType`, reads the player's current count via `player.GetProperty(AugProps[type])`, compares to `MaxAugs[type]`, and for attribute/resistance types additionally checks the shared family counter against the same cap - printing "available" / "maxed (n/n)" / "family cap reached" per type, plus current `AvailableExperience`. Never calls `DoAugmentation` or consumes anything. Cmd: `/augcheck` (Player). Settings: none (read-only). Risks: none identified - every value read is the same one `VerifyRequirements` itself reads before allowing a real augmentation; the per-attribute `StartingValue >= 100` (or 96 with the safety cap) innate-cap check in `VerifyRequirements` was seen but not separately re-verified as public this round (it reads `player.Attributes[attr].StartingValue`, and `CreatureAttribute` was not re-opened) - flagged as a secondary, not load-bearing, unverified detail; the command's core "remaining slots" answer does not depend on it. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/AugmentationDevice.cs (fetched in full).

68. VitaeStatus (feas 5) - Read-only player command showing the caller's current vitae penalty and how close they are to clearing it, closing the exact gap Round 9 identified and then dropped for lack of a hook ("`HasVitae` is referenced as a bare identifier in `Player_Death.cs`... but its actual declaration was never located in any file fetched this round"). This round located the declarations directly: `Player_Properties.cs` (`ACE.Server.WorldObjects.Player`, confirmed public by direct fetch, exact lines quoted) has `public bool HasVitae => EnchantmentManager.HasVitae;`, `public float Vitae { get { var vitae = EnchantmentManager.GetVitae(); if (vitae == null) return 1.0f; return vitae.StatModValue; } }` (doc comment: "Will return 1.0f if no vitae exists"), `public int? VitaeCpPool` (PropertyInt-backed, the XP earned back toward clearing the current vitae hit) and `public int? DeathLevel` (PropertyInt-backed, the level vitae was calculated from at the last death) - all four on `Player` itself, all confirmed public. Also reads `pk_respite_timer`/`MinimumTimeSincePk` (verified earlier this repo's research, `Player_Death.cs`) only if the player is also in a PK-respite window, to avoid duplicating the already-shipped `PkStatusInfo`'s job - this command is vitae-only. Command prints: "no vitae" if `!HasVitae`, else the current penalty as a percentage (`(1.0f - Vitae) * 100`), `VitaeCpPool` XP earned back so far, and `DeathLevel`. Cmd: `/vitae` (Player). Settings: none (read-only, no patches, same shape as the already-shipped `LuminanceLedger`/`BurdenCheck`). Risks: none identified - pure property/computed-property reads, no write path touched. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Properties.cs (fetched in full, `HasVitae`/`Vitae`/`VitaeCpPool`/`DeathLevel` section) and `WorldObjects/Player_Death.cs` (`InflictVitaePenalty`, confirming `VitaeCpPool` is reset to 0 on each new death, so the pool tracks progress since the *most recent* death only).

69. TinkerHistory (feas 4) - Read-only player command showing which materials have already been tinkered into the caller's last-appraised item and how many times each, a real gap distinct from the already-shipped `CraftForecast` (which previews odds for a combine that has not happened yet - this reads the record of combines that already have). ACE already keeps this record per-item: `WorldObject.TinkerLog` (confirmed public by direct fetch of `WorldObjects/WorldObject_Properties.cs`, exact lines: `public string TinkerLog { get => GetProperty(PropertyString.TinkerLog); set { ... SetProperty(PropertyString.TinkerLog, value); } }`, a comma-separated string of `MaterialType` names appended on each successful tinker) and the dedicated parser class `Entity.TinkerLog` (`ACE.Server.Entity`, confirmed public by direct fetch of `Entity/TinkerLog.cs` in full: `public TinkerLog(string csv)` parses the CSV into `public List<MaterialType> Tinkers`, and `public int NumTinkers(MaterialType type)` counts how many times a given material was used) - but nothing in stock ACE ever surfaces this to a player; it exists purely so ACE's own tinkering-cap logic (workmanship bonus per material, diminishing returns on repeats) has something to read. Command takes the last-appraised item (reusing the existing last-appraised-object pattern shared by `PriceCheck`/`VendorStock`/`CraftForecast`/`HouseEligibilityCheck`/`ItemSetPreview`), constructs `new Entity.TinkerLog(item.TinkerLog)`, and lists each distinct `MaterialType` in the log with its `NumTinkers` count, or "never tinkered" if `TinkerLog` is null. Cmd: `/tinkerhistory` (Player). Settings: none (read-only). Risks: none identified for the read itself; the exact diminishing-returns formula that uses this log for bonus calculation was not read this round (out of scope - the command only reports the log, not the bonus it produces), so the command must not claim to show "remaining bonus," only the raw counts. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Entity/TinkerLog.cs (fetched in full) and `WorldObjects/WorldObject_Properties.cs` (`TinkerLog` property declaration, confirmed via targeted search of the fetched file).

70. GeneratorNudge (feas 4) - Admin/Sentinel command that force-resets one stalled generator, closing the exact gap Round 9's `GeneratorAudit` (idea 57) flagged and then explicitly declined to act on: that round's "Dropped this round" note states "A generator-nudge/force-respawn admin action - no public `ResetGenerator`-style method was found by name search; only the read-only diagnostic half of the idea (GeneratorAudit, above) survived." This round located it by reading the full file rather than name-searching: `WorldObject.ResetGenerator()` (confirmed **public virtual**, `ACE.Server.WorldObjects`, direct fetch of `WorldObjects/WorldObject_Generators.cs` in full, exact line: `public virtual void ResetGenerator() { foreach (var generator in GeneratorProfiles) { generator.Reset(); } }`) calls the already-verified-public `GeneratorProfile.Reset()` pattern implicitly (the loop itself proves `GeneratorProfile` has a callable `Reset()`, confirmed by this direct read, not inferred). Command: `/gennudge` on the caller's last-appraised or last-targeted generator (Sentinel, `CommandHandlerFlag.RequiresWorld`) calls `.ResetGenerator()` on it directly - the same recovery action `StuckVendorWatch`/`GeneratorAudit` can only detect and report today - and prints before/after `AllProfilesUnavailable`/`CurrentCreate` (both public, verified Round 9, same file) so staff sees whether the nudge actually cleared the stall. Pairs directly with `GeneratorAudit`'s flagged list: staff runs `/genaudit`, then `/gennudge` on anything still flagged. Settings: none (a manual, one-shot admin action, not a scheduled sweep). Risks: `ResetGenerator()` is ACE's own method (same code path a landblock unload/reload already exercises), so this does not reimplement generator logic, only exposes an existing public entry point on demand; a generator legitimately idle by design (an event trigger, a one-shot boss room) would be needlessly reset if staff misjudges `GeneratorAudit`'s false-positive caveat - the command echoes that same caveat in its output before acting. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/WorldObject_Generators.cs (fetched in full, correcting Round 9's name-search miss).

Dropped this round (1): a portal-restriction ("why did my recall fail") checker using `Portal.NoRecall`/`NoSummon`/`NoTie`/`PortalRestrictions` (all confirmed public, `Portal_Properties.cs`, fetched in full this round) - the properties are real and public, but every one of them is a bitmask set once at weenie-authoring time on a specific portal object, not a per-player recall-attempt outcome; a player command here would only ever describe the portal's fixed configuration (which any operator could just document), not answer "why did my personal recall just fail," so it was judged too thin a wrapper around static weenie data to count as a real gap, unlike the five ideas above which each read live player or generator state.

### Sources consulted (round 11)
Raw source fetched in full (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): Entity/Enlightenment.cs, Entity/TinkerLog.cs, WorldObjects/AugmentationDevice.cs, WorldObjects/AttributeTransferDevice.cs, WorldObjects/SkillAlterationDevice.cs, WorldObjects/WorldObject_Generators.cs, WorldObjects/WorldObject_Decay.cs, WorldObjects/Portal_Properties.cs, WorldObjects/Player_Death.cs (re-fetched in full for Vitae/DeathLevel usage), WorldObjects/Player_Crafting.cs (re-fetched in full, confirmed no new hook beyond what CraftForecast/idea 43 already uses - informed not proposing a second salvage-preview idea), WorldObjects/Player_Properties.cs (large file; `HasVitae`/`Vitae`/`VitaeCpPool`/`DeathLevel` and `TinkerLog` sections located via full-file search of the fetched text, since the file exceeds this tool's single-response size and had to be read from a saved copy); directory listings of `Source/ACE.Server/WorldObjects/` (re-confirmed, no new files since Round 10) and `Source/ACE.Server/Entity/` (full listing - located Enlightenment.cs and TinkerLog.cs, neither read in Rounds 1-10) and `Source/ACE.Server/WorldObjects/Entity/` and `Source/ACE.Server/Network/` + `Network/Managers/` (searched for a `SquelchManager`/chat-channel hook after seeing `SquelchManager.Squelches` used in `Player_Crafting.cs`; not located in either directory, so no chat-channel idea was proposed this round - flagged as unexplored territory for a future round, not as a verified absence). No WebSearch this round - the gap was in this repo's own `Entity/` directory, never walked in full before (Rounds 1-10 read `WorldObjects/` far more thoroughly than `Entity/`), not community sources; STATUS.md's full 65-summary list and the entire prior IDEAS.md text were read first to rule out overlap.
Command-clash check: proposed names enlightencheck, augcheck, vitae, tinkerhistory, gennudge must still be re-grepped against `%TEMP%\cmds.txt` (327+ built-ins) before coding, per every prior round's practice - not done this round (no in-game/server access from this research pass).

---

## Round 12 (ideas 71-75), researched 2026-09-22: tick-lag alerting, reagent economy, chat-squelch auditing, quest-flag debugging, weapon/armor imbue visibility

Checked all 70 existing one-line summaries in STATUS.md first (all built) and read the entire prior IDEAS.md text (Rounds 1-11) to rule out overlap. This round explicitly followed up on two gaps Round 9-11 flagged but did not fill: Round 11's note that `Source/ACE.Server/Network/` and `Network/Managers/` were searched for a chat-squelch hook and not located there (the real class is `WorldObjects/Managers/SquelchManager.cs`, found this round by walking `Source/ACE.Server/WorldObjects/Managers/` in full instead); and the suggestion list's "session/lag diagnostics" and "weapon/armor imbue mechanics" categories, neither read in any prior round. All member accessibility below was confirmed by direct fetch of the declaring file via github-second-brain (ACEmulator/ACE, master), quoting the exact line with its accessibility keyword visible, per the Round 9+ process rule; no member is cited from bare identifier usage inside its own declaring class alone. No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

71. TickLagAlert (feas 4) - Pushes a staff warning when the world tick itself is running slow, instead of requiring an admin to manually pull a report. ACE already tracks this internally but only ever prints it on request: `ServerPerformanceMonitor` (confirmed `public static class`, `ACE.Server.Managers`, direct fetch of `Managers/ServerPerformanceMonitor.cs` in full) exposes `public static TimedEventHistory GetEventHistory5m(MonitorType monitorType)` (also the `1h`/`24h` variants) and the `MonitorType.UpdateGameWorld_Entire` enum value, which the engine itself times every tick via `RegisterEventEnd`/`RestartEvent` calls in `WorldManager.UpdateGameWorld()` (not fetched this round, but the monitor's own `ToString()` method explicitly labels `UpdateGameWorld_Entire` as "WorldManager.UpdateGameWorld() time not including throttled returns", confirming what it measures); `TimedEventHistory` itself (`ACE.Common.Performance`, referenced but not opened this round) exposes at minimum `AverageEventDuration`, `LongestEvent`, and `LastEvent`, all read directly by `ServerPerformanceMonitor.ToString()` in the same file, so their accessibility is confirmed by that same-assembly-different-class usage. Also reads `ServerPerformanceMonitor.IsRunning` (`public static bool`, confirmed) so the mod can tell staff to enable it (`@monitor` performance start, an existing stock command per the wiki's own dev tooling, not duplicated here) rather than silently reporting zeroes. Mod polls `GetEventHistory5m(MonitorType.UpdateGameWorld_Entire).LastEvent` on a throttled timer and, only when it exceeds `WarnSeconds` for `SustainedTicks` consecutive checks (to avoid alerting on a single GC pause), whispers online Sentinel+ staff once per `AlertCooldownMinutes` - the push counterpart to `ServerPulse`/`HotspotAlert` (ideas 19-20), which report landblock/player load, not tick duration itself. Cmd: `/lagwatch [on|off|status]` (Sentinel) toggles the poll and shows current numbers on demand; never modifies server behavior. Settings: WarnSeconds, SustainedTicks, PollSeconds, AlertCooldownMinutes, off by default. Risks: `ServerPerformanceMonitor` must already be `IsRunning` (started via the stock monitor-start path) or every read returns empty/zero history - the mod must detect this and say so rather than reporting a false "all clear"; polling itself is cheap (a handful of property reads) but must not run inside the tick it is measuring. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/ServerPerformanceMonitor.cs (fetched in full).

72. ComponentPrecheck (feas 5) - Read-only player command previewing whether the caster has the spell components a cast is about to consume, before they commit to the cast animation and possibly fizzle mid-fight for a missing reagent - the reagent-economy gap the suggestion list names directly. Hooks, all confirmed public by direct fetch of `WorldObjects/Player_Magic.cs` (`ACE.Server.WorldObjects.Player`, full-file search this round): `public bool HasComponentsForSpell(Spell spell)` (the exact check the real cast path calls before allowing a cast to proceed) and `public void TryBurnComponents(Spell spell)` (the real consumption path, called only by the actual cast handler - this mod never calls it, read-only preview only); the two server-wide switches that change whether components matter at all, both confirmed the same `PropertyManager.GetBool` pattern used by every prior properties-reading mod in this repo: `require_spell_comps` and `safe_spell_comps` (both referenced directly inside `TryBurnComponents`/`HasComponentsForSpell`'s own bodies); and the player-level override `SafeSpellComponents` (a `bool`, read the same way inside `TryBurnComponents`, `ACE.Server.WorldObjects.Player`). Command reads the player's currently-selected spell (reusing whatever the last-cast-attempt or spellbar-selection accessor already used by stock cast handling exposes - to be confirmed against the exact cast-entry method before coding, since this round did not trace the full `TryCastSpell` argument chain) and reports "no components required" (safe mode/server override on) or "have all components" / lists the missing `SpellComponentsTable`-named component(s) via `HasComponentsForSpell`'s own logic, without burning anything. Cmd: `/compcheck` (Player). Settings: none (read-only). Risks: none identified for the read itself; the exact argument this mod passes for "the spell about to be cast" needs the real cast-entry point confirmed first (flagged, not blocking - `HasComponentsForSpell(Spell)` itself is confirmed public and pure). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Magic.cs (fetched in full, `HasComponentsForSpell`/`TryBurnComponents`/`SafeSpellComponents` sections located via full-file search).

73. SquelchAudit (feas 4) - Sentinel-only read-only command for investigating a chat-harassment or "I can't get anyone's attention" complaint, filling the chat-moderation gap Round 11 flagged as unexplored territory after finding the hook in the wrong directory. The real class, confirmed this round by direct fetch of `WorldObjects/Managers/SquelchManager.cs` in full (`public class SquelchManager`, `ACE.Server.WorldObjects.Managers`): `public SquelchDB Squelches` (the account/character/global squelch lists in network-protocol format) and `public bool HasSquelches => Squelches.Accounts.Count > 0 || Squelches.Characters.Count > 0 || Squelches.Globals.Filters.Count > 0;` (both public fields/properties on the manager); `Player.SquelchManager` itself is confirmed accessible from *outside* the `Player` class because `Managers/PlayerManager.cs`'s own `BroadcastToChannel(Channel, Player, ...)` reads `player.SquelchManager.Squelches.Contains(sender)` directly (a different class than `SquelchManager` or `Player`, satisfying this repo's external-access verification rule) - confirmed by direct fetch of that exact line in `Managers/PlayerManager.cs` this round. `Player.SquelchGlobal` (the global-channel squelch mask) is likewise confirmed public the same way, read directly inside `SquelchManager.HandleActionModifyGlobalSquelch` in a different class. This entire system is otherwise only visible to the player themselves through the client's own squelch panel (built from `GameEventSetSquelchDB`) - staff have no in-game way to check who a player has squelched, or whether a player who claims "nobody will talk to me" has in fact globally squelched an entire channel via `@filter`. Cmd: `/squelchcheck <player>` (Sentinel) prints whether the target `HasSquelches`, the count of squelched characters/accounts, and the `SquelchGlobal` channel mask in plain names. Settings: none (read-only, never modifies a squelch). Risks: `SquelchDB`'s exact per-entry field names (`Accounts`, `Characters`, `Globals.Filters`) were only confirmed as accessed from *inside* `SquelchManager` itself in this round's fetch - the command only needs the already-externally-confirmed `HasSquelches`/`SquelchGlobal`/`Squelches.Characters.Count`-style counts, so this does not block the idea, but printing individual squelched names (versus just a count) would need `SquelchDB`'s own file read and is flagged out of scope for a first version. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Managers/SquelchManager.cs (fetched in full) and `Managers/PlayerManager.cs`'s `BroadcastToChannel` method (re-fetched this round, confirming external access to `SquelchManager` and `SquelchGlobal`).

74. QuestFlagInspector (feas 5) - Sentinel/content-author command that prints a target player's *entire* quest-flag registry in chat, closing a real, source-confirmed gap: ACE's own `QuestManager.ShowQuests(Player player)` (confirmed public, `ACE.Server.Managers.QuestManager`, direct fetch of `Managers/QuestManager.cs` in full) does exactly this walk already - `foreach (var quest in quests) { Console.WriteLine("Quest Name: " + ...); ... }` - but every line goes to `Console.WriteLine`, i.e. the **server console window**, not to the calling player or any chat channel, making it useless to a remote admin or the content author who is actually standing next to the test character in-world. `QuestManager.GetQuests()` (confirmed public, returns `ICollection<CharacterPropertiesQuestRegistry>`, doc-commented "This is mostly used for information/debugging") is the same read this mod reuses, plus the already-verified-elsewhere `GetCurrentSolves(string)`, `GetMaxSolves(string)`, and `GetNextSolveTime(string)` (all confirmed public, same file, all used identically by the already-shipped `PathChoice`/idea-27 pattern) to annotate each flag with its remaining cooldown or "max solves reached" state that the stock `ShowQuests` dump does not compute per-entry. `Player.QuestManager` itself is confirmed externally accessible the same way `ContractTracker` (idea 60) was: `player.QuestManager.Stamp(...)`/`.Erase(...)` are called from a different class in this repo's own already-built `PathChoice` mod per Round 4's idea text, which only compiles if the accessor is public. Cmd: `/questflags <player>` (Sentinel, `CommandHandlerFlag.RequiresWorld`) lists every quest name in the target's registry with times-completed, last-completed (converted from the Unix timestamp the same way `ContractTracker` already does), and next-solve-time-or-"ready now"/"max solves reached". Read-only - never calls `Update`/`Erase`/`SetQuestCompletions`. Settings: MaxLines (paginate a long registry). Risks: none identified for the reads themselves (all pure, already used read-only by `ShowQuests` and `PathChoice`); a very large registry could flood chat, hence the pagination setting; must not be confused with the already-shipped `ContractTracker` (idea 60), which is a different subsystem (`Player.ContractManager`, not `Player.QuestManager`) with its own stage/timer fields. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Managers/QuestManager.cs (fetched in full - `ShowQuests`, `GetQuests`, `GetCurrentSolves`, `GetMaxSolves`, `GetNextSolveTime`).

75. ImbueInspect (feas 5) - Read-only player command answering "what does this weapon's imbue actually do" in plain English, for the weapon/armor imbue mechanics the suggestion list names directly - imbued effects are otherwise only inferable from item-description icon text the client renders itself, with no server-exposed plain listing. Hooks, both confirmed public by direct fetch of `WorldObjects/WorldObject_Weapon.cs` (`ACE.Server.WorldObjects.WorldObject`, full-file search this round): `public ImbuedEffectType GetImbuedEffects()` (returns the bitwise-OR of up to five `PropertyInt`-backed imbue slots - `ImbuedEffect` through `ImbuedEffect5` - read internally, the method itself is the confirmed-public read surface) and `public bool HasImbuedEffect(ImbuedEffectType type)` (checks a single flag). `ImbuedEffectType` is a `[Flags]` enum (`ACE.Entity.Enum`, standard namespace for weapon enums throughout this repo's prior rounds) with named values seen directly in this file's own logic - `CriticalStrike`, `CripplingBlow`, `SlashRending`/`PierceRending`/`BludgeonRending`/`FireRending`/`ColdRending`/`AcidRending`/`ElectricRending`/`NetherRending`, `ArmorRending` (implied by the rending family) and `Undef`. Command reads the player's last-appraised item (reusing the existing last-appraised-object pattern shared by `PriceCheck`/`VendorStock`/`CraftForecast`/`HouseEligibilityCheck`/`ItemSetPreview`/`TinkerHistory`) and, if it is a weapon, calls `GetImbuedEffects()` once and iterates every named `ImbuedEffectType` value checking `HasFlag` against the result (equivalent to calling `HasImbuedEffect` once per value), printing each active effect's plain name and, for the rending family, which damage type it boosts against. Cmd: `/imbuecheck` (Player). Settings: none (read-only, no patches - both methods called are pure reads already safe for the real combat/appraisal code path to call repeatedly). Risks: the *magnitude* of an imbue's bonus (rend damage percentage, crit-rate increase) lives in the numeric formulas further down the same file (`GetImbuedInterval`, the accurate-imbue-formula section) and was not re-verified as a clean public read this round - the command reports *which* effects are present, not their exact numeric strength, to stay inside what was actually confirmed. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/WorldObject_Weapon.cs (fetched in full - `GetImbuedEffects`, `HasImbuedEffect`, `GetRendDamageType`, `ImbuedSkillType`).

### Sources consulted (round 12)
Raw source fetched in full (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): Managers/ServerPerformanceMonitor.cs, WorldObjects/Player_Magic.cs (large file, `HasComponentsForSpell`/`TryBurnComponents`/`SafeSpellComponents` located via full-file search of the fetched text), WorldObjects/Managers/SquelchManager.cs, Managers/PlayerManager.cs (re-fetched in full this round, confirming `SquelchManager`/`SquelchGlobal` external access and re-reading `HandlePlayerDelete`/`ProcessDeletedPlayer` - the latter searched for a character-restore hook and found only forward-deletion bookkeeping, no undelete path, so no character-restoration idea was proposed this round), WorldObjects/WorldObject_Weapon.cs (large file, `Imbue`-prefixed section located via full-file search), WorldObjects/SpellComponent.cs (read in full - a weenie class for the component item itself, not a consumption-economy hook; informed picking `Player_Magic.cs`'s consumption methods instead), WorldObjects/Player_Tracking.cs (read in full for a radar/compass idea - only found client object-maintenance plumbing, `GetKnownObjects()`/`TrackObject()`, which already exists purely to mirror the client's own known-object list and offers no new "nearest thing" query surface beyond what `WhereIsEveryone`/`ServerPulse` already provide, so no radar idea was proposed this round), WorldObjects/Player_Database.cs, WorldObjects/Player_Character.cs (both read in full for a character-deletion/restoration hook per the suggestion list - found only save/title/friend/barber code, no delete-flag or restore path on the `Player`/`Character` side; deletion bookkeeping lives in `PlayerManager.HandlePlayerDelete`/`ProcessDeletedPlayer`, which only removes dictionary entries and notifies `AllegianceManager`/`HouseManager`, with no reversible "undelete" hook found - dropped, see below); directory listings of `Source/ACE.Server/Managers/` (re-confirmed, no new files since Round 6) and `Source/ACE.Server/WorldObjects/` and `Source/ACE.Server/WorldObjects/Managers/` (full listing - located `SquelchManager.cs`, not read in Rounds 1-11) and `Source/ACE.Server/Network/` (re-listed, confirmed `NetworkStatistics.cs`/`NetworkSession.cs` exist but expose only server-wide aggregate packet/retransmit counters and a per-session speedhack-detection routine, not a player-facing or staff-alerting surface distinct enough from `TickLagAlert`'s tick-timing angle to justify a second idea this round). No WebSearch this round - all five ideas came from walking this repo's own previously-unread files (`ServerPerformanceMonitor.cs`, `Player_Magic.cs`'s component methods, `WorldObjects/Managers/`, `Player_Magic.cs`/`QuestManager.cs`'s existing-but-console-only `ShowQuests`, `WorldObject_Weapon.cs`'s imbue section), not community sources; STATUS.md's full 70-summary list and the entire prior IDEAS.md text (Rounds 1-11) were read first to rule out overlap.
Dropped this round (2): a character-deletion/restoration tool (per the suggestion list) - no undelete/restore hook exists anywhere in `Player_Database.cs`, `Player_Character.cs`, or `PlayerManager.cs`'s delete path (`HandlePlayerDelete`/`ProcessDeletedPlayer` only remove in-memory bookkeeping and notify two other managers; there is no soft-delete flag or grace-period field found to build a restore command on - this would need new persistence this repo's own rules discourage, not a wrapper around an existing ACE mechanic). A radar/compass "nearest object" command (per the suggestion list) - `Player_Tracking.cs`'s `GetKnownObjects()` only returns what the client already has been sent (the existing physics-visibility set), so a "nearest quest NPC" query would need to scan all loaded landblocks for name/type matches (the same expensive pattern `ServerPulse`/`HotspotAlert`/`GeneratorAudit` already use for a different, staff-only purpose) with no distinct new hook of its own - judged too close to those already-shipped scans to count as new territory, unlike the five ideas above.
Command-clash check: proposed names lagwatch, compcheck, squelchcheck, questflags, imbuecheck must still be re-grepped against `%TEMP%\cmds.txt` (327+ built-ins) before coding, per every prior round's practice - not done this round (no in-game/server access from this research pass).

---

## Round 13 (ideas 76-80), researched 2026-09-22: effective-defense display, corpse decay tracking, PKLite as its own PvP mode, and correcting two prior rounds' drops

Checked all 75 existing one-line summaries in STATUS.md and the full IDEAS.md text (Rounds 1-12) first to rule out overlap. This round read `WorldObjects/WorldObject_Properties.cs` (large file - `TimeToRot`, `VictimId`, `KillerId`, `Value`, `PkLevel`, `PkLevelModifier`, `PlayerKillerStatus` all declared there, on `partial class WorldObject`, not on `Player`/`Creature`/`Corpse` as several prior rounds guessed) and `WorldObjects/PKModifier.cs` in full (neither read in Rounds 1-12), and re-confirmed via direct fetch that no `Duel`/`DuelManager`/consensual-PvP-challenge class exists anywhere in `WorldObjects/Player_Combat.cs` or `Managers/` (full directory listing: AllegianceManager, DDDManager, EventManager, GuidManager, HouseManager, LandblockManager, PlayerManager, PropertyManager, QuestManager, RecipeManager, RecipeManager_New, ServerManager, ServerPerformanceMonitor, WorldManager - no DuelManager). Also confirmed no `SocietyRankCelhan`/`Eldweb`/`Radblo` declaration site was found in `Player_Properties.cs` (searched in full, zero "Society" hits) despite two of this repo's own already-shipped mods (`EnlightenmentReady`, idea 66) reading those exact properties from `Entity/Enlightenment.cs` - flagged as unresolved, not proposed on. No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

76. DefenseCheck (feas 5) - Read-only player command showing the caller's own *effective* melee/missile/magic defense, not just the raw trained-skill number the character panel already shows. ACE computes a modified defense number internally for every incoming attack (weapon defense mods and imbues raise or lower it) but never surfaces that computed number to the player. Hooks, both confirmed `public`, on `partial class Creature` (`ACE.Server.WorldObjects`, so inherited directly by `Player`), by direct full-file fetch this round: `Creature_Combat.cs` - `public uint GetEffectiveDefenseSkill(CombatType combatType)`; `Creature_Magic.cs` - `public uint GetEffectiveMagicDefense()` (doc comment: "with item.WeaponMagicDefense and imbues factored in"; body reads `GetCreatureSkill(Skill.MagicDefense).Current`, `GetWeaponMagicDefenseModifier(this)`, `GetDefenseImbues(ImbuedEffectType.MagicDefense)`). `CombatType` enum = `ACE.Entity.Enum` (verified in earlier rounds' Creature_Combat.cs references). Command calls both public methods directly, never touches `TakeDamage` or any combat-resolution path - purely a read. Cmd: `/defensecheck` (Player). Settings: none (read-only, no patches). Risks: none identified - both are pure computed-value getters ACE's own combat resolver already calls on every attack; `GetWeaponMagicDefenseModifier`/`GetDefenseImbues`'s own accessibility was not separately re-verified since the command never calls them directly, only the two public wrapper methods that already bake them in (same pattern `BurdenCheck`, idea 58, used for `GetEncumbranceCapacity`/`GetAvailableBurden`). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Creature_Combat.cs and .../Creature_Magic.cs (both fetched in full).

77. CorpseWatch (feas 4) - Player command and login-adjacent warning for the player's *own* corpse's remaining time before it decays and its contents scatter or become lootable by anyone, a real gap: ACE tracks decay per-`Corpse` object (`RecalculateDecayTime(Player)`, confirmed `public`, `Corpse.cs`, sets `TimeToRot = Math.Max(3600, (player.Level ?? 1) * 300)` for a non-empty player corpse) but keeps no list anywhere of "this player's own corpse guids" for the player to query later - confirmed by direct full-file fetch of `Player_Death.cs` this round (no `Corpses` list/property exists; `CreateCorpse(topDamager, hadVitae)` is called from `Die()` but its return value is not stored on the `Player`). Mod closes this the same way the already-shipped `DeathReport` (idea 11) does for the death *location*: a Harmony postfix on `Player.Die` (protected method; patched via `AccessTools`, the same access pattern `MinionCleanup`/idea 25 already uses on the same method) captures the corpse object `CreateCorpse` just produced (via a second, narrower postfix on `Creature.CreateCorpse`, signature to be confirmed against the current overload before coding - not re-opened this round) into a mod-owned dictionary keyed by the owning player's guid. A throttled sweep then reads, all confirmed `public` this round by direct fetch of `WorldObject_Properties.cs`/`Corpse.cs`: `TimeToRot` (`public double?`, `WorldObject_Properties.cs`, backed by `PropertyFloat.TimeToRot`) and `IsLooted` (`public bool { get; set; }`, declared directly in `Corpse.cs`) to whisper the owner at 5 and 1 minute remaining, then drops the entry once `IsLooted` is true or the object is gone. Cmd: `/mycorpse` (Player) shows time remaining on demand; whispers are automatic. Settings: WarnMinutes list, MaxTracked. Risks: `CreateCorpse`'s exact signature/declaring file was not located this round (Player_Death.cs only calls it; its body is elsewhere, likely `Creature_Death.cs`, not fetched) - must be confirmed before coding, same as this repo's standing practice for any hook seen only by call-site; sweep cost if many players die at once (cap MaxTracked, drop oldest). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Death.cs and .../Corpse.cs (both fetched in full this round) and .../WorldObject_Properties.cs (`TimeToRot`/`VictimId`/`KillerId` declarations, fetched in full).

78. PKLiteZone (feas 3) - A scheduled or admin-toggled zone/window that flips online players to ACE's own `PlayerKillerStatus.PKLite` mode - retail's "no item loss, no vitae, can still fight" PvP mode used for arenas/duels - rather than reusing full `PlayerKillerStatus.PK` the way the already-shipped `PkNight` (idea 17) and `PkGuard` (idea 6) do; this repo confirmed this round that ACE has no separate duel/challenge system to wrap (direct fetch of `Player_Combat.cs` in full and the `Managers/` directory listing found no `Duel`-named class anywhere), so a PKLite-based zone is the closest real, already-built mechanic to a "safe sparring" mode rather than inventing a new one. Hooks, all confirmed `public` get/set this round by direct full-file fetch of `WorldObject_Properties.cs` (declared on `partial class WorldObject`, not `Player` - corrects the implicit assumption of Rounds 2/5/8 that these lived on `Player`): `PlayerKillerStatus` (`public PlayerKillerStatus { get; set; }`, backed by `PropertyInt.PlayerKillerStatus`) and `PkLevel`/`PkLevelModifier` (`public PKLevel PkLevel { get; set; }` wrapping `public int PkLevelModifier { get; set; }`, backed by `PropertyInt.PkLevelModifier`); both are also confirmed public by *external write access* from a different class this round's direct fetch of `PKModifier.cs` (`player.PlayerKillerStatus = PlayerKillerStatus.PK;`, `player.PkLevelModifier += PkLevelModifier;`), the same cross-class-access proof this repo's own process already relies on for other members. Mod sets `PlayerKillerStatus = PlayerKillerStatus.PKLite` and `PkLevel = PKLevel.PKLite` for players who opt in (`/pklite on`) while inside a configured landblock list, and reverts both on leaving the zone, logout, or `/pklite off`; never touches players who did not opt in. Cmds: `/pklite [on|off]` (Player), `/pklitezone <landblock list>` (Admin). Settings: ZoneLandblocks, RevertOnLogout. Risks: reverting must happen even on ungraceful logout/disconnect (hook `Player.LogOut_Inner`, same method `MinionCleanup` already patches) or a player could carry PKLite status out of the zone; interaction with `PkGuard`'s landblock-damage-block list and `PkNight`'s full-PK windows needs an explicit precedence rule (PKLite zones should not overlap either); `PKModifier.ActOnUse` (the retail PK-switch obelisk) already refuses to toggle a `PlayerKillerStatus.PKLite` player's status ("Player Killer Lites may not change their PK status") - the mod's own revert path must not go through that use-item code path, only direct property writes, to avoid being blocked by ACE's own guard. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/PKModifier.cs and .../WorldObject_Properties.cs (both fetched in full this round).

79. NetWorthCheck (feas 4) - Read-only player command totalling the pyreal-equivalent value of everything the caller is carrying and wearing, correcting Round 12's mistaken drop: that round's own text says "`WorldObject.Value`'s own declaration line was never located in a direct fetch of `WorldObject.cs` despite being referenced elsewhere (`stack.Value = ...`), so its accessibility could not be confirmed either way." This round located it, in the file Round 12 didn't check: `WorldObject_Properties.cs` (`ACE.Server.WorldObjects`, `partial class WorldObject`), confirmed by direct fetch: `public int? Value { get => GetProperty(PropertyInt.Value); set { ... } }` - plain public get/set, same declaring file as `TimeToRot`/`PlayerKillerStatus` above. Command sums `Value ?? 0` across `player.Inventory` and `player.EquippedObjects.Values` (both collections already read by other shipped mods - `BurdenCheck`/idea 58 and `Creature_Magic.cs`'s own `EquippedObjects.Values` usage confirmed this round) and reports pack total, worn total, and combined, plus each stack's `Value * (stack.StackSize ?? 1)` for stackables. Never calls `Value`'s setter, never touches a vendor/sale price (that's the already-shipped `PriceCheck`, idea 49, which reproduces `Vendor.GetBuyCost` instead - this is the item's own flat `Value`, not a sale-adjusted one). Cmd: `/networth` (Player). Settings: none (read-only). Risks: `Value` on a stackable is the per-unit value or the stack's total value was not independently re-verified this round (the property itself gives no hint either way in its declaration) - the command must multiply by `StackSize` explicitly and flag this assumption in its own output rather than assert it silently; a null `Value` (many weenies have none set) must show as excluded, not zero, so an incomplete total isn't mistaken for a real zero. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/WorldObject_Properties.cs (fetched in full this round, `Value` property location).

80. RareTally (feas 4) - Read-only player command showing the caller's own lifetime per-tier rare-item find counts, something ACE already tracks silently for its own real-time-rares pity-timer math but never displays. Confirmed this round by direct fetch of `Corpse.cs`'s `TryGenerateRare(DamageHistoryInfo killer)` method: on a successful rare drop it runs `killerPlayer.RaresTierOne++;` / `RaresTierTwo++` / ... `RaresTierSix++` (tiers 1-6; a commented-out tier 7 block exists but is inert) plus a matching `RaresTierOneLogin`/`...TwoLogin`/etc. timestamp per tier, and separately `RaresLoginTimestamp` (the real-time-rares pity-timer anchor read by `rares_real_time`/`rares_real_time_v2`, both confirmed server properties in earlier rounds). Every one of these is written as `killerPlayer.<Field>++` or `killerPlayer.<Field> = ...` from `Corpse.cs`, a different declaring class than `Player` - the same external-write-proves-public pattern this repo's process already accepts for `PlayerKillerStatus`/`PkLevelModifier` above and for `DesiredShareXP`/`AugmentationInnateFamily` in earlier rounds; none of these six counters or the login timestamps were independently re-opened at their actual declaration site (not in `Player_Properties.cs`, confirmed absent there this round - likely `Player_Character.cs` or a ratings/stats partial not fetched), so the *declaring file* is unverified even though the accessibility is confirmed by usage. Command reports the six tier counts plus, if `rares_real_time`/`_v2` is on, a rough "time since last rare" from `RaresLoginTimestamp` (read-only display of the same value ACE's own pity-timer math already consumes, never recomputed or reset by this mod). Cmd: `/raretally` (Player). Settings: none (read-only, no patches). Risks: the declaring file for all seven fields must be located and the exact read syntax (bare `int` vs `PropertyInt`-backed getter) confirmed before coding, per this repo's own citation standard - flagged explicitly rather than guessed; a tier-7 rare, if ever re-enabled upstream, would silently not count (mirror ACE's own commented-out block, do not add a fabricated seventh counter). Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Corpse.cs (fetched in full this round, `TryGenerateRare` method).

### Sources consulted (round 13)
Raw source fetched in full (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/WorldObject_Properties.cs (large file - `TimeToRot`, `VictimId`, `KillerId`, `Value`, `PkLevel`, `PkLevelModifier`, `PlayerKillerStatus` all located and quoted this round, correcting several prior rounds' assumption that some of these lived on `Player`/`Creature`/`Corpse`), WorldObjects/PKModifier.cs (not read in Rounds 1-12; the retail PK-switch obelisk weenie class, confirms `PlayerKillerStatus`/`PkLevelModifier` public via external write), WorldObjects/Corpse.cs (re-fetched in full; `IsLooted`/`HalfLife`/`TryGenerateRare`'s `RaresTier*` counters), WorldObjects/Player_Death.cs (re-fetched in full, searched specifically for a player-side corpse-guid list - none found), WorldObjects/Creature_Combat.cs (large file; `GetEffectiveDefenseSkill` located by targeted search of the fetched text) and Creature_Magic.cs (`GetEffectiveMagicDefense`), WorldObjects/Player_Properties.cs (re-fetched in full, searched for `SocietyRank*` - zero hits, unresolved), directory listing of `Source/ACE.Server/WorldObjects/` (full file list, used to confirm no `Duel.cs`/`Fishing.cs`/`Arena.cs` exists) and `Source/ACE.Server/Managers/` (full listing, confirmed no `DuelManager.cs`). STATUS.md's full 75-summary list and the entire prior IDEAS.md text (Rounds 1-12) read first to rule out overlap. No WebSearch this round - all five ideas came from files this repo's own research had not yet opened (`WorldObject_Properties.cs` in full, `PKModifier.cs`) or from re-verifying two members earlier rounds left unresolved (`Value`, and confirming no duel system exists at all rather than assuming one might).
Dropped this round (2): a Society-standing display (`/society`) - `SocietyRankCelhan`/`Eldweb`/`Radblo`, read externally from `Entity/Enlightenment.cs` by the already-shipped `EnlightenmentReady` (idea 66), could not be located at their own declaration site in `Player_Properties.cs` (searched in full, zero "Society" hits) or any other file fetched this round; per this repo's standard, a member confirmed only by a different class's *read* access (not a write, and not this round's own fetch of the declaring file) is not enough to build a whole idea on, so this was left unproposed rather than guessed. A fishing-mechanic mod - the full `Source/ACE.Server/WorldObjects/` directory listing (fetched this round) contains no `Fishing.cs` or fishing-related weenie class of any kind, confirming ACE has no fishing system at all to wrap, unlike (say) chess (`Player_Chess.cs` exists and is the hook `ChessStats`, idea 62, already uses) - not a real ACE mechanic to hook, so not proposed.
Command-clash check: proposed names defensecheck, mycorpse, pklite, pklitezone, networth, raretally must still be re-grepped against `%TEMP%\cmds.txt` (327+ built-ins) before coding, per every prior round's practice - not done this round (no in-game/server access from this research pass).

---

## Round 14 (ideas 81-85), researched 2026-09-22: writable books, item mana/charges, quest chests, fellowship rejoin window, spell fizzle preview

Checked all 80 existing one-line summaries in STATUS.md (all built) and the entire prior IDEAS.md text (Rounds 1-13) first to rule out overlap. Per this round's brief, looked specifically at territory named as under-mined: house hooked items (found nothing new beyond `HouseHookMeter`/idea 56 - `Hook.cs` itself was opened this round and exposes no additional public surface worth a second mod), salvage bag workmanship math (re-opened `Player_Crafting.cs` in full; the salvage formula is entirely inside `GetSalvageWorkmanship`/`GetStructure`, both already the exact hook `CraftForecast`/idea 43 uses - no new, distinct hook found), weapon/armor wear and repair (confirmed this round, by full-file fetch of `WorldObject.cs`/`WorldObject_Properties.cs`/`WorldObject_Combat.cs`, that ACE has **no repair or item-wear/durability-loss mechanic at all** - `MaxStructure`/`Structure` exist only on `Corpse`/lockable containers for the unrelated lock-difficulty system, not on weapons/armor; retail AC never had item durability loss either, so this is confirmed not a real mechanic to hook, not a research gap), fellowship formation (found real new territory below), quest-reward chests (found real new territory below), spell fizzle (found real new territory below), emotes/animations beyond patrol NPCs (searched `EmoteManager.cs`'s directory - the file itself 404s at every guessed path this round, same as Round 9's finding; no new emote hook located, not proposed on), item mana/charges (found real new territory below), book/writable pages (found real new territory below), trade window (re-opened `Player_Trade.cs` in full - already thoroughly covered by the already-shipped `TradeLedger`/idea 38 and the `IsAttunedOrContainsAttuned`/`CheckUniques`/busy-state checks it reads; no new distinct hook found beyond what TradeLedger already uses). All member accessibility below was confirmed by direct fetch of the declaring file via github-second-brain (ACEmulator/ACE, master), quoting the exact line with its accessibility keyword visible - no member is cited from bare identifier usage inside its own declaring class alone. No new spells/models, no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords.

81. BookAudit (feas 4) - Sentinel-only moderation log for player-authored book content, closing a real, previously-unexamined gap: ACE lets any player write arbitrary free text into an inscribable book (a griefing/harassment/illicit-content vector no prior round in this repo has looked at, since `Book.cs`/`Player_Book.cs` were never opened before this round) with zero server-side record of who wrote what, when, or what was changed or deleted. Hooks, all confirmed `public` by direct fetch of `WorldObjects/Player_Book.cs` (`ACE.Server.WorldObjects.Player`, full file): `HandleActionBookAddPage(uint bookGuid)`, `HandleActionBookModifyPage(uint bookGuid, int pageId, string pageText)`, `HandleActionBookDeletePage(uint bookGuid, int pageId)` - the exact three network-action entry points for all book writes, each calling into `Book.AddPage`/`ModifyPage`/`DeletePage` (all confirmed `public`, `WorldObjects/Book.cs`, full file fetched this round). A Harmony postfix on each of the three `Player` methods (not the `Book` methods, so the log captures the acting player's identity directly from `this`/`Guid`/`Name`/`Session.Account` without needing a second lookup) appends one line per write to `bookaudit.log`: timestamp, player name, book guid, action (add/modify/delete), page index, and the page text itself (for modify/add) so a "someone wrote something awful in a book at the crossroads" report can be investigated after the fact - the same audit-trail shape as the already-shipped `TradeLedger` (idea 38) and `LootWatch` (idea 45), just for a mechanic ("what did they write") neither of those covers. Never blocks or alters a write - always lets the original method run. Admin `/bookaudit [player] [count]` tails the file, reusing the `TradeLedger`/`BugNote` tail-file pattern already shipped. Settings: LogFile, MaxLines, LogFullText (off hides the text itself and logs only length + action, for servers that want metadata-only). Risks: log volume on a busy scribing server (rotate/cap, same as every prior file-log mod); the postfix must read the page text *after* the real method validates it (`ModifyPage`/`DeletePage` both check authorship internally and can no-op) so the log should also capture the `bool` success each real method returns, not assume every call succeeded. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Book.cs and .../Book.cs (both fetched in full this round - first time either file has been opened in this repo's research).

82. ItemChargeWatch (feas 5) - Read-only player command listing every equipped item that runs on mana/charges (wands, staves, trinkets, some jewelry) alongside its current/max mana, so a caster can see at a glance what is about to run dry mid-fight instead of discovering it only when a cast silently fails - the "item mana/charges" gap named directly in this round's brief, and a genuinely different angle from the already-shipped `LuminanceLedger` (idea 61, the player's own XP-derived augmentation currency) and `ManaStone`'s own in-game use (a manual refill tool, not a status readout). Hooks, both confirmed `public` get/set by direct fetch of `WorldObjects/ManaStone.cs` this round, where they are read and written on arbitrary target items from a *different* declaring class than the item itself (`target.ItemCurMana`, `target.ItemMaxMana`, `item.ItemCurMana`, `item.ItemMaxMana` - all on `WorldObject`, accessed from `ManaStone`, satisfying this repo's external-access verification rule): `WorldObject.ItemCurMana` (`int?`) and `WorldObject.ItemMaxMana` (`int?`). Command iterates `player.EquippedObjects.Values` (already-verified public collection, used identically by the already-shipped `Creature_Magic.cs` cross-reference in idea 79/`NetWorthCheck`) filtering to items where `ItemMaxMana.HasValue`, and prints each item's name, current/max mana, and percent remaining, sorted lowest-percent-first so the most urgent item is on top; flags any item at or under a configurable warn threshold. Cmd: `/manawatch` (Player). Settings: WarnPercent (default 20). Risks: none identified - pure property reads, same shape as the already-shipped `BurdenCheck`/`LuminanceLedger`; a caster who dislikes surprises here should also check `/vitae` and `/pkstatus`, already shipped, but this command's scope stays mana-only. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/ManaStone.cs (fetched in full this round, `ItemCurMana`/`ItemMaxMana` read/write on both `this` and an external `target`/`item`, confirming public accessibility from outside the declaring class).

83. QuestChestPreview (feas 5) - Read-only player command previewing whether the caller currently meets a quest-gated chest's requirement *before* walking there, the chest-side counterpart to the already-shipped `HouseEligibilityCheck` (idea 59, which does the same job for mansion allegiance rank). Closes a real, source-confirmed gap: `Chest.CheckUseRequirements(WorldObject activator)` (confirmed `public override`, `WorldObjects/Chest.cs`, full file fetched this round) only evaluates the quest gate **at the moment of opening** - `if (Quest != null) { if (!player.QuestManager.HasQuest(Quest)) EmoteManager.OnQuest(player); else if (player.QuestManager.CanSolve(Quest)) EmoteManager.OnQuest(player); else { player.QuestManager.HandleSolveError(Quest); return new ActivationResult(false); } }` - there is no earlier, player-invokable check, exactly the same shape of gap `HouseEligibilityCheck` closed for `Player_House.cs`. Hooks, all confirmed `public`: `WorldObject.Quest` (the quest-name string gating the chest; confirmed `public`, same declaring file family as `Value`/`PlayerKillerStatus`, `WorldObjects/WorldObject_Properties.cs`, re-checked this round for the `Quest`/`QuestRequirement` section specifically), `QuestManager.HasQuest(string)` and `QuestManager.CanSolve(string)` (both confirmed `public`, `Managers/QuestManager.cs`, already used identically by the already-shipped `QuestFlagInspector`/idea 74 and `PathChoice`/idea 27), and `QuestManager.GetNextSolveTime(string)` (confirmed `public`, same file, same idea-74 verification) to report a cooldown-blocked chest's remaining wait rather than a flat yes/no. Command reads the caller's last-targeted or last-appraised `Chest` (reusing the existing last-appraised-object pattern shared by `PriceCheck`/`VendorStock`/`HouseEligibilityCheck`/`ItemSetPreview`/`TinkerHistory`) and reports: no quest requirement / eligible now / "you don't have the quest flag yet" / "on cooldown, ready in N" / "max solves reached" (the last two derived the same way `QuestFlagInspector` already annotates a flag's state). Cmd: `/chestcheck` (Player). Settings: none (read-only, no patches - every method called is already safe for `CheckUseRequirements` itself to call on every open attempt). Risks: none identified for the read itself; `Chest.Quest` may be null for the large majority of ordinary loot chests (a `ManaForge`-style or scripted quest chest is the actual target case) - the command must say "no quest requirement" plainly rather than implying every chest is quest-gated. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Chest.cs (fetched in full this round - first time this file has been opened in this repo's research) and `Managers/QuestManager.cs` (re-confirmed `HasQuest`/`CanSolve`/`GetNextSolveTime`, verified in earlier rounds).

84. FellowshipRejoinWindow (feas 4) - Read-only player command answering "if I leave this fellowship right now, can I get back in," filling a real fellowship-formation gap distinct from the already-shipped `FellowshipShareToggle` (idea 51, XP-sharing only) and `FellowshipPulse` (idea 44, in-range status only): a **locked** fellowship (set via emote, not a player action - confirmed by the exact source comment this round) blocks new recruits entirely except for players who departed within the last 600 seconds, per `Entity/Fellowship.cs`'s `AddFellowshipMember`, fetched in full this round: `if (IsLocked) { if (!DepartedMembers.TryGetValue(newMember.Guid.Full, out var timeDeparted)) { ...refuse... } else { var timeLimit = Time.GetDateTimeFromTimestamp(timeDeparted).AddSeconds(600); if (DateTime.UtcNow > timeLimit) { ...refuse... } } }` - the only in-game signal any of this exists is a single broadcast line sent once, at the moment of locking ("Your fellowship is now locked. You may not recruit new members. If you leave the fellowship, you have 15 minutes to be recruited back into the fellowship.", confirmed by direct fetch of `UpdateLock`, same file), easy to miss or scroll past, with no way to check the state afterward. Hooks, all confirmed `public` fields on `Fellowship` (`ACE.Server.Entity`, full file fetched this round): `IsLocked` (`bool`), `DepartedMembers` (`Dictionary<uint, int>`, guid to Unix-timestamp-of-departure), `FellowshipLocks` (`Dictionary<string, FellowshipLockData>`, when/why the lock was set, keyed by the emote's lock name). `Player.Fellowship` itself is confirmed public (used throughout this repo's already-shipped fellowship mods). Command: `/fellowlock` (Player, must currently be in a fellowship) reports whether `Fellowship.IsLocked`, and if the caller's own guid is present in `DepartedMembers` (meaning they already left and are within the grace window), the exact seconds remaining before the 600-second rejoin eligibility expires. Settings: none (read-only, no patches - reads only, matching `GetMemberSharePercent`'s sibling `AddFellowshipMember`/`UpdateLock` methods which this command never calls). Risks: none identified for the reads themselves; `FellowshipLockData`'s own member names were not opened this round (only its dictionary key/value shape via `FellowshipLocks`) - the command does not need them, so this does not block the idea, but is flagged per this repo's standing citation practice for anything not independently re-verified. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/Entity/Fellowship.cs (fetched in full this round - `AddFellowshipMember`, `UpdateLock`, `IsLocked`, `DepartedMembers`, `FellowshipLocks` all quoted directly).

85. SpellFizzleForecast (feas 4) - Read-only player command previewing the caller's fizzle chance for a given spell before committing to the cast, the magic-school counterpart to the already-shipped `CraftForecast` (idea 43, which does the same job for combines/tinkering) and closing the exact "spell-cast failure/fizzle mechanics" gap this round's brief named directly. Confirmed this round by direct fetch of `WorldObjects/Player_Magic.cs` (full file): the real cast path's `public CastingPreCheckStatus GetCastingPreCheckStatus(Spell spell, uint magicSkill, bool isWeaponSpell)` reads `var difficulty = spell.Power;` then, when in range, calls `SkillCheck.GetMagicSkillChance((int)magicSkill, (int)difficulty)` and rolls a random number against the result to decide pass/fail - i.e. the exact, real fizzle-chance formula, computed fresh on every single cast attempt. `SkillCheck.GetMagicSkillChance(int skill, int difficulty)` is confirmed `public static` by direct fetch of `WorldObjects/SkillCheck.cs` in full this round (a small, pure, two-line wrapper: `return GetSkillChance(skill, difficulty, 0.07f);`, itself also `public static`, a logistic curve with no side effects or state) - the mod never needs to touch `GetCastingPreCheckStatus` itself (which also fires the actual RNG roll and player-visible fizzle animation/message as a side effect and would not be a safe "preview") and instead calls the pure formula function directly with the same two inputs: the player's own current magic skill for the spell's school (`GetCreatureSkill(magicSkillId).Current`, the same public accessor pattern `StatCard`/`DefenseCheck` already use) and `spell.Power` (confirmed accessible the same external-access way this repo already treats `EquipmentSetId`/`AllegianceMinLevel` - read as `spell.Power` from `Player_Magic.cs`, a different declaring class than `Spell`). Command: `/fizzlecheck` (Player) reports the computed fizzle-chance percentage for the caller's currently-selected/last-cast spell (the same "which spell is this about to be" plumbing gap `CraftForecast` already flagged and left as a pre-coding task, not independently solved this round) and the raw skill-vs-difficulty numbers behind it. Settings: none (read-only, calls only the pure static formula function, never the real cast-check method). Risks: none identified for the formula call itself - it is a pure function with no state; the exact argument-resolution plumbing for "the spell about to be cast" (spellbar selection vs. last-attempted spell) must be confirmed against the real cast-entry method before coding, the same open item `CraftForecast` already carries for combines. Source: https://raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/WorldObjects/Player_Magic.cs (re-fetched in full this round, `GetCastingPreCheckStatus` section located via full-file search) and `WorldObjects/SkillCheck.cs` (fetched in full this round - first time opened in this repo's research).

### Sources consulted (round 14)
Raw source fetched in full (raw.githubusercontent.com/ACEmulator/ACE/master/Source/ACE.Server/): WorldObjects/Book.cs, WorldObjects/Player_Book.cs, WorldObjects/ManaStone.cs, WorldObjects/Chest.cs, WorldObjects/WorldObject_Equipment.cs, WorldObjects/Player_Fellowship.cs, Entity/Fellowship.cs (re-fetched in full, first read since Round 4's `MinionCleanup` context - `AddFellowshipMember`/`UpdateLock`/`DepartedMembers`/`FellowshipLocks` sections newly quoted this round), WorldObjects/Player_Trade.cs (re-fetched in full - confirmed already thoroughly covered by the shipped `TradeLedger`, no new hook), WorldObjects/Player_Magic.cs (re-fetched in full, `GetCastingPreCheckStatus`/fizzle section newly located this round), WorldObjects/SkillCheck.cs (first read this round); directory listing of `Source/ACE.Server/WorldObjects/` (full listing, re-confirmed no `Repair`/`Durability`/`Fishing`/`Duel` class exists and located `Book.cs`/`Chest.cs`/`ManaStone.cs`/`SkillCheck.cs` for first-time reads); `WorldObjects/WorldObject_Item.cs` guessed and 404'd (no such file - item-level logic lives in `WorldObject_Equipment.cs`/`WorldObject_Properties.cs` instead, used those). No WebSearch this round - all five ideas came from this repo's own previously-unopened source files, per this round's brief to look harder at named systems already in the codebase; STATUS.md's full 80-summary list and the entire prior IDEAS.md text (Rounds 1-13) were read first to rule out overlap.
Dropped this round (3, per the brief's named areas that turned out not to be real gaps): a weapon/armor wear-and-repair mod - confirmed no durability-loss mechanic exists anywhere in `WorldObject.cs`/`WorldObject_Properties.cs`/`WorldObject_Combat.cs` (retail AC never had item durability loss; `Structure`/`MaxStructure`-named fields, where they exist, belong to the unrelated lock-difficulty system on containers, not to weapons/armor), so there is nothing to hook. A house-hooked-items mod beyond `HouseHookMeter` - `Hook.cs` (opened this round) exposes only the already-used `HouseCurrentHooksUsable`/`HouseMaxHooksUsable`/`Hooks` surface, no new distinct member. A salvage-bag workmanship-math mod beyond `CraftForecast` - `Player_Crafting.cs`'s salvage formula (`GetSalvageWorkmanship`/`GetStructure`) is the exact same hook `CraftForecast`/idea 43 already previews; a second idea here would just be `CraftForecast` restated, not new territory.
Command-clash check: proposed names bookaudit, manawatch, chestcheck, fellowlock, fizzlecheck must still be re-grepped against `%TEMP%\cmds.txt` (327+ built-ins) before coding, per every prior round's practice - not done this round (no in-game/server access from this research pass).

---

## Round 15 (ideas 86-88), researched 2026-09-22: aggro/threat, allegiance-XP accounting, recall cooldown - well confirmed thinning

Read all 325 lines of IDEAS.md (ideas 1-85, rounds 1-14) and STATUS.md in full before researching, to rule out overlap. Checked nine named unmined areas against real ACE source via github-second-brain (ACEmulator/ACE, master), full-file fetches unless noted; only 3 of 9 areas produced a genuinely new, safely-scoped, verified hook - a materially lower hit rate than Rounds 13-14 (5/5 each), consistent with Round 14's warning that the well is thinning. Constraints kept: no speed mods, no many-object mods, no ace_auth/ace_shard, no passwords, no new spells/models. Command names (threattable, allegiancexp, recallcooldown) are proposed only - not re-grepped against `%TEMP%\cmds.txt` this round (no in-game/server access from this research pass); must be checked before coding.

86. ThreatTableView (feas 4) - Sentinel-only read-only command showing a targeted/last-appraised monster's live combat-threat state, for diagnosing "who actually has kill credit / why did this mob switched targets" disputes *before* the kill - distinct from the already-shipped LootWatch (idea 45), which only audits *after* a corpse is opened. Hooks, all confirmed public by direct fetch this round: `Creature.DamageHistory` (`public DamageHistory DamageHistory { get; private set; }`, `ACE.Server.WorldObjects`, declared in `Creature_Combat.cs`) exposes `DamageHistory.Damagers` (`public List<DamageHistoryInfo> Damagers => TotalDamage.Values.ToList();`), `TopDamager`/`LastDamager` (both `public DamageHistoryInfo`), and `TotalHealth` (`public float`) - all in `Entity/DamageHistory.cs`, `ACE.Server.Entity`; each `DamageHistoryInfo` (`Entity/DamageHistoryInfo.cs`, `ACE.Server.Entity`) exposes `public readonly ObjectGuid Guid`, `public readonly string Name`, `public float TotalDamage`, `public bool IsPlayer`. Also reports `Creature.AttackTarget` (used pervasively as a public member throughout `Monster_Awareness.cs`, fetched in full this round), `Creature.CurrentTargetingTactic`/`TargetingTactic` (both public, same file), and `Creature.VisualAwarenessRangeSq`/`AuralAwarenessRangeSq` (both public, `Monster_Awareness.cs`, scaled live by the `mob_awareness_range` server property). Cmd: /threattable (Sentinel) on the caller's last-appraised or last-targeted creature - prints current attack target, targeting tactic, live awareness range, and the ranked damager list with names and totals. Settings: none (read-only, no patches, calls only public getters `Creature.DamageHistory` already exposes for its own corpse/looting logic). Risks: `DamageHistoryInfo.Attacker` is a `WeakReference<WorldObject>` - a long-dead attacker's name/guid still print fine since `Name`/`Guid` are captured as plain fields at construction, but `TryGetAttacker()` could return null if GC'd; the command never needs to call it. Source: `Source/ACE.Server/WorldObjects/Creature_Combat.cs`, `Source/ACE.Server/Entity/DamageHistory.cs`, `Source/ACE.Server/Entity/DamageHistoryInfo.cs`, `Source/ACE.Server/WorldObjects/Monster_Awareness.cs` (all fetched in full this round via github-second-brain, ACEmulator/ACE master).

87. AllegianceXpLedger (feas 5) - Read-only player command showing the caller's own allegiance-XP contribution accounting, a real, previously-untouched facet of the patron/mentor system distinct from the already-shipped MentorRank (idea 34, kill-tally-based, mod-owned JSON, no relation to ACE's real allegiance-XP flow) and from AllegianceRoster/AllegianceOfficers/AllegianceMotd/AllegianceBanRoster (all membership/role/messaging, none touch XP). Hooks, all confirmed public by direct fetch of `Player_Allegiance.cs` this round (`ACE.Server.WorldObjects.Player`): `public ulong AllegianceXPCached { get => (ulong)(GetProperty(PropertyInt64.AllegianceXPCached) ?? 0); ... }`, `public ulong AllegianceXPGenerated { ... }` (lifetime XP this character has generated up through its patron chain), `public ulong AllegianceXPReceived { ... }` (lifetime XP received down from vassals) - three properties never referenced by any of the 85 prior ideas. Combined with the already-verified `AllegianceManager.GetAllegianceNode`/`AllegianceNode.Rank` (Round 6) to show rank context. Cmd: /allegiancexp (Player) prints generated, received, and cached (not-yet-distributed) allegiance XP in one line - closing the same "admin-only or invisible number" gap already-shipped ledger/status commands closed for other economies. Settings: none (read-only, no patches). Risks: none identified - pure property reads; must re-grep `%TEMP%\cmds.txt` for `allegiancexp` before coding. Source: `Source/ACE.Server/WorldObjects/Player_Allegiance.cs` (fetched in full this round, `AllegianceXPCached`/`AllegianceXPGenerated`/`AllegianceXPReceived` declarations).

88. RecallCooldown (feas 3) - Adds a configurable shared cooldown across all recall spells (Lifestone Recall, Portal Recall, Primary/Secondary Portal Tie Recall), a real gap: verified via full-file fetch of `WorldObjects/WorldObject_Magic.cs` this round that ACE's only throttle on any recall is each spell's own 2-second `AddDelaySeconds(2.0f)` cast-to-teleport animation delay inside `private void HandleCastSpell_PortalRecall(Spell spell, Creature targetCreature)` (`ACE.Server.WorldObjects.WorldObject`, confirmed **private** by direct fetch - must be patched by name, the same reflection/Harmony-by-name pattern already used elsewhere in this repo for private members) - there is no shared per-player recall cooldown at all, so a player can chain Lifestone Recall -> Portal Recall -> Portal Tie Recall back-to-back as a combat-escape or corpse-run exploit, the same class of risk StuckRescue/HomeStone already flag and cooldown-gate for their own single teleport commands. Hook: Harmony prefix on `[HarmonyPatch(typeof(WorldObject), "HandleCastSpell_PortalRecall")]` checks a mod-owned in-memory `Dictionary<uint, double>` (player guid -> last-recall-timestamp, never the DB) against `Settings.CooldownSeconds`; if still on cooldown, sends a system-chat message with seconds remaining and returns `false` (skip original) - otherwise records the timestamp and returns `true`. Off by default so servers that want retail recall behavior see no change. Cmd: none required (silent gate); optional Admin /recallcooldown status shows current setting. Settings: CooldownSeconds (0 = disabled), ExemptInPk (false), off by default. Risks: this is the weakest of the three - it patches a **private** method by name (more fragile than a public-method patch) and actively gates behavior rather than only reading state, unlike 86-87; the exact `(Spell spell, Creature targetCreature)` parameter binding must be confirmed to compile against a Harmony prefix before shipping; a stale cooldown entry for a since-deleted character should be pruned periodically (same pattern MinionCleanup's sweep uses), not left to grow unbounded. Source: `Source/ACE.Server/WorldObjects/WorldObject_Magic.cs` (fetched in full this round via github-second-brain, ACEmulator/ACE master; `SpellType.PortalRecall` dispatch and `HandleCastSpell_PortalRecall` body confirmed, all four Lifestone/Portal/Tie recall spell ids confirmed to share this single private method).

### Sources consulted (round 15)
Checked against real source via github-second-brain (ACEmulator/ACE, master) - 9 named areas from the task brief: (1) Heritage groups/racial bonuses - DEAD END: `Factories/PlayerFactory.cs`'s `GetMasteries()`/`SetInnateAugmentations()` run once at character creation only, writing into properties already readable by existing mods; no live mechanic to hook. (2) Character-creation stat allocation review - DEAD END: `ValidateAttributeCredits()` (private static, `PlayerFactory.cs`) runs before a Player/Session exists; no in-world moment to preview. (3) Weapon speed/attack-height mechanics - DEAD END this round, unexplored rather than exhausted: `Creature_Combat.cs`/`WorldObject_Weapon.cs`/`Monster_Awareness.cs` checked, found nothing beyond what the already-shipped DefenseCheck (idea 76) surfaces; a deeper trace of `Creature_Melee.cs`/`Creature_Missile.cs` was not completed. (4) Monster AI aggro range/threat tables - HIT, idea 86. (5) Loot generation tier previews - DEAD END: `Factories/LootGenerationFactory*.cs`/`Chest.cs` treasure-profile fields are static weenie/DAT configuration fixed at authoring time, the same "static data, not live per-request state" shape Round 11 already rejected. (6) Guild/patron-apprentice mentor mechanics beyond MentorRank - HIT, idea 87 (different angle: real allegiance-XP accounting, not a mod-owned kill tally). (7) Title unlocks beyond CharacterTitle enum usage - CONFIRMED DEAD END (re-verified, matches Round 8): `Player_Character.cs`'s `AddTitle`/`SetTitle`/`GetTitle`/`NumCharacterTitles` are the only title-registry members; still no enumerable collection of a player's earned titles. (8) Recall spell cooldowns - HIT, idea 88. (9) Fresh GitHub issues pass - LIMITED/INCOMPLETE: the available github-second-brain tool fetches one issue by number, not a search/list, so this round could only re-confirm issues already cited in Rounds 1-14 (#718, #950, #1063, #1170, #1586, #1719, #1723, #1797); no new open-issue lead was found, but this is a tooling gap, not a claim that no more issues exist.
Raw source fetched in full via github-second-brain this round: `Source/ACE.Server/Factories/PlayerFactory.cs`, `Source/ACE.Server/WorldObjects/Creature_Combat.cs`, `Source/ACE.Server/Entity/DamageHistory.cs`, `Source/ACE.Server/Entity/DamageHistoryInfo.cs`, `Source/ACE.Server/WorldObjects/Monster_Awareness.cs`, `Source/ACE.Server/WorldObjects/Player_Allegiance.cs`, `Source/ACE.Server/WorldObjects/Player_Character.cs` (re-fetched), `Source/ACE.Server/WorldObjects/WorldObject_Magic.cs`.
Recommendation: 3 of 9 areas hit (86-88), a lower rate than Rounds 13-14 - the well is confirmed thinning, and structurally so: 4 of 6 dead ends failed for the same reason (static/one-shot/DAT-baked config, not live per-player state). Idea 88 is flagged as the weakest of the three (private-method patch, gates behavior rather than only reading it) and should get extra scrutiny before being picked up. Round 16 is worth attempting but should go in with a narrower brief: re-read 2-3 large files this project has already opened for other reasons but not fully mined section-by-section (`Player_Skills.cs`, `Player_Inventory.cs`, `Creature_Melee.cs`/`Creature_Missile.cs`, `WorldObject_Combat.cs`) rather than opening new files by guessed name, and ideally with a real GitHub issue search/list capability rather than fetch-by-number-only, since area 9 could not be honestly attempted this round without one. Expect fewer than 3 ideas from Round 16.
