# mods-proposed status (source only, nothing deployed)
XpBoost | READY (compiled) | 2026-09-20 | XP multiplier via Player.EarnXP prefix, /xpboost admin cmd
GentleDeath | READY (compiled) | 2026-09-20 | caps dropped items and vitae penalty, /gentledeath admin cmd
WhereIsEveryone | READY (compiled) | 2026-09-20 | admin /who2, /gotoplayer, /bringplayer
AnnounceEvents | READY (compiled) | 2026-09-20 | scheduled broadcasts, admin /announce start|stop|say
HomeStone | READY (compiled) | 2026-09-20 | player /mark and /gomark with cooldown, PK-timer block
PkGuard | READY (compiled) | 2026-09-21 | blocks PvP damage in listed landblocks via Player.TakeDamage prefix, admin /pkguard
AutoLoot | READY (compiled) | 2026-09-21 | opt-in /autoloot: Corpse.Open postfix moves coins/notes/gems to pack
BuffBot | READY (compiled) | 2026-09-21 | player /buffme: level-6 self buffs, cooldown, off by default
Leaderboard | READY (compiled) | 2026-09-21 | player /top: top levels via PlayerManager.GetAllPlayers plus monster-kill tally (Creature.OnDeath postfix, JSON file)
AdminAudit | READY (compiled; fixed out-param attribute) | 2026-09-21 | logs Advocate+ in-game commands to file via CommandManager.GetCommandHandler postfix, redacts password commands
DeathReport | READY (compiled) | 2026-09-21 | broadcasts fun line on player death, deaths.log, player /lastdeath returns to death spot
RestartWarn | READY (compiled; fixed static settings access) | 2026-09-21 | extra countdown warnings for ACE-scheduled shutdowns plus player /restartwhen; read-only, never shuts down
MilestoneRewards | READY (compiled) | 2026-09-21 | off-by-default skill credits at configured levels via Player.CheckForLevelup postfix, guid-keyed JSON, player /milestones
IdleKick | READY (compiled; needs in-game test - thread safety + activity detection unverified) | 2026-09-21 | off-by-default idle warn then Session.LogOffPlayer; skips staff, combat, PK timer
TimedMute | READY (compiled) | 2026-09-21 | Admin /mute /unmute /mutes: custom-duration mute over stock gag, JSON list, re-applied at login
SkillRespec | READY (compiled; off by default; test on a throwaway character before enabling) | 2026-09-21 | off-by-default player /respec <skill> [confirm] via Player.ResetSkill, cooldown, pyreal cost, combat refusal
PkNight | READY (compiled; off by default; two-character in-game test needed) | 2026-09-21 | off-by-default scheduled/manual PK window via Player.CheckPKStatusVsTarget postfix (damage rules, no status changes), announcements, Admin /pknight
LoginGreeter | READY (compiled; edit the placeholder rules text before use) | 2026-09-21 | first-login vs returning greeting, online count, first-login tips and /rules over ACE's static server_motd; Enabled default true
ServerPulse | READY (compiled) | 2026-09-21 | read-only Sentinel /pulse [top]: online count, loaded/dormant landblocks, busiest landblocks by players/creatures
WorldBoss | READY (compiled; off by default; boss reward uncapped setting default 0 - keep small) | 2026-09-21 | event: /worldboss start|stop|status spawns ONE boss creature, announces, names top damager, optional pyreal reward, schedule off by default
KillRace | READY (compiled; off by default; reward capped by RewardCap) | 2026-09-21 | event: /killrace start|stop|status, player /racetop, timed kill-count contest with announcements and capped reward
TreasureHunt | READY (compiled; off by default; reward capped by MaxRewardPyreals) | 2026-09-21 | event: riddle hunt via /hunt, /hint gives distance bands only, first player in radius wins; no world objects
HotspotAlert | READY (compiled; off by default; timer only starts if Enabled at load - restart to enable) | 2026-09-21 | throttled admin whispers when a landblock holds too many creatures/players, Sentinel /hotspots, read-only, no files
TriviaNight | READY (compiled; off by default; questions trimmed to 9 - review lore/wording before enabling; prize capped by MaxPyreals) | 2026-09-21 | event: /trivia start|stop|status, first correct chat answer wins a round via Player.HandleActionTalk postfix, scoreboard, optional capped prize
StatCard | READY (compiled) | 2026-09-21 | player /statcard: caller's own level/XP/attributes/vitals/skills; read-only, no files
BugNote | READY (compiled; DataFile path is relative to the server working dir, not the mod folder) | 2026-09-21 | player /bugnote <text> (rate-limited), Sentinel /bugnotes and /bugnoteclear; stores only char name, time, landblock, text
RaiseSkeleton | READY (compiled; off by default; needs in-game test: several CombatPets per owner is not how ACE normally works; SQL in sql/ NOT applied) | 2026-09-21 | player /raiseskel consumes nearest monster corpse -> ACE CombatPet minion up to floor(Self/10) else feral skeleton; /minions, /dismiss
LoginShield | READY (compiled; off by default; per-character login spawn-protection window only; untargeted spells and DoT not covered) | 2026-09-21 | short 0-damage shield after login via Player.TakeDamage prefix, ends on first attack if BreakOnAttack, Admin /loginshield; no credentials, accounts, IPs, DB or files
RaiseSkeleton DEPLOYED on ace-vr-server (test server) 2026-09-21: SQL 900021240/900021241 applied, mod in /opt/acbuilds/mods-vr/RaiseSkeleton, Enabled=true
SettingsPeek | READY (compiled; off by default; allowlist of bool/long/double keys only, no strings, no ACE Config; key names to verify on the server build) | 2026-09-21 | player /serverrules prints the allowlisted gameplay properties (rates, PK rules) via PropertyManager.GetBool/GetLong/GetDouble
MinionCleanup | READY (compiled; off by default; destroys only CombatPets whose wcid is in MinionWcids [default 900021240]; on owner logout/death/teleport + periodic sweep; MaxDistance 60 may remove minions during long runs; not yet installed anywhere) | 2026-09-21 | fixes RaiseSkeleton multi-pet leak; Admin /minionsweep
PathChoice | READY (compiled; off by default; paths stored as ACE quest stamps path_<name> via QuestManager; add matching rows to the ace_world quest table (max_Solves 1) before relying on InqQuest gating - untested) | 2026-09-21 | /path, /path choose <name>, Admin /path reset <player>; PatchClass.GetPath(Player) for other mods; optional title
MinionOrders | READY (compiled; off by default; /order attack|hold|follow only for the caller's own minions in MinionWcids; hold = stand still not heel, follow = resume stock AI; Harmony patches on CombatPet target search untested) | 2026-09-21 | pairs with RaiseSkeleton + MinionCleanup
CorpseBurst | READY (compiled; off by default; casts an existing ring/arc spell (default FlameRing) from the player at the nearest living monster to the consumed corpse; no damage scaling by corpse health; no per-cast target cap; PK behaviour untested) | 2026-09-21 | player /burst
AmbushStrike | READY (compiled after adding the missing AttackHook using; off by default; postfix on DamageEvent.CalculateDamage multiplies Damage x2 [max 5] for Player->creature melee/missile when the creature has no AttackTarget; whether the bonus is what actually reaches the health bar is untested) | 2026-09-21 | rogue sneak-attack approximation; optional RequireBehind and RequirePathQuest (path_rogue); Admin /ambush
StuckRescue | READY (compiled; off by default; renamed /unfreeze -> /unstick because ACE has /unfreeze; outdoor ring search within 30 units on walkable terrain, otherwise recall to Player.Sanctuary; refused in combat/PK timer, 300s cooldown; Sanctuary read from a mod untested) | 2026-09-21 | player /unstick, Admin /unstickplayer <name>
PetSpells | READY (compiled; off by default; SpellOnTarget/SpellOnOwner default empty so nothing casts until you set SpellId names; pet.TryCastSpell from a non-player caster untested for all spell types, no cast animation) | 2026-09-21 | timer sweep of minions in MinionWcids casts an existing spell at their AttackTarget or on the owner; cooldown >=5 s; Admin /petspells
DecayClock | READY (compiled; off by default; minion decay only outside a configured Nexus, health capped via Creature.UpdateVital then Destroy; client health bar refresh and IsDaytime match untested; Nexus.Cell is a DECIMAL uint in JSON) | 2026-09-21 | pairs with RaiseSkeleton; player /decay
CommandIndex | READY (compiled; off by default; /modhelp lists allowlisted custom commands at or below the caller's access level using CommandManager.GetCommands(); empty Description shows name only) | 2026-09-21 | read-only, no patches, no files
MentorRank | READY (compiled; off by default; rewards default 0 and capped; needs same fellowship + within 100 units; no apprentice XP bonus, chat line only; state in mentorrank-data.json by character guid) | 2026-09-21 | player /mentor invite|accept|leave|status, Admin /mentor list; CheckForLevelup postfix
Redraw | READY (compiled; off by default; re-sends already-tracked objects to the caller only via ObjMaint.GetKnownObjectsValues + PhysicsObj.enqueue_objs then SendUpdatePosition; cannot fix a real server-side visibility bug; whether the client rebuilds an object it already holds is untested) | 2026-09-21 | player /redraw, Admin /redrawplayer <name>; 30 s cooldown, 20 per hour, max 300 objects
HeadsetPreset | READY (compiled; off by default; read-only Admin /headset prints teleport_visibility_fix and mob_awareness_range and points to /modifylong and /modifydouble; ACE has no per-player visibility radius or object cap so no player /lite) | 2026-09-21 | changes nothing
ModTest | READY (compiled; off by default; Admin /modtest runs read-only PASS/FAIL/SKIP checks: mods loaded+Active via ModManager.GetModContainerByName, expected commands via CommandManager.GetCommands, required weenies via GetCachedWeenie, one property read; cannot read other mods' Enabled) | 2026-09-21 | fixes: README/Settings claimed order is an ACE built-in - it is not; MinionOrders now checked
TradeLedger | READY (compiled; off by default; postfix on private Player.FinalizeTrade logs only when TradeTransferInProgress is set; whether that flag is still set at postfix time is untested; log tradeledger.log in server working dir, rotated) | 2026-09-21 | character names, items, coins, landblock only; Sentinel /tradelog [n|name]
RentReminder | READY (compiled; off by default; reads the caller's OWN houses via HouseManager.GetCharacterHouses + House.GetRentDue(Player.HousePurchaseTimestamp), shows the soonest due time in UTC; cannot show paid/unpaid; rent queue may be empty for offline-loaded houses) | 2026-09-21 | login reminder when due within WarnDays (3) + player /rent; per-player cooldown; read-only
TeleBack | READY (compiled; off by default; Harmony prefix on Player.Teleport records the previous position only for admins >= MinAccess (Sentinel), last 5 in memory, dictionary capped at 200; /teleback pops it and skips recording the return trip; a teleport that never reaches Player.Teleport can leave a stale undoing mark) | 2026-09-21 | Admin /teleback [n]
AllegianceRoster | READY (compiled; off by default; read-only, caller's own allegiance only via AllegianceManager.GetAllegianceNode; shows monarch/patron/vassals/members/online/level; no last-login field exists; Rank shown is ACE's numeric vassal rank, not an officer title) | 2026-09-21 | player /roster [all|vassals]
PriceCheck | READY (compiled; off by default; read-only; sell price reproduces Vendor.GetBuyCost with an assumed BuyPrice multiplier (default 1.0) since no specific vendor is known; Attuned/Bonded shown as informational, ACE's real sale block is Retained + IsSellable) | 2026-09-21 | player /pricecheck [item name] on last-appraised item or own inventory
CraftForecast | READY (compiled; off by default; read-only; calls RecipeManager.GetRecipe/GetNewRecipe/GetRecipeChance/GetTinkerChance directly, nothing reproduced; salvage-yield preview not built; inherits ACE's own unbounded TinkeringDifficulty index for 10+ tinkered items, caught by try/catch) | 2026-09-21 | player /forecast <target item name> using the last-appraised item as the tool
FellowshipPulse | READY (compiled; off by default; read-only, caller's own Fellowship only; shows leader, members, level, vitals %, in-range via Fellowship.WithinRange, ShareXP/EvenShare; ACE's roster holds online members only, so no offline case; range thresholds not printable) | 2026-09-21 | player /fellow
LootWatch | READY (compiled; off by default; read-only postfix on Corpse.Open, logs when a non-killer/non-fellowship player opens a monster corpse; never touches ACE's own HasPermission decision; rotated lootwatch.log) | 2026-09-21 | Sentinel /lootlog [n|name]
AllegianceMotd | READY (compiled; off by default; MOTD keyed by allegiance monarch's stable guid in allegiancemotd-list.json; monarch or patron can set; a patron's live broadcast may only reach their own branch, not the whole allegiance - unverified) | 2026-09-21 | player /amotd, /amotd set <text> at login + on demand
MinionFollow | READY v3 (v2 followed on raise but got stuck after first kill: CombatPet.FindNextTarget never clears a dead AttackTarget, so the pet looks permanently "in combat" to any mod/tick that checks AttackTarget!=null - v3 clears it itself; untested) (compiled; off by default; v1 only started the walk once per second and never progressed it, so the minion just ran in place - v2 also drives PhysicsObj.update_object+UpdatePosition_SyncLocation+SendUpdatePosition every 0.2s while following, matching ACE's own Pet.Tick loop for passive pets; still untested in game) | 2026-09-21 | fixes the "runs but does not follow" bug reported live
EventClock | READY (compiled; off by default; starts/stops ACE's own named events via EventManager.StartEvent/StopEvent on a Days/Start/End schedule, same window logic as PkNight; never drives game logic itself, only ACE's real event system; no catch-up for missed boundaries during downtime) | 2026-09-21 | Admin /eventclock [status]
AgentFeed | READY (compiled; off by default, WebhookUrl empty by default; read-only telemetry - login/logout/death to rotated agent/feed/events.log; optional outbound webhook via HttpClient/Task.Run, only network call in the AI-agent-hooks suite; chat-heard event deferred until AgentDialogue exists) | 2026-09-22 | Sentinel /agentfeed (status only, never toggles); part of the AI-agent-hooks suite (docs/AI_AGENT_HOOKS_PLAN.md)
ZoneSweep | READY (compiled; off by default; Sentinel /zonesweep <landblock hex> [confirm]: preview-then-confirm, re-checks landblock+online-guid-set before acting (stale = restart), never PlayerManager.BootAllPlayers - only Player.LogOut() (normal graceful logoff) on players still in that exact landblock after WarnSeconds; rotated log) | 2026-09-22 | last idea on the 48-idea list; AccessLevel is a documented judgment call, not ACE-mandated
AgentDialogue | READY (compiled after fixing WorldObject.LocalBroadcastRange static-vs-instance access; off by default, WatchedWcids empty by default; postfixes EmoteManager.OnHearChat/OnTalkDirect -> agent/dialogue/inbox.jsonl, polls outbox.jsonl -> speaks via Say broadcast; length-capped + control-char stripped both directions) | 2026-09-22 | part of the AI-agent-hooks suite (docs/AI_AGENT_HOOKS_PLAN.md); no external LLM call inside the game process
AgentActions | READY (compiled after fixing Weenie.Type -> WeenieType and missing ACE.Server.Entity using for GetCell; off by default, AllowedSpawnWcids empty by default; polls agent/actions/inbox.jsonl for speak/broadcast/spawn/teleport_npc, every request logged accepted-or-refused to agent/actions/log.jsonl, teleport_npc hard-refuses Player targets, spawn double-checks WeenieType even past the allowlist) | 2026-09-22 | part of the AI-agent-hooks suite (docs/AI_AGENT_HOOKS_PLAN.md); 3 of 4 done, AgentBridge (network) remains
AgentBridge | READY (compiled; off by default, refuses to start with an empty SharedSecret; loopback-only HttpListener (127.0.0.1/localhost only, port never configurable to a real interface); fixed one real bug on review - GetRawText() preserved client formatting incl. real newlines, breaking the one-line-per-inbox-entry contract, replaced with JsonSerializer.Serialize; also corrected README's Windows http.sys claim - these containers are Linux) | 2026-09-22 | 4th and last of the AI-agent-hooks suite (docs/AI_AGENT_HOOKS_PLAN.md); thin file-append layer over AgentDialogue/AgentActions, does no validation itself; the only mod in this repo that opens a network port - read its README before ever enabling
HouseOverpayRefund | READY (compiled; off by default; fixes ACE #1723 - the bug is real but narrower than the title: plain pyreal coin overpayment is already handled correctly by vanilla, only trade-note (fixed-denomination) overpayment on house buy/rent loses the excess; refund is bounded by construction (consumedValue-owed, only minted after payment actually succeeded); fixed 2 real state-leak bugs on review - Pending[guid] was only set when overpay>0, so a stale nonzero value from an earlier exception OR an earlier early-return path could be picked up and refunded by a later, unrelated, zero-overpay transaction; now unconditionally reset to 0 as the first action of every prefix call) | 2026-09-22 | Player.HandleActionBuyHouse/HandleActionRentHouse pre+postfix pair
AccountStash | READY (compiled; off by default; despite the name, uses ONLY Session.Account (in-memory, per-connection) - no ace_auth/ace_shard access anywhere; a pyreal-only stash shared across every character on one account, balance in a mod-owned JSON file; deposit consumes-then-credits, withdraw debits-then-creates, both refund the moved side on any later failure; shared FileLock correctly serializes two sessions of the same account racing a transaction, cooldown is an extra throttle not the real concurrency guard) | 2026-09-22 | player /stash deposit|withdraw <amount>|balance; reviewed in full before compile given the name/currency handling
FellowshipShareToggle | READY (compiled; off by default; confirmed real gap - no ACE built-in or player command lets a leader flip XP sharing after fellowship creation, only workaround is disband+reform; leader-only /fellowshare on|off|status, DesiredShareXP set + Fellowship.CalculateXPSharing/UpdateAllMembers invoked via Harmony Traverse (both private); fixed on review - DesiredShareXP now reverted in the catch block if the reflection calls throw, so a failed toggle can never leave it drifted from the actual computed sharing state; also fixed an inaccurate comment comparing this to AmbushStrike's usage, which doesn't use reflection) | 2026-09-22 | per-fellowship cooldown
VendorStock | READY (compiled; off by default; player /vendorstock on last-appraised vendor: OpenForBusiness, default vs unique item counts, IsGenerator flag; reset-timer countdown deliberately omitted - ResetTimestamp is protected, confirmed by a real compile failure not just a source read, not worth reflecting into for a cosmetic number) | 2026-09-22 | idea 52; note: an earlier draft under the name VendorStockPeek built a different, mismatched feature from a bad paraphrase and was discarded before compiling, not shipped
PkStatusInfo | READY (compiled; off by default; player /pkstatus, read-only, no patches; caught and fixed two inaccuracies in the sourced idea text itself on re-verification - the static field is PKLogoffTimer not PKLogoutTimer, and the timer math reads pk_timer not pk_respite_timer as the entry implied (pk_respite_timer is a separate key used elsewhere); dropped PkTimestamp and pk_new_character_grace_period as unused-in-code, would have been fabricated numbers not real reads) | 2026-09-22 | idea 53
StuckVendorWatch | READY (compiled; off by default; chose approach (a) - Harmony Traverse read-only field reflection into protected Vendor.ResetTimestamp/ResetInterval, guarded per-vendor with automatic fallback to OpenForBusiness-only wording if the read fails - over the honest-but-weaker (b) fallback, since (b) alone defeats the idea's whole point; found and fixed a real type bug before compiling - ResetInterval is int? not double per Vendor.cs's own usage, the original GetValue<double>() would have thrown on every call and silently degraded to fallback for every vendor; a successful compile does NOT prove the runtime read works, since Traverse.GetValue<T>() is unchecked reflection - untested against a live vendor) | 2026-09-22 | idea 54, final idea of Round 8 (48-54 all built); Sentinel /vendorwatch
HouseGuestList | READY (compiled; off by default; player /myguests, read-only, no patches; every named member (House.Guests, House.StorageAccess, HouseManager.GetCharacterHouses) re-verified public against fresh source fetch, nothing dropped or adjusted from the idea's claims - clean this round; used PlayerManager.FindByGuid (online+offline) instead of the idea's implied online-only lookup, since a guest list would otherwise show almost nobody; deleted-character guests print raw guid instead of erroring) | 2026-09-22 | idea 55, first of Round 9
HouseHookMeter | READY (compiled; off by default; player /myhooks, read-only, no patches; every named member (HouseMaxHooksUsable, HouseCurrentHooksUsable, Hooks, GetCharacterHouses) re-verified public against fresh source fetch, nothing dropped or adjusted - clean this round, same as HouseGuestList; reads house_hook_limit first and says plainly when the server-wide cap isn't enforced rather than printing a misleading number) | 2026-09-22 | idea 56
GeneratorAudit | READY (compiled; off by default; Sentinel /genaudit [landblock], read-only, no patches; resolved the idea's own flagged gap - found GeneratorProfile.cs at Entity/ not WorldObjects/ (the wrong guessed path is why it 404'd during research), confirmed IsPlaceholder/IsMaxed/IsAvailable public, though the mod never needed them directly since it only calls the confirmed-public WorldObject-level aggregates; stall duration self-tracked in memory (no public aggregate timestamp exists), same pattern as DecayClock; never resets/touches a generator) | 2026-09-22 | idea 57
BurdenCheck | READY (compiled; off by default; player /burden, read-only, no patches; GetEncumbranceCapacity/GetAvailableBurden/EncumbranceVal all re-verified public, exact bodies matched the idea text, nothing adjusted; AugmentationIncreasedCarryingCapacity's own accessibility correctly left unverified/out-of-scope since the command never reads it directly, only via the two public methods that already bake it in) | 2026-09-22 | idea 58
HouseEligibilityCheck | READY (compiled; off by default; player /housecheck on last-appraised SlumLord, read-only, no patches; found a genuine declaration for Player.Allegiance/AllegianceNode this time (public get/set), so the accessibility gap I flagged in the idea's own reasoning before dispatch turned out not to apply here; mansion_min_rank read in the exact server-property-first-then-weenie-fallback order the game itself uses) | 2026-09-22 | idea 59
ContractTracker | READY (compiled after fixing a namespace/type name collision - our own project namespace is also "ContractTracker", which shadowed ACE.Server.Network.Structure.ContractTracker and had to be fully qualified; off by default; every named member (Player.ContractManager, ContractManager.ContractTrackerTable, ContractTracker.Contract/Stage/TimeWhenDone/TimeWhenRepeats, Contract.ContractId/ContractName) confirmed public against fresh source, nothing needed a reflection fallback; the real ContractStage enum has 4 named values not 2 (Available, InProgress, DoneOrPendingRepeat, ProgressCounter), and runtime values can exceed ProgressCounter (base+N for progress-tracked contracts) - handled explicitly plus a generic unrecognized-stage fallback) | 2026-09-22 | idea 60; player /mycontracts, read-only, no patches, self-service equivalent of Sentinel-only @contract
LuminanceLedger | READY (compiled; off by default; player /luminance, read-only, no patches; every named member (Player.AvailableLuminance/MaximumLuminance in Player_Properties.cs, PropertyManager.GetDouble in PropertyManager.cs, both luminance_modifier and quest_lum_modifier keys as read inside Player_Luminance.cs EarnLuminance) re-verified public against fresh source fetch, nothing dropped or adjusted from the idea entry; GetDouble returns a Property<double> struct not a bare double, read via .Item as ACE's own EarnLuminance does) | 2026-09-22 | idea 61, first idea of Round 10
ChessStats | READY (compiled after fixing 2 missing usings - ACE.Server.Entity for IPlayer, ACE.Entity.Enum.Properties for PropertyInt; off by default; player /chessrank [player], read-only, no patches; every named member (ChessMatch.Finish/AdjustPlayerRanks reading/writing PropertyInt.ChessRank/ChessTotalGames/ChessGamesWon/ChessGamesLost purely via Player.GetProperty/SetProperty, Player.ChessMatch public field, PlayerManager.FindByName(name, out isOnline) public static returning IPlayer) re-verified public against fresh source fetch, nothing needed reflection; own stats for any player, admin-only for another name, online or offline via FindByName) | 2026-09-22 | idea 62
ItemSetPreview | READY (compiled; off by default; player /itemset [item name] on an item held or last-appraised, read-only, no patches; every named member (WorldObject.EquipmentSetId/HasItemSet/ItemLevel/HasItemLevel/GetSpellSetAll, Spell.Name) re-verified public against fresh source fetch of WorldObject_Set.cs and SpellProperties.cs, nothing needed reflection; deliberately reports the all-tiers spell list via the static GetSpellSetAll rather than the current-tier GetSpellSet, which needs every other equipped set piece plus branching on ItemXpStyle to get the tier number right - out of scope per the idea's own risk note) | 2026-09-22 | idea 63, first idea of the given batch; closes the gap Round 7 dropped for lack of a locatable hook
AllegianceOfficers | READY (compiled; off by default; player /officers, read-only, no patches; every named member (Allegiance.Officers, IsOfficer/IsOfficerRank/IsSpeaker/IsSeneschal/IsCastellan, GetOfficerTitle, AllegianceSpeakerTitle/SeneschalTitle/CastellanTitle, HasCustomTitles, AllegianceManager.GetAllegiance/GetMonarch) re-verified public against fresh source fetch of WorldObjects/Allegiance.cs and Managers/AllegianceManager.cs; the round-flagged caveat resolved - Player.AllegianceOfficerRank (Player_Allegiance.cs) is a plain public int? property, no reflection needed) | 2026-09-22 | idea 64, distinct from the already-shipped AllegianceRoster (which lists the whole member tree by rank, never officer status)
AllegianceBanRoster | READY (compiled; off by default; player /allegianceban, monarch-only via Allegiance.Monarch.PlayerGuid.Full == caller guid, read-only, no patches; every named member (Allegiance.Monarch, BanList, ApprovedVassals, IsBanned, HasApprovedVassal, AddBan, AddApprovedVassal, PlayerManager.FindByGuid) re-verified public against fresh source fetch of WorldObjects/Allegiance.cs; the round-flagged caveat resolved - fetched ACE.Entity.Models.PropertiesAllegiance directly, both per-entry fields (Banned, ApprovedVassal) are plain public bool auto-properties, no reflection needed; command lists dictionary keys by section rather than re-printing the always-true per-entry flag) | 2026-09-22 | idea 65, last idea of Round 10 - closes out ideas 61-65; Round 10 fully built, IDEAS.md needs a new research round for the next loop tick
EnlightenmentReady | READY (compiled; off by default; player /enlightencheck, read-only, no patches; every named member (Enlightenment.VerifyRequirements/VerifyLumAugs/VerifySocietyMaster all public static in Entity/Enlightenment.cs, Player.Enlightenment public int, Player.GetFreeInventorySlots() public instance method) re-verified public against fresh full-file source fetch; corrects Round 8's mistaken drop, which searched only WorldObjects/ for a Player_Enlightenment.cs and never found the real hook in Entity/) | 2026-09-22 | idea 66, first idea of Round 11
AugmentationPlanner | READY (compiled; off by default; player /augcheck, read-only, no patches; every named member (AugmentationDevice.MaxAugs/AugProps/AttributeAugmentationSafetyCapEnabled, Player.AugmentationInnateFamily/AugmentationResistanceFamily/AvailableExperience, AugTypeHelper.IsAttribute/IsResist) re-verified public/public static against fresh source fetch; mirrors AugmentationDevice.VerifyRequirements' own family-then-per-type cap comparisons, never calls DoAugmentation or consumes a gem; the idea's own flagged gap - the per-attribute StartingValue>=96/100 innate-value ceiling inside VerifyRequirements - left unverified and dropped rather than guessed, since it reads CreatureAttribute.StartingValue which wasn't re-opened this round and isn't load-bearing for the family/per-type cap answer) | 2026-09-22 | idea 67, first idea of the given batch after Round 11
VitaeStatus | READY (compiled; off by default; player /vitae, read-only, no patches; closes Round 9's dropped gap - HasVitae/Vitae/VitaeCpPool/DeathLevel all confirmed public on Player itself in Player_Properties.cs, and Player_Death.cs InflictVitaePenalty confirmed VitaeCpPool/DeathLevel are reset on every new death, so the pool tracks progress since the most recent death only, not a lifetime total - worded accordingly; adds an optional PK-respite note via Player.MinimumTimeSincePk/pk_respite_timer without duplicating the already-shipped PkStatusInfo) | 2026-09-22 | idea 68
TinkerHistory | READY (compiled; off by default; player /tinkerhistory [item name] on an item held or last-appraised, read-only, no patches; every named member (WorldObject.TinkerLog public string property in WorldObjects/WorldObject_Properties.cs, ACE.Server.Entity.TinkerLog public class with public Tinkers list and public NumTinkers(MaterialType) in Entity/TinkerLog.cs) re-verified public against fresh source fetch, nothing needed reflection; reports only the raw historical counts ACE already keeps for its own tinkering-cap logic - deliberately does not compute or claim any remaining workmanship bonus, that formula out of scope per the idea's own risk note) | 2026-09-22 | idea 69, distinct from the already-shipped CraftForecast (previews future odds, not past history)
GeneratorNudge | READY (compiled; OFF by default; Admin-only /gennudge on your last-appraised generator, calls the real public virtual WorldObject.ResetGenerator() confirmed by full-file fetch of WorldObject_Generators.cs, correcting Round 9 GeneratorAudit's declined name-search miss; NOT read-only - GeneratorProfile.Reset() (also fetched in full) unconditionally Destroy()s every currently-spawned object including live creatures/items, no kill credit/loot, recursing into nested generators - safe to call in the sense that it is the same code path landblock unload/Chest.Reset() already exercise, but destructive to any live spawns, so the command prints before/after AllProfilesUnavailable/CurrentCreate and the full mutation warning every call rather than acting silently) | 2026-09-22 | idea 70, last idea of Round 11 - closes out ideas 66-70, Round 11 fully built; Round 12 research needed for next loop tick
TickLagAlert | READY (compiled; off by default; Sentinel /lagwatch [on|off|status]; read-only, no patches; every named member (ServerPerformanceMonitor.IsRunning/GetEventHistory5m(MonitorType) public static in Managers/ServerPerformanceMonitor.cs, TimedEventHistory.LastEvent public in Common/Performance/TimedEventHistory.cs) re-verified public against fresh full-file source fetch of both files; polls UpdateGameWorld_Entire.LastEvent on a throttled timer, whispers online Sentinel+ only after SustainedTicks consecutive slow checks, and detects/reports IsRunning==false explicitly rather than a false "all clear" when the stock @monitor performance command was never started) | 2026-09-22 | idea 71, first idea of Round 12
ComponentPrecheck | READY (compiled; off by default; player /compcheck <spellId>, read-only, no patches; verified TryBurnComponents against fresh full-file fetch of WorldObjects/Player_Magic.cs - it IS mutating (line 1213 TryConsumeFromInventoryWithNetworking inside the burn loop), so this mod never calls it, using only the pure Player.HasComponentsForSpell plus a manual re-derivation of SpellFormula.CurrentFormula/SpellComponentsTable for the per-component missing list; command takes a numeric spellId argument rather than an unverified 'currently selected spell' accessor, since no such public hook was found in a full-file search) | 2026-09-22 | idea 72, Round 12
SquelchAudit | READY (compiled) | 2026-09-22 | Sentinel /squelchcheck <player>, read-only chat-squelch audit; every named member (SquelchManager.Squelches/HasSquelches, Player.SquelchManager, Player.SquelchGlobal, PlayerManager.FindByName) re-verified public against fresh source fetch, nothing needed reflection; online-only by design since SquelchManager is an in-memory object built per-login with no offline/database-backed equivalent found; counts only, not squelched names, per the idea's own flagged scope limit
QuestFlagInspector | READY (compiled; off by default; Sentinel /questflags <player>, read-only, no patches; every named member (Creature.QuestManager public get-only property in WorldObjects/Creature.cs, QuestManager.GetQuests()/GetCurrentSolves()/GetMaxSolves()/GetNextSolveTime() all public in Managers/QuestManager.cs, PlayerManager.FindByName already verified by SquelchAudit) re-verified public against a fresh full-file fetch of both files; confirmed the stock QuestManager.ShowQuests(Player) only ever calls Console.WriteLine, never a GameMessageSystemChat - the exact gap this mod closes by sending the same walk to the requesting Sentinel's own chat, annotated per-entry with solves/cooldown state via GetNextSolveTime; online-only by design, same constraint as SquelchAudit, since GetQuests() needs a live loaded Player.Character; one compile-error fix during check-mod.sh - TimeSpan.GetFriendlyString() needed `using ACE.Common.Extensions;`, missing on the first pass) | 2026-09-22 | idea 74, Round 12
ImbueInspect | READY (compiled; off by default; player /imbuecheck on last-appraised weapon, read-only, no patches; GetImbuedEffects() and HasImbuedEffect(ImbuedEffectType) both confirmed public instance methods against fresh full-file fetch of WorldObject_Weapon.cs, but HasImbuedEffect only reads the ImbuedEffect property (slot 1 of 5) not the combined GetImbuedEffects() result, so this mod calls GetImbuedEffects() once and checks each flag itself rather than using HasImbuedEffect and silently missing slots 2-5; ImbuedEffectType (fetched in full from ACE.Entity/Enum) has 15 named values, more than the idea listed - MeleeDefense/MissileDefense/MagicDefense/Spellbook/IgnoreSomeMagicProjectileDamage/AlwaysCritical/IgnoreAllArmor were not in the idea text but are reported too; reports which effects are active, not their numeric strength, per the idea's own risk note) | 2026-09-22 | idea 75, last idea of Round 12 - closes out ideas 71-75, Round 12 fully built; Round 13 research needed for next loop tick
DefenseCheck | READY (compiled; off by default; player /defensecheck, read-only, no patches; both named members (Creature.GetEffectiveDefenseSkill(CombatType) and Creature.GetEffectiveMagicDefense(), both public instance methods in Creature_Combat.cs/Creature_Magic.cs) re-verified public against fresh full-file source fetch; CombatType (ACE.Entity.Enum) resolves via the existing GlobalUsings, same pattern AmbushStrike already uses; command reports melee/missile/magic effective defense in one line, never calls TakeDamage or any combat-resolution path, and does not call GetWeaponMeleeDefenseModifier/GetWeaponMissileDefenseModifier/GetWeaponMagicDefenseModifier/GetDefenseImbues directly since the two public wrappers already bake them in, same pattern BurdenCheck used for GetEncumbranceCapacity/GetAvailableBurden) | 2026-09-22 | idea 76, Round 13
CorpseWatch | READY (compiled; off by default; player /mycorpse plus automatic 5/1-minute whispers before a player's own corpse decays or opens to looting; hooks Corpse.EnterWorld (public override bool EnterWorld() in Corpse.cs), not the protected Player.Die/Creature.CreateCorpse the idea pointer suggested - CreateCorpse's Corpse object is a local variable never returned or stored on a field, so a plain postfix there cannot recover it; EnterWorld() is the first public point where the finished Corpse (VictimId/TimeToRot/Location already set) is available as __instance, confirmed against fresh full-file fetch of Player_Death.cs/Creature_Death.cs/Corpse.cs; TimeToRot (public double?, WorldObject_Properties.cs) and IsLooted (public bool {get;set;}, Corpse.cs) both re-verified public this round; remaining time estimated as CreationTimestamp + TimeToRot - Time.GetUnixTime(), the same anchor ACE's own Corpse.cs logging uses; in-memory tracking of the player's most recent corpse only, capped at MaxTracked; /mycorpse confirmed free of the 327 built-in command names) | 2026-09-22 | idea 77, Round 13
PKLiteZone | READY (compiled after adding missing ACE.Entity.Enum.Properties using for PropertyInt; off by default; player /pklite on|off opts into retail PlayerKillerStatus.PKLite/PKLevel.PKLite while inside Admin-configured landblocks (/pklitezone), reverting on leaving the zone, /pklite off, or logout; PlayerKillerStatus/PkLevel/PkLevelModifier re-verified public {get;set;} on WorldObject via fresh full-file fetch of WorldObject_Properties.cs, confirmed by PKModifier.cs's own external writes to the same properties; only NPK<->PKLite is ever touched, never PK/Free/etc; writes the properties directly rather than through PKModifier's use-item path, since PKModifier.CheckUseRequirements explicitly refuses to let an already-PKLite player change status that way - going through it would have blocked our own revert; RevertOnLogout via a Player.LogOut_Inner prefix, same hook MinionCleanup already uses; scope reduction - no runtime cross-mod coordination with PkGuard/PkNight, documented as an admin configuration rule instead (don't overlap zone landblocks); confirmed "pklite" and "pklitezone" absent from the 327 built-in command names) | 2026-09-22 | idea 78, Round 13
NetWorthCheck | READY (compiled; off by default; player /networth, read-only, no patches; corrects Round 12's mistaken drop of idea 79 - WorldObject.Value re-verified public {get;set;} (public int? Value { get => GetProperty(PropertyInt.Value); set {...} }) in WorldObject_Properties.cs line 1221, the file Round 12 didn't check; Player.Inventory (Container.cs, public Dictionary<ObjectGuid, WorldObject>) and Creature.EquippedObjects (Creature_Equipment.cs, public Dictionary<ObjectGuid, WorldObject>) both re-verified public against fresh full-file fetches; sums Value * (StackSize ?? 1) across both collections, reports pack/worn/combined totals, and excludes (never zeros) items with a null Value, flagging the excluded count separately so an incomplete total is never mistaken for a real zero; does not recurse into side-pack contents separately since Container.TryAddToInventory already rolls a contained item's Value into the side pack's own Value; never touches Vendor.GetBuyCost (that's the separate, already-shipped PriceCheck/idea 49); confirmed "networth" absent from the 327 built-in command names) | 2026-09-22 | idea 79, Round 13
RareTally | READY (compiled; off by default; player /raretally, read-only, no patches; declaring file located this round (Round 13's own idea text had flagged it unlocated, and Player_Character.cs was re-checked and also does not have it) - RaresTierOne..Six and RaresLoginTimestamp are all PropertyInt-backed public instance properties on partial class Player in Source/ACE.Server/WorldObjects/Player_Properties.cs, re-verified against a fresh full-file fetch; RaresTierSeven/RaresTierSevenLogin confirmed commented-out and inert there, matching Corpse.cs's own switch (no case 7) - no fabricated seventh counter added; Corpse.cs's TryGenerateRare re-fetched in full to confirm both the write side (killerPlayer.RaresTierOne++ etc. per tier) and the pity-timer read side (PropertyManager.GetBool("rares_real_time"/"rares_real_time_v2").Item gating a RaresLoginTimestamp comparison), which this command mirrors read-only for an optional "time since/until" line, correctly handling that rares_real_time stores a future timestamp while rares_real_time_v2 stores a past one; confirmed "raretally" absent from the 327 built-in command names) | 2026-09-22 | idea 80, last idea of Round 13 - closes out ideas 76-80, Round 13 fully built; Round 14 research needed for next loop tick
BookAudit | READY (compiled) | 2026-09-22 | Sentinel /bookaudit [n|character], off by default; Harmony postfix on Book.AddPage/ModifyPage/DeletePage (not Player_Book.cs handlers) - all three re-verified public against fresh full-file fetch of Book.cs/Player_Book.cs; chose the Book-level hook over the Player handler the idea named because Player.HandleActionBookModifyPage discards the real success bool (always replies success:true to the client), while Book.ModifyPage/DeletePage return the true outcome and AddPage/ModifyPage/DeletePage each already carry the acting identity as a direct parameter, so no second lookup was needed either way; logs timestamp, character name, action, book guid, page index, success, and page text (LogFullText toggle for metadata-only logging), rotated like TradeLedger; AddPage entries are expected to show blank text since the client always adds a page empty then fills it via a separate ModifyPage call | idea 81, first idea of Round 14
ItemChargeWatch | READY (compiled) | 2026-09-22 | Player /manawatch, off by default, read-only, no patches; ItemCurMana/ItemMaxMana re-verified public {get;set;} on WorldObject via fresh full-file fetch of ManaStone.cs, which reads/writes both on an external target/item of type WorldObject from its own class body (target.ItemCurMana.HasValue, item.ItemCurMana += adjustedRation), confirming external accessibility; player.EquippedObjects.Values (Creature_Equipment.cs) is the same collection ManaStone.cs and the already-shipped NetWorthCheck both use; lists only equipped items with ItemMaxMana > 0, sorted lowest-percent-remaining first, flags items at or under Settings.WarnPercent (default 20) as [LOW]; null ItemCurMana treated as 0; confirmed "manawatch" absent from the 327 built-in command names | idea 82, Round 14
QuestChestPreview | READY (compiled) | 2026-09-22 | Player /chestcheck, off by default, read-only, no patches; the chest-side twin of HouseEligibilityCheck; Chest.Quest/QuestManager.HasQuest/CanSolve/GetNextSolveTime re-verified public against fresh full-file fetches of Chest.cs and QuestManager.cs; deliberately does NOT call Chest.CheckUseRequirements standalone - full-body read showed it fires EmoteManager.OnQuest and QuestManager.HandleSolveError (real player-visible emotes/messages) plus lock-sound broadcasts and open/close state mutation when run outside a real open attempt, so the mod reimplements only the pure-read `if (Quest != null)` predicate by hand (HasQuest/CanSolve/GetNextSolveTime confirmed read-only by reading every line of QuestManager.cs); confirmed "chestcheck" absent from the 327 built-in command names
FellowshipRejoinWindow | READY (compiled) | 2026-09-22 | Player /fellowlock, off by default, read-only, no patches; Fellowship.IsLocked/DepartedMembers (public bool / public Dictionary<uint,int>) re-verified against a fresh full-file fetch of Entity/Fellowship.cs; reuses AddFellowshipMember's own eligibility math (Time.GetDateTimeFromTimestamp(timeDeparted).AddSeconds(600) vs DateTime.UtcNow) to report seconds left in the 600-second rejoin grace window for a caller who left a locked fellowship, or that it has expired, or that the fellowship is locked but does not affect a current member; FellowshipLockData's own member fields not independently re-verified this round (not needed - command never reads FellowshipLocks), flagged per citation practice; confirmed "fellowlock" absent from the 327 built-in command names | idea 84, Round 14
